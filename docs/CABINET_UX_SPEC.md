# CABINET_UX_SPEC.md — личные кабинеты «Будет на ЕГЭ»: проектный документ

Документ собирается по этапам. После каждого этапа — остановка и подтверждение Натальи.
Ветка: `claude/cabinet-ux-spec-0y1hqq`. В этой сессии меняется только этот файл.

Как читать: технические термины объясняются в скобках при первом появлении.
Показатель без указанного источника данных в документе не появляется.
Разделы «Предложения, требующие решения Натальи» и «Открытые вопросы» в конце документа
пополняются на каждом этапе; итоговый вид они получат на этапе 4.

## Оглавление

- [Этап 0. Аудит репозитория (без изменений в коде)](#этап-0-аудит-репозитория-без-изменений-в-коде)
  - [0.1. Из чего состоит репозиторий](#01-из-чего-состоит-репозиторий)
  - [0.2. Что уже есть и работает](#02-что-уже-есть-и-работает)
  - [0.3. Что есть как заглушка](#03-что-есть-как-заглушка)
  - [0.4. Чего нет совсем](#04-чего-нет-совсем)
  - [0.5. Таблица соответствия «типовой элемент EdTech-платформы → наш аналог»](#05-таблица-соответствия-типовой-элемент-edtech-платформы--наш-аналог)
  - [0.6. Границы четырёх сущностей: домашняя работа, «Контроль», тренировочная работа, теоретический зачёт](#06-границы-четырёх-сущностей-домашняя-работа-контроль-тренировочная-работа-теоретический-зачёт)
  - [0.7. Решение по меню](#07-решение-по-меню)
- [Этап 1. Путь ученика и путь учителя](#этап-1-путь-ученика-и-путь-учителя)
  - [1А. Путь ученика (12 этапов: 10 базовых + 2 на новые разделы)](#1а-путь-ученика-12-этапов-10-базовых--2-на-новые-разделы)
  - [1Б. Путь учителя (10 этапов: 8 базовых + 2 на новые разделы)](#1б-путь-учителя-10-этапов-8-базовых--2-на-новые-разделы)
  - [1В. Где пути пересекаются](#1в-где-пути-пересекаются)
- [Этап 2. Экраны кабинетов](#этап-2-экраны-кабинетов)
  - [2.0. Общие правила для всех экранов](#20-общие-правила-для-всех-экранов)
  - [2.1. Адрес главной кабинета и куда попадает пользователь после входа](#21-адрес-главной-кабинета-и-куда-попадает-пользователь-после-входа)
  - [2.2. Карта экранов](#22-карта-экранов)
  - [2.3. Экраны, которые видят оба (в разном виде)](#23-экраны-которые-видят-оба-в-разном-виде)
  - [2.4. Экраны ученика](#24-экраны-ученика)
  - [2.5. Экраны учителя](#25-экраны-учителя)
  - [2.6. Новые компоненты](#26-новые-компоненты)
  - [2.7. Изменения существующих компонентов (в будущих сессиях; меню не трогается)](#27-изменения-существующих-компонентов-в-будущих-сессиях-меню-не-трогается)
- [Этап 3. Автоматизации](#этап-3-автоматизации)
  - [3.0. Общие правила](#30-общие-правила)
  - [3.1. Список автоматизаций](#31-список-автоматизаций)
  - [3.2. Какие можно сделать уже сейчас без сервера, а какие — только после запуска Supabase](#32-какие-можно-сделать-уже-сейчас-без-сервера-а-какие--только-после-запуска-supabase)
  - [3.3. Тексты писем — черновик, на утверждение Натальи](#33-тексты-писем--черновик-на-утверждение-натальи)
- [Этап 4. Сводный план разработки](#этап-4-сводный-план-разработки)
  - [4.0. Находка перед планом: на статическом хостинге код доступа ничего не закрывает](#40-находка-перед-планом-на-статическом-хостинге-код-доступа-ничего-не-закрывает)
  - [4.1. Сущности базы данных](#41-сущности-базы-данных)
  - [4.2. Сессии Claude Code](#42-сессии-claude-code)
  - [4.3. Где собраны решения и вопросы](#43-где-собраны-решения-и-вопросы)
- [Предложения, требующие решения Натальи](#предложения-требующие-решения-натальи)
  - [П-1. Целевое меню кабинета — **Решено: вариант А** (24.09.2026). Вариант Б — план на время, когда появятся другие учителя](#п-1-целевое-меню-кабинета--решено-вариант-а-24092026-вариант-б--план-на-время-когда-появятся-другие-учителя)
  - [П-2. Домашняя и тренировочная работа — **Решено: объединить**](#п-2-домашняя-и-тренировочная-работа--решено-объединить)
  - [П-3. Тренировочные работы — **Решено**](#п-3-тренировочные-работы--решено)
  - [П-4. Теоретические зачёты — **Решено**](#п-4-теоретические-зачёты--решено)
  - [П-5. Убрать фейковые показатели — **Решено: в план, в этой сессии не исправлять**](#п-5-убрать-фейковые-показатели--решено-в-план-в-этой-сессии-не-исправлять)
  - [П-6. Что видит ученик в пункте «Для учителя» — **Решено: карточку своего учителя**](#п-6-что-видит-ученик-в-пункте-для-учителя--решено-карточку-своего-учителя)
  - [П-7. Адрес главной кабинета — **Решено: вариант А**](#п-7-адрес-главной-кабинета--решено-вариант-а)
  - [П-8. На телефоне «Мои задания», «Избранное» и «История решений» недоступны из навигации — **Решено: добавить на экран «Ещё»**](#п-8-на-телефоне-мои-задания-избранное-и-история-решений-недоступны-из-навигации--решено-добавить-на-экран-ещё)
  - [П-9. «Избранное» и «История решений» — **Решено: позже, заглушки остаются**](#п-9-избранное-и-история-решений--решено-позже-заглушки-остаются)
  - [П-10. Что открыто в платном задании без кода — **Решено: только «О задании»**](#п-10-что-открыто-в-платном-задании-без-кода--решено-только-о-задании)
  - [П-11. Может ли ученик сдать зачёт без выдачи учителем — **Решено: да, с условием**](#п-11-может-ли-ученик-сдать-зачёт-без-выдачи-учителем--решено-да-с-условием)
  - [П-12. Что видит ученик после зачёта — **Решено: вариант 1**](#п-12-что-видит-ученик-после-зачёта--решено-вариант-1)
  - [П-13. Сколько вопросов в попытке зачёта — **Решено**](#п-13-сколько-вопросов-в-попытке-зачёта--решено)
  - [П-14. Переименовать «Мои задания» — **Решено: позже** (меню в этой сессии не меняется)](#п-14-переименовать-мои-задания--решено-позже-меню-в-этой-сессии-не-меняется)
  - [П-15. Ежедневная сводка учителю письмом — **Решено: писем учителю пока нет**](#п-15-ежедневная-сводка-учителю-письмом--решено-писем-учителю-пока-нет)
  - [П-16. Код доступа в октябре: к аккаунту или к браузеру — **Решено: без аккаунта (вариант 2)**](#п-16-код-доступа-в-октябре-к-аккаунту-или-к-браузеру--решено-без-аккаунта-вариант-2)
  - [П-17. Кто и где выдаёт коды доступа — **Решено: отдельный аккаунт администратора; способ входа в С-2 выбирает Наталья**](#п-17-кто-и-где-выдаёт-коды-доступа--решено-отдельный-аккаунт-администратора-способ-входа-в-с-2-выбирает-наталья)
- [Позже](#позже)
- [Открытые вопросы](#открытые-вопросы)

Состояние документа:

| Этап | Содержание | Статус |
|---|---|---|
| 0 | Аудит репозитория и таблица соответствия | подтверждён 24.09.2026 с правками (внесены) |
| 1 | Путь ученика и путь учителя | подтверждён 24.09.2026, решения внесены |
| 2 | Экраны кабинетов | подтверждён 24.09.2026, решения внесены |
| 3 | Автоматизации | подтверждён 24.09.2026, решения внесены |
| 4 | Сводный план разработки | подтверждён 24.09.2026, решения внесены |

Решения Натальи, принятые после этапа 0 и действующие на всех этапах:

1. Замороженное меню — живой кабинет из `app/src/content/appNav.ts`. Заглушка «Настройки» не трогается и не проектируется. Код меню в этой сессии не меняется.
2. Роль куратора полностью выполняет учитель ученика: выдача работ, проверка, обратная связь, уведомления. Эти функции проектируются целиком.
3. В кабинете появляются два новых раздела: **«Тренировочные работы»** и **«Теоретические зачёты»**. Вопросы и тексты зачётов пишет Наталья; в документе ни одного вопроса и ни одной формулировки нет — только структура.
4. Для платного доступа код проверяется исключительно на сервере. Проверка кода класса на старом сайте (хеш в браузере) — только прообраз интерфейса.
5. Витринные данные в заглушках в этой сессии не исправляются, а собираются в предложение «Убрать фейковые показатели».
6. Адрес `/` занят лендингом; адрес главной кабинета и место, куда попадает пользователь после входа (ученик и учитель отдельно), предлагаются на этапе 2.

Решения Натальи, принятые после этапа 1:

7. Целевое меню — вариант А из П-1. Вариант Б остаётся в документе как план на время, когда появятся другие учителя.
8. Домашняя и тренировочная работа — **одна сущность «работа»** с полями: *вид* (домашняя / тренировочная), *правила* (обычные / «Контроль»), *способ сдачи* (по одной задаче / целиком).
9. Тренировочные работы: создают учитель и ученик. Работа ученика помечается «самостоятельная», уведомлений учителю не шлёт, но видна учителю в статистике ученика. Состав — «задания на выбор» и «формат ЕГЭ»; у «формата ЕГЭ» честная подпись «из N открытых заданий», N считается из конфига. Состав первой части не зашивается в код: он лежит в конфиге и сверяется с официальной демоверсией ФИПИ, когда она выйдет. Время — на выбор учителя, по умолчанию без ограничения. Одна сдача, ответы можно править до сдачи. Показ результата и разбора — настройка у работы, по умолчанию сразу; у самостоятельных работ — всегда сразу.
10. Теоретические зачёты: независимы от тренажёра. Порог задаёт учитель, значение по умолчанию лежит в конфиге. Не более 3 пересдач, пауза между попытками 24 часа, вопросы и варианты ответов перемешиваются. Все три числа — в конфиге.
11. Ученик в пункте «Для учителя» видит карточку своего учителя.
12. П-5 «Убрать фейковые показатели» принято в план, в этой сессии не исправляется.
13. Вопросы зачётов вводятся через редактор в кабинете учителя, сразу в MVP (MVP — минимальная первая версия, которую запускают для реальных пользователей). Требования: а) редактор доступен только роли «учитель»; б) правильные ответы хранятся и проверяются только на сервере, в браузер ученика не попадают ни в каком виде; в) формулы в тексте вопроса и вариантов — с живым предпросмотром, как их увидит ученик; г) вопрос можно сохранить черновиком, в зачёт попадают только опубликованные; д) правка опубликованного вопроса не меняет уже сданные попытки; е) зачёт без опубликованных вопросов выдать нельзя.
14. Перенос прогресса из браузера — один раз при первом входе, по кнопке, добровольно. Переносятся только реально накопленные счётчики. Если в браузере пусто, кнопка не показывается.
15. Регистрация на платформе — с 14 лет. Путь через родителя сейчас не проектируется и записан в раздел «Позже».
16. Школа и класс — обязательные поля при регистрации ученика.
17. Почтовый сервис не выбирается: в спецификации это абстрактный «почтовый сервис» с требованием — российский, данные хранятся в России.
18. Состав первой части ЕГЭ 2027 — см. п. 9: конфиг и сверка с официальной демоверсией.

Решения Натальи, принятые после этапа 2:

19. Главная кабинета — `/kabinet/`; вошедший пользователь с `/` перенаправляется туда, гость видит лендинг (П-7).
20. На телефоне в экран «Ещё» добавляются «Мои задания», «Избранное», «История решений»; «Настройки» не добавляются. Это разрешение только на эту правку экрана «Ещё»; в этой сессии код не меняется (П-8).
21. «Избранное» и «История решений» — позже, заглушки остаются (П-9).
22. В платном задании без кода открыта только вкладка «О задании» (П-10).
23. Самостоятельный зачёт разрешён, только если опубликованных вопросов в банке задания больше, чем вопросов в одной попытке; тогда каждая попытка — случайная выборка. Самостоятельные попытки помечаются «самостоятельный», не расходуют пересдачи выданного зачёта, видны учителю в карточке ученика. Если вопросов не больше, чем в попытке, кнопки самостоятельного зачёта нет (П-11).
24. После зачёта: «сдан» — с пояснениями; «не сдан» — только методы с ошибками, без верных ответов (П-12).
25. Число вопросов в попытке задаёт учитель при выдаче зачёта; по умолчанию — все опубликованные; если меньше — случайная выборка из банка (П-13).
26. «Кому нужна помощь» по умолчанию: точность по заданию ниже 60 % при не менее 10 решённых задачах этого задания. Числа — в конфиге.
27. Порог зачёта по умолчанию — 80 %, в конфиге.
28. QR-код на странице класса — библиотека на выбор разработки: генерация в браузере без внешних сервисов, небольшой размер, открытая лицензия (выбор — в 2.6).
29. Тексты карточек знакомства — черновики в документе с пометкой «черновик, на утверждение Натальи». Текст согласия на ПДн не пишется: на экране место под текст с пометкой «текст юриста».
30. Опоздание по умолчанию: после срока работу можно сдать, она помечается «сдано с опозданием»; учитель может закрыть приём вручную. Настройка у работы.
31. Список классов при регистрации: 9, 10, 11, «Другое». В конфиге.
32. Переименование «Мои задания» — записано как П-14, сейчас не решается; меню в этой сессии не меняется.

Решения Натальи, принятые после этапа 3:

33. Писем учителю пока нет, сводка — только в кабинете. Возможность включить письмо-сводку учителю — в «Позже» (П-15).
34. Неактивность: учитель видит отметку «не заходил» после 7 дней без входа. Письмо ученику (А-17) — после 10 дней, не чаще одного такого письма в 14 дней. А-17 остаётся «позже». Числа — в конфиге.
35. Самостоятельная попытка зачёта — 10 вопросов (конфиг). Кнопка «Проверить себя» есть, только если опубликованных вопросов больше 10.
36. «Много ошибок в одном методе»: не менее 40 % сдавших ошиблись хотя бы в одной задаче этого метода, при не менее 3 сдавших. Числа — в конфиге.
37. Доступ по коду действует до 31 июля 2027 года (сезон ЕГЭ с пересдачами). Дата хранится у каждого кода своя, значение по умолчанию — в конфиге. Предупреждение об окончании — за 14 дней и за 3 дня.
38. Окно отправки писем — 8:00–21:00 по времени ученика. Часовой пояс определяется по браузеру при регистрации, по умолчанию — Москва. Письмо вне окна уходит в начале следующего окна.
39. Тексты писем — черновики приняты к рассмотрению, утверждаются отдельно; пометка «на утверждение Натальи» остаётся.
40. В плане разработки серверная часть кодов доступа (А-13, А-14) идёт первой, раньше кабинетов, чтобы коды заработали с октября.

Решения Натальи, принятые после этапа 4:

41. Код доступа в октябре — без аккаунта: сервер проверяет код и выдаёт подписанную метку доступа в браузер; число устройств ограничено; при регистрации доступ переносится в аккаунт. Если ученик потерял доступ (очистил браузер, сменил устройство), повторный ввод того же кода — новая активация в пределах лимита (П-16).
42. Коды выдаёт отдельный аккаунт администратора (П-17). Как Наталья входит на страницу выдачи кодов в С-2, пока нет системы аккаунтов, — два варианта в П-17, выбирает Наталья.
43. Лимит — 3 активации на код, значение в конфиге. На странице выдачи кодов — кнопка «Сбросить активации» для конкретного кода, с записью в `audit_log`.
44. VDS на Selectel Наталья арендует сама, на своё имя, и передаёт доступы. Бэкапы — в Yandex Object Storage, у другого провайдера, чем сервер. Cloudflare R2 — зарубежное хранилище, по 152-ФЗ не подходит и убирается из плана бэкапов во всех документах (список мест — Д-1 в 4.2).
45. Числа на лендинге: то, что можно посчитать по содержимому сайта (прототипы, задания, темы), считается автоматически при сборке. Всё, для чего нужны данные об учениках или использовании, убирается с лендинга до появления реальных данных. Выдуманных и «примерных» чисел не остаётся.
46. П-14 (переименование «Мои задания») и перенос генерации тренажёра на сервер (вопрос 21) — «позже».

---

## Этап 0. Аудит репозитория (без изменений в коде)

Дата аудита: 24.09.2026. Проверялась ветка `main` на коммите `4f4f4ff`.

### 0.1. Из чего состоит репозиторий

| Часть | Где лежит | Что это |
|---|---|---|
| Приложение «Будет на ЕГЭ» | `app/` | Next.js 15 (фреймворк — набор готовых инструментов для сборки сайта), React 19, TypeScript. Собирается в статические страницы (`output: 'export'`): готовые HTML-файлы без сервера. |
| Старый сайт учителя | корень репозитория: `index.html`, `classes.html`, `class.html`, `materials.html`, `about.html`, `data/*.json`, `assets/js/*` | Личный сайт «Наталия Багаева — учитель математики». Живёт отдельно, в сборку приложения не входит. |
| Старая домашняя работа по №3 | `dz_stereometria_3.html` (на сервере — `/ege/3/`) | Одна страница на 56 задач с отчётом учителю. Разобрана в `docs/HTML REVIEW DZ3.md`. |
| Документация | `docs/*.md` | Архитектура, схема базы данных, безопасность, продуктовая стратегия, бриф и дорожная карта, юридика, деплой, разведка по №8. |
| Дизайн-референс | `design-reference/` | Эталонные токены (`ui/tokens.css`), компоненты (`ui/components.css`), витрина (`styleguide.html`), макеты экранов. |
| Публикация | `.github/workflows/` | `check.yml` (проверка сборки на pull request), `deploy.yml` (выгрузка `app/out/` на Timeweb по FTP), `preview.yml` (снимки страниц 360/768/1440), `pdf-*.yml` (сборка печатных PDF). |

Важно для всех следующих этапов: **сервера у приложения нет**. Ни одного `'use server'` в коде, ни базы данных, ни авторизации. Всё, что «запоминается», лежит в браузере ученика.

### 0.2. Что уже есть и работает

**Оболочка кабинета** (`app/src/components/layout/AppShell.tsx`).
Одна на все страницы кабинета: слева сайдбар (боковое меню) со списком 13 заданий, сверху шапка с разделами, поиском, колокольчиком и именем пользователя, снизу на телефоне — `BottomNavigation` (нижняя панель навигации, 4 ячейки: Главная, Задания, Статистика, Ещё). Ниже 1024px сайдбар сужается до 72px, ниже 768px прячется. Список заданий в сайдбаре считается из `content/tasks.ts`, пункты меню — из `content/appNav.ts`.

**Замороженное меню** (`app/src/content/appNav.ts`) — так оно работает на сайте сейчас и так остаётся:

| Место | Пункты | Адреса |
|---|---|---|
| Шапка (широкий экран) | Главная · Задания · Статистика · Мои материалы · Для учителя | `/` · `/zadaniya` · `/statistika/` · `/moi-materialy/` · `/dlya-uchitelya/` |
| Колокольчик в шапке | Уведомления | `/uvedomleniya/` |
| Сайдбар, под списком заданий («Личные разделы») | Мои задания · Избранное · История решений · Настройки | `/moi-zadaniya/` · `/izbrannoe/` · `/istoriya-resheniy/` · `/nastroyki/` |
| Нижняя панель (телефон) | Главная · Банк заданий · Статистика · Ещё | «Ещё» открывает `/eshche/` со списком: Мои материалы · Учитель · Уведомления |

Пункт «Настройки» существует как заглушка и в этом документе не проектируется. Маршрут `/moi-konspekty/` есть, ссылки в меню нет.

**13 заданий** — названия строго из `app/src/content/tasks.ts`:

| № | Название в конфиге | Статус в конфиге |
|---|---|---|
| 01 | Планиметрия | soon |
| 02 | Векторы | soon |
| 03 | Стереометрия | active |
| 04 | Вероятность: простая | active |
| 05 | Вероятность: сложная | active |
| 06 | Случайные величины и распределения | soon (бейдж «структура 2027») |
| 07 | Уравнения | soon |
| 08 | Вычисления и преобразования | soon (страницы раздела собраны, карточка ещё закрыта) |
| 09 | Производная и первообразная | soon |
| 10 | Задачи с физическим смыслом | soon |
| 11 | Текстовая задача | soon |
| 12 | Графики функций | active (подтемы «Линейные» и «Квадратичная» открыты, остальные четыре — soon) |
| 13 | Финансовая грамотность и экономические задачи | soon (бейдж «структура 2027») |

**Разделы задания** — лента вкладок из конфига (`content/vkladki.ts`, `content/veroyatnost.ts`, `content/vychisleniya.ts`): О задании · Теория · Ключевые методы решения · Опорные задачи (прежнее имя — «Подготовительные задачи») · Тренажёр · Генератор, плюс кнопка-меню «Для репетиторов» в той же ленте.

**Тренажёр.** Конфигуратор: навык → режим (`Отработка` / `Смешанная` / `Повтор ошибок` / `Контроль`) → количество (5 / 10 / 20 / Все) → сложность → «Начать тренировку». Сессия собирается в браузере на свежем seed (seed — число-зерно, от которого детерминированно, то есть воспроизводимо, зависят все числа задачи). Экран задания: условие, чертёж, одно поле ответа, подсказка цепочкой шагов, разбор. Итог подхода (`TrainerResult`): верно / всего, процент, ошибок, решено с подсказкой, время, разбивка по типам заданий.

**Проверка ответа без сервера.** Общий модуль `lib/secret.ts`: в браузер уходит не ответ, а его отпечаток (fingerprint — короткая необратимая свёртка строки) с солью раздела и ключом задачи; введённое нормализуется и сверяется по отпечатку; разбор зашифрован тем же отпечатком и раскрывается по нажатию. Так устроены №3, №4, №5, №8 и опорные задачи №12 (`lib/prepSecret.ts`). Тренажёр №12 держит ответ в памяти вкладки (в состоянии React) и сравнивает функцией `sameNumber` — в разметку он тоже не попадает. Честная оговорка из кода: это защита от подглядывания в разметку и в исходник, а не от человека с отладчиком. Правило «правильные ответы никогда не попадают в DOM» уже соблюдается на всех открытых разделах.

**Прогресс в браузере** — только `localStorage` (хранилище браузера на одном устройстве). Ключи, найденные в коде:

| Ключ | Что хранит | Кто пишет |
|---|---|---|
| `budetege:trainer:v1` | счётчики тренажёра №12 по наборам (решено / верно / секунды), список идентификаторов задач с ошибкой | `lib/trainerProgress.ts` |
| `budetege:prep:v1` | номера решённых опорных задач №12 по навыкам | `lib/prepProgress.ts` |
| `budetege:theory:<подтема>` | идентификаторы дочитанных разделов теории (пока только у квадратичной) | `lib/theoryRead.ts` |
| `budetege:state` | число «изучено разделов» для линейной подтемы; стартовое значение — DEMO (6) | `state/AppState.tsx` |
| `budetege:z3:trainer:v1` | то же, что у №12, для стереометрии | `lib/zadanie3/progress.ts` |
| `budetege:veroyatnost-4:v2`, `-5:v2`, `-4-uznay:v2`, `-5-uznay:v2`, `-4:prep:v1`, `-5:prep:v1` | тренажёр, режим «Узнай метод» и опорные задачи №4 и №5, плюс исход каждой задачи (`right` / `wrong` / `revealed`) | `lib/progressStore.ts` |
| `budetege:vychisleniya-8:v1`, `-8:prep:v1` | то же для №8 | `lib/vychisleniya/progress.ts` |

Ни в одном ключе нет правильных ответов — только счётчики, секунды, идентификаторы задач и исходы. Фабрика `lib/progressStore.ts` и прослойка `lib/storage.ts` написаны так, чтобы «подмена хранилища на сервер не задела ни один компонент». Это готовая точка для переноса прогресса в базу.

**Статистика внутри тренажёров** (реальные данные из `localStorage`): `TrainerStats` (№12), `Solid3Stats` (№3), `ProgressMetody` (№4, №5), `Trenazher8Stats` (№8). Показатели: всего заданий, решено, верно, неверно, точность, среднее время, разбивка по типам, совет «Рекомендуем повторить» (тип с наименьшей точностью при ≥3 решённых), список ошибок, кнопка «Сбросить».

**«Повтор ошибок»** уже есть как режим тренажёра: берутся идентификаторы задач из списка ошибок, генерируются те же задачи на новых seed (`lib/trainerSession.ts`, `fromMistakes`). Задача уходит из списка, когда решена начисто.

**Генератор** (вкладка «Генератор», `GeneratorScreen`): вид работы (самостоятельная / домашняя / проверочная / контрольная / тренировочный вариант / своё название) + дата → навыки → количество, сложность, колонки, тема (цветная / чёрно-белая) → две ссылки: «Лист для ученика» (`pechat/?…`) и «Лист с ответами» (`pechat/otvety/?…`). Все параметры и seed — в адресе, поэтому один адрес всегда даёт один и тот же лист, а лист ученика и лист с ответами совпадают по задачам. Лист собирается в браузере и сразу отправляется в печать (сохранить как PDF). Оговорка: лист с ответами тоже собирается в браузере, и тот, у кого есть его адрес, увидит ответы — защита только в том, что с листа ученика ссылки на него нет.

**Материалы для репетиторов** — меню `TutorMenu` с карточками из конфига (у №12, №4, №5, №8): рабочая тетрадь и PDF-практикум ученических версий. PDF с ответами по правилу `lib/sheet/outputs.js` в репозиторий не попадают (`app/pdf-private/` в `.gitignore`), собираются в CI и забираются архивом.

**Дизайн-система** — `app/src/styles/tokens.css` (семантические токены `--color-*`, кегли `t-*`, шаг сетки 4px, брейкпоинты 768 / 1024 / 1280), `components.css`, витрина `/styleguide`. Готовые компоненты в `components/ui/` (единственная точка импорта — `components/ui/index.ts`): `Button`, `Input`, `Select`, `Checkbox`, `Radio`, `FieldLabel`, `FieldMessage`, `Tabs`, `Badge`, `Avatar`, `AvatarGroup`, `Tooltip`, `Skeleton`, `Breadcrumbs`, `HandNote`, `Card`, `MetricRow`, `Metric`, `TaskCard`, `HomeworkCard`, `RecommendationCard`, `ProgressBar`, `ProgressRing`, `TopicList`, `Table`, `BarChart`, `TrajectoryChart`, `Sparkline`, `Heatmap`, `EmptyState`, `ErrorState`, `SuccessState`, `Details`, `FigureZoom`, `Toast`, `Modal`, `Notification`, `GlassBadge`, `Sidebar`, `NavIcon`, `Topbar`, `BottomNavigation`. То есть карточка домашней работы, уведомление, пустое состояние, ошибка, скелет загрузки, таблица и тепловая карта уже нарисованы — данных под них нет.

**Старый сайт учителя** (корень репозитория) — рабочий прообраз «класса»:
- `classes.html` / `class.html?grade=N` — раздел класса 5–11 за кодом доступа. Код один на класс, проверяется в браузере: SHA-256 (хеш — необратимая свёртка) введённого кода сравнивается с `codeHash` из `data/classes.json`. Служебная страница `code.html` считает хеш для нового кода. **Это только прообраз интерфейса ввода кода.** Для платного доступа код проверяется исключительно на сервере: хеш в открытом файле подбирается перебором, а «открыто / закрыто» решает браузер, которому нельзя доверять (решение Натальи, п. 5 ответа на этап 0; совпадает с `PRODUCT BRIEF AND ROADMAP.md`, пакет А2).
- `data/classes/N.json` — домашние задания класса: название, тема, описание, дата выдачи, дедлайн (срок сдачи), заметка учителя; теория и рабочие файлы.
- `assets/js/deadlines.js` — статусы срока: «Сдано», «Просрочено на N дней», «Срок сегодня», «Завтра», «Осталось N дня», «До 8 сентября», «Без срока». Сортировка: сначала горящие. Эти формулировки и правило сортировки переиспользуются в кабинете.
- Отметка «Сделал» хранится в браузере (`socrat-hw-*`), учитель её не видит — так и написано на странице. «Сдать решение» — ссылка в Telegram или на почту.
- `dz_stereometria_3.html` — отчёт учителю уходил через Formspree (зарубежный сервис, в `docs/HTML REVIEW DZ3.md` помечен к удалению из-за 152-ФЗ).

**Документация**, на которую опираются следующие этапы:
- `docs/TECH ARCHITECTURE.md` — стек Next.js 15 + Supabase (готовая связка: база PostgreSQL, авторизация, файлы), роли `student` / `teacher` / `org_admin` / `admin` / `content_editor` / `parent`, RLS (Row Level Security — правила базы, кто какие строки видит).
- `docs/DATABASE SCHEMA.md` — таблицы `profiles`, `students`, `teachers`, `groups` (с `invite_code`), `group_members`, `prototypes` → `task_templates` → `generated_tasks` + секретная `answers`, `attempts`, `errors`, `assignments` + `assignment_tasks` + `deadlines`, `worksheets` + `generated_pdfs`, `statistics`, аналитические витрины.
- `docs/SECURITY.md` — три слоя защиты, матрица прав, «правило нулевой утечки» ответов, лимиты, 152-ФЗ базово.
- `docs/PRODUCT STRATEGY.md` — продакшен в России (self-hosted Supabase на VDS), MVP «репетитор + его ученики», кураторов от платформы не заводить (лицензионный риск), тарифы как гипотеза.
- `docs/PRODUCT BRIEF AND ROADMAP.md` — пакет В: В1 кабинет ученика (регистрация: почта, школа, класс; перенос прогресса из браузера), В2 кабинет учителя (список учеников, домашние задания, результаты), В3 генератор и PDF, В4 Роскомнадзор. Коды доступа — с октября 2026, кабинеты — март 2027.
- `docs/LEGAL IP RU.md` — согласие на ПДн отдельным действием (с 01.09.2025), локализация базы в РФ, до 14 лет — согласие представителя, письма только по отдельному согласию на рассылку, российский SMTP.
- `docs/REPOSITORY STRUCTURE.md` — целевая структура репозитория (`src/features/*`, `supabase/`). С текущей папкой `app/` не совпадает: сейчас соглашение — `app/src/lib/<домен>/`. На это стоит смотреть при планировании сессий разработки.

### 0.3. Что есть как заглушка

| Что | Где | Как выглядит сейчас |
|---|---|---|
| Страницы кабинета | `app/src/app/{moi-zadaniya, statistika, moi-materialy, dlya-uchitelya, uvedomleniya, nastroyki, izbrannoe, istoriya-resheniy, moi-konspekty, diagnostika}/page.tsx` | Все — один компонент `StubPage`: крошки, заголовок, `EmptyState` «Раздел готовится». Данных нет. |
| Пользователь в шапке и сайдбаре | `app/src/data/demo.ts` → `demoUser` | «Наталья Витальевна · Преподаватель», инициалы «НВ». Помечено DEMO, настоящий придёт из аккаунта. |
| Кольцо «Вы изучили N из M разделов» у линейной подтемы | `demoStudied = 6` | Витринное число; у квадратичной подтемы кольцо уже честное (по дочитанным разделам). |
| Точка «есть новые» на колокольчике | `Topbar` (`unread = true` по умолчанию, `AppShell` значение не передаёт) | Точка горит всегда. Это ложный сигнал, источника данных нет. |
| Поле поиска в шапке | `Topbar` | Поле есть, ничего не ищет. |
| «Войти», «Начать бесплатно», «Тарифы», «Оферта», «Обработка данных», «Контакты» | `app/src/content/site.ts` | Ссылки ведут на `#`. |
| «Пройти диагностику» | `content/tasks.ts` → `/diagnostika/` | Заглушка; диагностики нет. |
| Цифры и таблицы на лендинге | `app/src/content/landing.ts` | «9 980+ задач в банке», результат диагностики «76 баллов», таблица класса «11 «А»» с именами и баллами, «Что делать дальше» — витринные данные лендинга без источника. |
| Материалы подтемы | `app/src/data/materials.ts` | «Интерактивная тетрадь» — active (есть страница `tetrad/`), «PDF-практикум», «Краткая теория», «Видеоразборы» — soon. |
| Вкладки оглавления №12 «Статистика» и «Материалы» | `content/vkladki.ts` → `VKLADKI_OGLAVLENIYA` | Вкладки объявлены; содержимого под них в кабинете нет. |

Все витринные показатели из этой таблицы собраны в предложение П-5 «Убрать фейковые показатели» (раздел «Предложения»). В этой сессии не исправляются.

### 0.4. Чего нет совсем

| Чего нет | Следствие для кабинетов |
|---|---|
| Сервера и базы данных | Никакие данные не переживают смену устройства и не видны учителю. |
| Регистрации, входа, профиля, ролей | Кабинет один на всех и безымянный; «ученик» и «учитель» неразличимы. |
| Согласия на обработку ПДн, политики конфиденциальности, оферты | Регистрировать учеников нельзя, пока этого нет (152-ФЗ). |
| Кода доступа в приложении | Единственный код — на старом сайте, один на класс, проверяется в браузере (хеш в открытом JSON). Кода «к заданию №12» и серверной проверки нет. |
| Оплаты (ЮKassa) | Только план в документах. |
| Домашней работы «учитель → ученик» в приложении | Есть генератор листа для печати и старые JSON-списки на сайте учителя. Назначения, дедлайны, сдача — только в схеме базы. |
| Тренировочных работ и теоретических зачётов | Нет ни в коде, ни в схеме базы (`assignments` покрывает только набор задач с дедлайном). |
| Кабинета учителя: классов/групп, приглашений, списка учеников, результатов | Только в `DATABASE SCHEMA.md` (`groups`, `invite_code`, `assignments`) и в стратегии. |
| Уведомлений и писем | Нет ни таблицы, ни отправки почты, ни SMTP. |
| Главной страницы кабинета (Dashboard — сводный экран «что у меня сейчас») | Адрес `/` — публичный лендинг. После входа вести некуда. Адрес и содержание — этап 2. |
| Синхронизации прогресса между устройствами | Прогресс живёт в одном браузере; на другом компьютере счёт нулевой (так и написано в `prepProgress.ts`). |
| Диагностики, сертификатов, рейтингов, календаря, сообщества | Нет и в коде, и в схеме. |

### 0.5. Таблица соответствия «типовой элемент EdTech-платформы → наш аналог»

Подтверждена Натальей 24.09.2026 с правками (строки «Куратор», «Тест», две новые строки).
Вердикты: **есть аналог** / **нужен** / **не подходит нашей платформе**.

| Типовой элемент | Наш аналог | Вердикт | Объяснение в одну строку |
|---|---|---|---|
| Курс | Задание ЕГЭ №1–№13 (карточка банка, `content/tasks.ts`) | есть аналог | У нас нет линейной программы с началом и концом; единица «курса» — одно задание экзамена со своими разделами. |
| Модуль | Подтема задания: тип функции (№12), фигура (№3), метод (№4, №5), навык (№8) | есть аналог | Именно на этом уровне живут теория, опорные задачи и тренажёр. |
| Урок | Вкладки «Теория» и «Ключевые методы решения» подтемы | есть аналог | Урок у нас — прочитанный раздел теории, без видео и расписания; дочитанность уже считается у квадратичной подтемы. |
| Практика | «Опорные задачи» и «Тренажёр» с параметрическим генератором | есть аналог | Работает сейчас, прогресс — в браузере; сервер нужен только чтобы его увидел учитель. |
| Домашка | **Домашняя работа**: набор задач из генератора, выданный учителем ученику или классу с дедлайном; результат — в кабинете учителя | нужен | Есть все части по отдельности (генератор, режимы, дедлайны на старом сайте, `assignments` в схеме), но нет связки «учитель выдал → ученик сдал → учитель увидел». |
| Тест | Не отдельная сущность, а **правило проведения** «Контроль» (без подсказок, разбор после сдачи), которое включается у домашней или тренировочной работы; отдельно — **теоретический зачёт** для проверки теории | есть аналог (как правило) + см. границы в 0.6 | Режим «Контроль» уже есть в тренажёре; как «тест» он бессмыслен без учителя, поэтому становится флагом работы, а не разделом. |
| **Тренировочная работа** (новый раздел) | Работа из нескольких заданий, сдаётся целиком, как вариант на экзамене; задачи из генератора, у каждого свои числа | нужен | Переиспользуются: генератор сессии (`lib/trainerSession.ts`, `pickTasks`), экран задания и итог (`TrainerScreen`, `TrainerResult`), проверка по отпечатку (`lib/secret.ts`), лист для печати (`lib/generatorSheet.ts`). Нового: «сдать целиком», таймер, хранение результата на сервере. |
| **Теоретический зачёт** (новый раздел) | Проверка знания теории по заданию: определения, формулы, методы; вопросы пишет Наталья | нужен | Переиспользуются: привязка к заданию и методам (`content/metody.ts`, `veroyatnost-metody.ts`), проверка по отпечатку для ответов вводом, `EmptyState` «Материал готовится», карточки методов. Нового: банк вопросов на сервере, форматы вопросов, порог «сдан / не сдан», пересдача. |
| Куратор | **Учитель ученика** (роль `teacher`) | есть аналог | Отдельной роли нет, но все функции куратора — выдача работ, проверка, обратная связь, уведомления — выполняет учитель ученика; эти функции проектируются полностью (решение Натальи). Кураторов от платформы не заводим (`LEGAL IP RU.md` §2). |
| Сертификат | — | не подходит нашей платформе | Платформа не оказывает образовательную услугу и не аттестует; документ выдавать не от чего, источника данных для «сертификата» нет. |
| Сообщество | — | не подходит нашей платформе | Чат с несовершеннолетними требует модерации и хранения переписки (`LEGAL IP RU.md` §4); связь ученик–учитель идёт вне платформы. |
| Календарь | Список «Ближайшие сроки» домашних работ, тренировочных работ и зачётов (не календарная сетка) | нужен | Ученику важен ответ «что сдать и когда», а не сетка месяца; источник — дедлайны работ и зачётов. |

### 0.6. Границы четырёх сущностей: домашняя работа, «Контроль», тренировочная работа, теоретический зачёт

| | Домашняя работа | Правило «Контроль» | Тренировочная работа | Теоретический зачёт |
|---|---|---|---|---|
| Что это | Набор задач с дедлайном, выданный учителем | Не сущность, а **свойство работы**: без подсказок и без разбора до сдачи | Работа из нескольких заданий, решаемая и сдаваемая **как единое целое** (вариант) | Проверка теории: вопросы по определениям, формулам, методам |
| Кто создаёт | Учитель | Учитель включает у работы; ученик — у собственной тренировки в тренажёре | Учитель и ученик сам (работа ученика — «самостоятельная»; решено, П-3) | Учитель выдаёт; вопросы пишет Наталья |
| Из чего состоит | Задачи генератора по выбранным навыкам одного или нескольких заданий | — | Задачи генератора: на выбор или в формате первой части ЕГЭ «из N открытых заданий» (решено, П-3) | Вопросы из банка вопросов, не задачи |
| Как решается | По одной задаче, ответ проверяется сразу; можно вернуться позже | Проверка сразу, но без подсказок; разбор — после сдачи всей работы | Все задачи открыты вместе, ответы вводятся в любом порядке, работа сдаётся одной кнопкой | Вопросы по одному или списком; сдаётся одной кнопкой |
| Когда разбор | Сразу после ответа (если «Контроль» не включён) | После сдачи или после дедлайна | По настройке работы, по умолчанию сразу; у самостоятельной — всегда сразу (П-3) | После сдачи; что именно показывать — П-12 |
| Результат | Верно / всего, ошибки по типам; у учителя — таблица класса | Тот же, что у работы | Верно / всего, время, баллы первичные (если формат ЕГЭ); у учителя — таблица | «Сдан / не сдан» по порогу; у учителя — таблица |
| Что переиспользуется | Генератор, тренажёр, проверка по отпечатку, статусы сроков со старого сайта | Режим `control` тренажёра | Генератор, `pickTasks` с воспроизводимым seed, лист для печати, `TrainerResult` | Проверка по отпечатку (для ввода), карточки методов, `EmptyState` |

Вывод по границам. Две сущности пересекаются: **домашняя работа с включённым «Контролем»** и **тренировочная работа, выданная учителем**, различаются только тем, сдаются ли задачи по одной или все вместе. **Решено объединить** (П-2): одна сущность «работа» с полями *вид* (домашняя / тренировочная), *правила* (обычные / «Контроль») и *способ сдачи* (по одной задаче / целиком). Теоретический зачёт остаётся отдельной сущностью: у него другой источник содержания (вопросы, а не генератор) и другой результат (порог, а не счёт).

### 0.7. Решение по меню

Замороженное меню — живой кабинет из `appNav.ts` (таблица в 0.2). Заглушка «Настройки» не проектируется. Новые разделы «Тренировочные работы» и «Теоретические зачёты», а также «Домашние задания», «Прогресс», «Ближайшие сроки» и главная кабинета должны найти место внутри существующих пунктов; где именно — два варианта для решения Натальи в П-1. Код меню в этой сессии не меняется.

### Чеклист этапа 0

- [x] Не изменён ни один файл, кроме `docs/CABINET_UX_SPEC.md`
- [x] Меню не тронуто, «Настроек» нет
- [x] Нет показателей без источника данных (в аудите у каждого показателя указан файл-источник)
- [x] Нет выдуманного содержания (числа и названия — из конфигов и документов репозитория; вопросов зачётов нет)
- [x] Все термины объяснены при первом появлении
- [x] Для каждого элемента указано, нужен ли сервер (раздел 0.4 и колонка «Вердикт» с пояснением)

---

## Этап 1. Путь ученика и путь учителя

Принцип для обоих путей: **в любой точке пользователь понимает свой следующий шаг**. Это обеспечивается тремя правилами:
1. На каждом экране один главный CTA (главная кнопка действия), он всегда ведёт к следующему этапу пути.
2. Главная кабинета всегда показывает «Что дальше»: ближайший срок, невыполненную работу или совет из статистики. Если ничего нет — честное пустое состояние с одной кнопкой.
3. Если пользователь застрял (не сделал шаг за отведённое время), система возвращает его сама: письмом (только при согласии на рассылку) или карточкой на главной.

Обозначения в колонке «Сервер»: **да** — без серверной части этап невозможен; **нет** — работает на статическом сайте уже сейчас; **нет / да** — работает без сервера, но результат виден только в этом браузере, а для учителя и других устройств нужен сервер.

### 1А. Путь ученика (12 этапов: 10 базовых + 2 на новые разделы)

| № | Этап | Что видит пользователь | CTA (текст кнопки) | Условие перехода к следующему этапу | Автоматическое действие системы | Если застрял — как вернуть | Сервер |
|---|---|---|---|---|---|---|---|
| 1 | Приглашение или регистрация | Либо ссылку-приглашение от учителя (в ней уже зашит класс), либо страницу регистрации: почта, пароль, имя, школа (обязательно), класс (обязательно), отметка «Мне исполнилось 14 лет» (обязательно), поле «код класса» (необязательное, если пришёл без ссылки). Ссылки «Политика конфиденциальности» и «Оферта» видимы до отправки формы. | «Зарегистрироваться» | Все обязательные поля заполнены, отметка «14 лет» стоит, почта не занята, класс по коду найден (если код введён). | Создаётся аккаунт со статусом «почта не подтверждена»; при коде класса — заявка в класс учителя. Отправляется письмо с подтверждением почты (служебное письмо, согласие на рассылку не требуется). Почту отправляет почтовый сервис — российский, данные в России. | Ссылка-приглашение открывается повторно и показывает то же самое; на странице регистрации есть «Уже есть аккаунт? Войти». Нет отметки «14 лет» — кнопка неактивна, под отметкой пояснение: регистрация на платформе с 14 лет (путь через родителя — в «Позже»). | да |
| 2 | Согласие на обработку ПДн и подтверждение почты | Отдельный экран согласия (не пункт в договоре, по 152-ФЗ): два независимых чекбокса — обработка ПДн (обязательный) и рассылка напоминаний (необязательный). Затем экран «Проверьте почту». | «Принимаю и продолжаю»; в письме — «Подтвердить почту» | Согласие на ПДн дано и сохранено с датой и версией текста; ссылка из письма открыта. | Запись согласий (`profiles.consents`), статус аккаунта → «активен»; если была заявка в класс — ученик появляется у учителя в списке. | Письмо не пришло → кнопка «Отправить письмо ещё раз» (не чаще 1 раза в минуту) и подсказка проверить «Спам»; через 24 часа без подтверждения — одно повторное служебное письмо. | да |
| 3 | Первое знакомство (onboarding — короткий ввод в кабинет при первом входе) | Три экрана-карточки без лишних слов: «Задания» (13 карточек, открытые выделены), «Работы от учителя» (где появятся домашние и тренировочные работы и зачёты), «Ваш прогресс» (что считается и где смотреть). Если ученик уже в классе — имя учителя и название класса. Если в этом браузере есть реально накопленные счётчики (ключи `budetege:*` хотя бы с одной закрытой задачей или прочитанным разделом) — предложение перенести их в аккаунт. Если в браузере пусто — кнопки переноса нет. | «Далее» → «Начать»; отдельно «Перенести прогресс» (добровольно) | Нажата «Начать» (или пропуск). | Отметка «знакомство пройдено» в профиле — второй раз не показывается. Перенос — только по кнопке и только один раз, при первом входе: на сервер уходят счётчики (решено, верно, секунды), список ошибок, исходы задач, прочитанные разделы. Демонстрационное число из `budetege:state` не переносится. После переноса — отметка «перенос выполнен», кнопка больше не появляется. | Экран пропущен — карточка «Как устроен кабинет» остаётся на главной до первой решённой задачи. Перенос не предлагается повторно (решение «один раз при первом входе»). | нет / да (сам показ — без сервера; отметка и перенос — сервер) |
| 4 | Выбор задания | Банк заданий: 13 карточек по конфигу, открытые — ссылки, закрытые — «Скоро». У платного задания (№12) — бейдж «По коду доступа» и замок, если код не введён. | Карточка задания (вся карточка — ссылка) | Открыта страница задания или подтемы. | Нет. | Ученик не знает, с чего начать → на главной карточка «Что дальше» ведёт на задание из ближайшей работы учителя, а без работ — на первое открытое задание. | нет |
| 5 | Ввод кода доступа (только для платного задания) | Экран задания закрыт плашкой: «Задание открывается по коду доступа», поле ввода, подсказка, где взять код (у учителя или после оплаты), ссылка «Как получить код». | «Открыть доступ» | Сервер подтвердил код: он существует, не использован сверх лимита, срок не истёк. | Проверка **только на сервере**. При успехе до появления аккаунтов (С-3) сервер выдаёт в браузер подписанную метку доступа — это одна активация из 3; у вошедшего ученика доступ записывается за аккаунтом и открывается на всех его устройствах; метка гостя переносится в аккаунт при регистрации (П-16). Неверный код — общая ошибка «Код не подошёл», без уточнения причины; лимит попыток. | Кода нет → «Написать учителю» (контакт учителя из класса) или «Как получить код». Срок доступа истёк → плашка возвращается с текстом «Срок доступа закончился» и той же кнопкой. | да |
| 6 | Теория и ключевые методы | Вкладки «О задании», «Теория», «Ключевые методы решения» подтемы; кольцо «Вы изучили N из M разделов» (M — длина списка разделов; N — дочитанные разделы). | «К опорным задачам» (внизу теории) | Ученик открыл опорные задачи или тренажёр. Читать всё не обязательно. | Дочитанный до конца раздел отмечается прочитанным (сейчас — `lib/theoryRead.ts` в браузере; с сервером — в аккаунте). | Долго читает и не решает → на главной совет «Попробуйте опорные задачи по теме, которую читали». | нет / да |
| 7 | Опорные задачи и тренажёр | Опорные задачи по навыкам (10 задач на навык, кружки-исходы), затем тренажёр: конфигуратор и подход. Проверка сразу, подсказка цепочкой шагов, разбор по нажатию. | «Проверить», «Следующее задание», «Начать тренировку» | Закрыт хотя бы один подход тренажёра (появляется итог). | Каждая закрытая задача пишется в прогресс (`recordAttempt`): счётчики, секунды, идентификатор ошибки. С сервером — те же данные в `attempts`, учитель видит их в статистике. | Бросил подход на середине → подход запоминается (`lib/trainerRound.ts`) и предлагается продолжить; три дня без входа — письмо «Вас ждёт незакрытая тренировка» (при согласии). | нет / да |
| 8 | Работа от учителя, вид «домашняя» | Раздел «Мои задания», вкладка «Работы»: карточки `HomeworkCard` — название, вид, срок словами (по правилам `deadlines.js`), выполнено N из M. Внутри — задачи со своими числами. Способ сдачи задаёт учитель: по одной задаче (проверка сразу, если правила обычные) или целиком. При правилах «Контроль» — без подсказок и без разбора до сдачи. | «Начать» → «Проверить» → «Сдать работу» | Все задачи закрыты или нажата «Сдать работу» (частично сданную работу тоже можно сдать — учитель увидит, что не решено). | Задачи для ученика сгенерированы на сервере при выдаче (свой seed на ученика); ответы проверяются на сервере; после сдачи — уведомление учителю; статус работы «сдана» / «сдана с опозданием». | За 48 и 24 часа до срока — напоминание в кабинете и письмом (при согласии); после срока — карточка «Просрочено на N дней» остаётся сверху списка, пока учитель не закроет работу. | да |
| 9 | Работа, вид «тренировочная» (новый раздел) | Та же вкладка «Работы» с фильтром «Тренировочные»: работы от учителя и созданные самим учеником (пометка «самостоятельная»). Кнопка «Собрать работу»: «задания на выбор» или «формат ЕГЭ» с подписью «из N открытых заданий». Внутри — все задачи открыты вместе, поля ответов, счётчик «отвечено N из M», таймер (если учитель его задал; по умолчанию без), одна кнопка сдачи. Ответы можно править до сдачи. | «Начать работу» → «Сдать работу»; для своей — «Собрать работу» | Нажата «Сдать работу» или истекло время. | Задачи сгенерированы на сервере одним seed на ученика; сдача одна; при сдаче все ответы проверяются разом; считается результат (верно / всего, время). Результат и разбор — по настройке работы (по умолчанию сразу); у самостоятельной — всегда сразу. Уведомление учителю — только для работы учителя; самостоятельная попадает в статистику ученика у учителя без уведомления. | Не начал до срока → напоминания за 48 и 24 часа; начал и ушёл → черновик ответов хранится (на сервере), кнопка «Продолжить работу» на главной; при таймере — предупреждение перед началом «Время пойдёт сразу». | да |
| 10 | Теоретический зачёт (новый раздел) | Список зачётов по заданиям: «сдан» / «не сдан» / «доступен» / «пересдача через N дней». Пока вопросов нет — `EmptyState` «Материал готовится». Внутри — вопросы (форматы: выбор одного, выбор нескольких, ввод числа или короткого ответа, соответствие «метод → задача», порядок шагов), одна кнопка сдачи. | «Начать зачёт» → «Сдать зачёт» | Нажата «Сдать зачёт». | Вопросы и верные ответы живут только на сервере; порядок вопросов и вариантов перемешивается на сервере для каждой попытки; результат считается на сервере; сравнивается с порогом, который задал учитель → «сдан» / «не сдан»; уведомление учителю; при «не сдан» — дата ближайшей пересдачи: не более 3 пересдач, пауза 24 часа (числа — в конфиге). | Не сдан → карточка «Пересдача доступна с …» и ссылка на теорию и методы того задания; сдан → карточка уходит вниз списка. | да |
| 11 | Проверка и разбор ошибок, «Повтор ошибок» | После работы или подхода: итог, список ошибок по типам, кнопка «Повторить ошибки». Повтор — те же типы задач на новых числах. У домашней работы — комментарий учителя к работе, если он его оставил. | «Повторить ошибки» | Ошибки отработаны (задача уходит из списка, когда решена начисто) или ученик ушёл в другой раздел. | Режим `mistakes` тренажёра: берёт идентификаторы из списка ошибок (сейчас — `localStorage`, с сервером — из `attempts`); учитель может назначить «Повтор ошибок» как отдельную работу (путь учителя, этап 8). | Ошибки копятся и не отрабатываются → на главной карточка «N задач ждут повтора» с той же кнопкой; учителю — сигнал «много ошибок в одном методе» (этап 3). | нет / да |
| 12 | Прогресс по заданиям | Раздел «Статистика»: по каждому открытому заданию — решено, верно, точность, среднее время, слабый тип; сроки работ; сданные зачёты. Источник каждого числа — счётчики попыток (сейчас `localStorage`, с сервером — `attempts` / `statistics`). Проценты «освоения» без реальных попыток не показываются. | «Тренировать слабое место» (ведёт в тренажёр на тип с наименьшей точностью при ≥3 решённых) | Ученик вернулся к этапам 6–10. Путь циклический. | Пересчёт статистики после каждой попытки (сейчас — сразу в браузере; с сервером — триггером после записи попытки). | Нет входа N дней → письмо «Вас ждут задания» (при согласии), с прямой ссылкой на ближайшую работу или слабое место. | нет / да |

### 1Б. Путь учителя (10 этапов: 8 базовых + 2 на новые разделы)

Учитель один — Наталья. Аккаунт учителя создаётся вручную (регистрация учителей закрыта), поэтому этап «регистрация» в пути отсутствует.

| № | Этап | Что видит пользователь | CTA (текст кнопки) | Условие перехода к следующему этапу | Автоматическое действие системы | Если застрял — как вернуть | Сервер |
|---|---|---|---|---|---|---|---|
| 1 | Вход | Страница входа: почта, пароль, «Забыли пароль?». После входа — главная учителя (адрес — этап 2): классы, работы к проверке, ближайшие сроки, сигналы по ученикам. | «Войти» | Роль аккаунта — `teacher`. | Открывается кабинет учителя, а не ученика (по роли из аккаунта, не по флагу в браузере). | Забыт пароль → письмо со ссылкой сброса (служебное). Аккаунт учителя может завести только администратор — кнопки «Зарегистрироваться как учитель» нет. | да |
| 2 | Создание класса или группы | Раздел «Для учителя»: список классов. Пустое состояние: «Классов пока нет» и одна кнопка. Форма: название («11 «А»», «Группа среда 18:00»), учебный год. | «Создать класс» | Класс сохранён. | Генерируется код приглашения класса (8 знаков без похожих символов, по `SECURITY.md` §4) и ссылка-приглашение. | Не создаёт класс → главная учителя показывает единственную карточку «Начните с класса». | да |
| 3 | Приглашение учеников | Страница класса: код и ссылка-приглашение, кнопка «Скопировать», QR-код для доски, список учеников (пусто → «Учеников пока нет. Отправьте ссылку или назовите код на занятии»). Заявки без подтверждённой почты — отдельной строкой «ждёт подтверждения». | «Скопировать ссылку» / «Показать код» | В классе хотя бы один ученик со статусом «активен». | Ученик по ссылке или коду попадает в класс автоматически; учитель может удалить ученика или отключить код (ротация — новый код, старый перестаёт работать). | Ученики не приходят → карточка на главной «В классе 0 учеников» с той же ссылкой; напоминание учителю через 7 дней в кабинете (без письма — учитель и так в кабинете). | да |
| 4 | Выдача работы через генератор, вид «домашняя» | Один экран «Выдать работу» для обоих видов работы. Тот же конфигуратор, что во вкладке «Генератор»: задание → подтема → навыки → количество → сложность; плюс поля работы: вид, кому (класс или отдельные ученики), срок, правила (обычные / «Контроль»), способ сдачи (по одной задаче / целиком), «у каждого свои числа» (включено всегда), комментарий ученикам. Предпросмотр варианта с ответами — только учителю. | «Выдать работу» | Выбраны получатели и срок. | Сервер генерирует задачи для каждого ученика (seed от работы и ученика), сохраняет ответы в закрытой таблице, ставит статус «выдана», уведомляет учеников в кабинете и письмом (при согласии). Лист для печати той же работы доступен из карточки работы. | Форма брошена → черновик работы сохраняется (статус «черновик»), на главной «У вас черновик работы». | да |
| 5 | Выдача работы, вид «тренировочная» (новый раздел) | Тот же экран «Выдать работу», вид «тренировочная»: состав «задания на выбор» (несколько заданий и навыков) или «формат ЕГЭ» с подписью «из N открытых заданий» (состав первой части — из конфига; закрытые номера пропускаются, и об этом сказано явно). Время — поле «ограничение по времени», по умолчанию выключено. Сдача одна, ответы правятся до сдачи. Показ результата и разбора: сразу (по умолчанию) / после дедлайна / когда откроет учитель. Кому, срок. | «Выдать работу» | Выбраны состав, получатели и срок. | Генерация одного варианта на ученика; ответы закрыты на сервере; уведомление ученикам; лист для печати (ученик / с ответами) — из карточки работы. | Как у этапа 4 — черновик. | да |
| 6 | Загрузка вопросов и выдача теоретического зачёта (новый раздел) | Раздел зачётов: по каждому заданию — есть ли вопросы (N вопросов) или «Вопросов пока нет». Редактор вопроса: задание → метод (из карточек методов) → формат → текст → варианты / верный ответ → пояснение. Формулы — с живым предпросмотром «как увидит ученик». Вопрос сохраняется черновиком или публикуется. Пока Наталья не опубликовала ни одного вопроса — `EmptyState` «Материал готовится», выдать зачёт нельзя. Форма выдачи: класс, срок, порог (по умолчанию из конфига), пересдачи (по умолчанию 3, пауза 24 часа — из конфига). | «Сохранить черновик» / «Опубликовать» → «Выдать зачёт» | По заданию есть хотя бы один опубликованный вопрос; выбраны класс и срок. | Вопросы и верные ответы хранятся только на сервере (в браузер ученика уходят текст вопроса и варианты без пометки верного); правка опубликованного вопроса создаёт его новую версию, уже сданные попытки ссылаются на старую и не меняются; при выдаче — уведомление ученикам. | Вопросов нет → кнопка «Выдать зачёт» неактивна с подписью почему. | да |
| 7 | Просмотр результатов | Карточка работы или зачёта: таблица «ученик × статус (не начал / в процессе / сдана / с опозданием) × верно из всего × время × ошибки по типам». Клик по ученику — его вариант, его ответы (числа), верно / неверно по задаче, разбор. У зачёта — «сдан / не сдан», процент, попытка №. Поле «Комментарий ученику» к работе. Самостоятельные работы учеников в таблицы работ не попадают — они видны в карточке ученика. | «Открыть работу ученика»; «Отправить комментарий» | Учитель посмотрел результаты (путь дальше ветвится: этапы 8–10). | Таблица считается из попыток (`attempts`) на сервере при открытии; после сдачи каждым учеником учителю приходит уведомление в кабинете: «сдано», после дедлайна — «не сдано: N учеников», по итогам работы — «много ошибок в одном методе» (порог — этап 3). | Учитель не заходит → еженедельное письмо-сводка (только если Наталья включит его себе; согласие своё). | да |
| 8 | «Повтор ошибок» для ученика | В карточке ученика — его ошибки по типам за период; кнопка создаёт работу из тех же типов задач на новых числах, только этому ученику. | «Назначить повтор ошибок» | Работа выдана (это обычная домашняя работа с пометкой «повтор ошибок»). | Состав работы собирается автоматически из списка ошибок ученика (идентификаторы типов задач); срок по умолчанию — 7 дней, редактируется. | Ошибок нет → кнопка неактивна: «У ученика нет неотработанных ошибок». | да |
| 9 | Статистика класса | Раздел «Статистика» в роли учителя: по классу — тепловая карта `Heatmap` «ученик × задание» (уровень = точность из попыток; ученики без попыток — пустая ячейка, не ноль), список работ и зачётов с процентом сдавших, «кому нужна помощь» (ученики с точностью ниже порога при ≥N решённых задач; порог и N — этап 2). Никаких «прогнозов балла» без модели и данных. | «Открыть ученика» / «Назначить повтор ошибок» | Учитель перешёл к этапам 4, 5, 8 или 10. Путь циклический. | Агрегаты пересчитываются на сервере по расписанию и после каждой сдачи работы. | Данных мало → пустое состояние с объяснением «Статистика появится после первой сданной работы». | да |
| 10 | Материалы для печати | Вкладка «Генератор» и меню «Для репетиторов» у заданий — как сейчас: лист ученика, лист с ответами, PDF-практикумы. Из карточки любой выданной работы — «Скачать лист» (те же задачи, что у ученика на экране: тот же seed). | «Лист для ученика» / «Лист с ответами» | — (конечный этап, повторяется по мере надобности). | Лист собирается из параметров в адресе; для выданной работы — из сохранённого seed работы и ученика. | Печать не открылась → страница остаётся открытой, есть кнопка «Напечатать ещё раз» (сейчас `window.print()` вызывается автоматически). | нет (как сейчас) / да (лист выданной работы) |

### 1В. Где пути пересекаются

| Событие у учителя | Что видит ученик | Событие у ученика | Что видит учитель |
|---|---|---|---|
| Выдал работу / зачёт (Б4–Б6) | Карточка в «Мои задания» + уведомление (А8–А10) | Сдал работу / зачёт (А8–А10) | Строка таблицы меняет статус + уведомление (Б7) |
| Оставил комментарий к работе (Б7) | Комментарий в карточке сданной работы + уведомление (А11) | Отработал ошибки (А11) | Список ошибок ученика уменьшается (Б8) |
| Назначил повтор ошибок (Б8) | Работа с пометкой «повтор ошибок» (А8) | Не входит N дней | Отметка «не заходил N дней» в списке класса (Б9) |
| Отключил или сменил код класса (Б3) | Старая ссылка-приглашение показывает «Приглашение недействительно, спросите новую у учителя» | Ввёл код доступа к заданию (А5) | Ничего: доступ к платному заданию — отношения ученика и платформы, учитель видит только, открыто ли задание у ученика (в карточке ученика) |

### Чеклист этапа 1

- [x] Не изменён ни один файл, кроме `docs/CABINET_UX_SPEC.md`
- [x] Меню не тронуто (новые разделы размещаются внутри существующих пунктов; варианты — П-1), «Настроек» нет
- [x] Нет показателей без источника данных (каждый показатель в путях привязан к попыткам, срокам, согласиям или конфигу)
- [x] Нет выдуманного содержания (ни одного вопроса зачёта, ни одного текста задачи, ни одного числа учеников)
- [x] Все термины объяснены (onboarding, CTA, дедлайн, seed, отпечаток, роль, служебное письмо)
- [x] Для каждого этапа указано, нужен ли сервер

---

## Этап 2. Экраны кабинетов

### 2.0. Общие правила для всех экранов

- **Оболочка.** Экраны кабинета живут внутри `AppShell`: сайдбар, шапка `Topbar`, нижняя панель `BottomNavigation`. Вход, регистрация, согласие и подтверждение почты — в оболочке публичных страниц `SiteHeader` / `SiteFooter`: гость ещё не в кабинете.
- **Визуал не придумывается.** Только семантические токены `--color-*`, кегли `t-*` (`t-h1` … `t-caption`), отступы по шагу 4px (`--space-*`), радиусы `--radius-*`, высоты `--h-control`, `--h-topbar`, `--h-bnav`. Компоненты — из `app/src/components/ui/index.ts`. Новые компоненты перечислены в 2.6, изменения существующих — в 2.7.
- **Состояния.** Пустое — `EmptyState` (приглашение к действию, одна кнопка). Загрузка — `Skeleton` в форме будущего содержимого; по правилу компонента он показывается, только если ожидание дольше 200 мс. Ошибка — `ErrorState` (объясняет, что делать, не извиняется) и кнопка «Попробовать ещё раз». Итог действия — `Toast` или `SuccessState`.
- **Один главный CTA** на экране — `Button` вида `primary`. Остальные действия — `ghost` или `secondary`.
- **Ответы.** В браузер ученика не уходит ни верный ответ, ни его отпечаток для работ и зачётов: проверка идёт на сервере, сервер отвечает «верно / неверно» и, если правила разрешают, присылает разбор. В режиме «Контроль» сервер до сдачи отвечает только «ответ принят». Верные ответы видит **только роль «учитель»** — для своих работ и своих вопросов (как представление `teacher_task_answers` в `DATABASE SCHEMA.md`). Тренажёр и опорные задачи вне работ пока работают как сейчас — по отпечатку в браузере.
- **Показатели.** У каждого указан источник. «Хранится» — поле в базе. «Вычисляется» — считается из хранимого при показе или по расписанию. Имена таблиц рабочие; окончательный список сущностей и сверка с `DATABASE SCHEMA.md` — этап 4.
- **Телефон, 360px.** Сайдбар скрыт, навигация — `BottomNavigation`: Главная · Банк заданий · Статистика · Ещё (высота `--h-bnav`). Закреплённые снизу кнопки встают над ней. Таблицы шире экрана — либо в `.table-scroll` (прокрутка внутри блока, а не страницы), либо строки превращаются в карточки. Поле числового ответа — `inputmode="decimal"` (числовая клавиатура).
- **Сервер.** У каждого экрана указано. Хостинг сейчас статический, поэтому экраны с «Сервер: да» появятся только после запуска Supabase.

### 2.1. Адрес главной кабинета и куда попадает пользователь после входа

Адрес `/` занят лендингом, пункт меню «Главная» ведёт на `/` (`appNav.ts`), а меню заморожено. Варианты:

| Вариант | Как устроено | Плюсы | Минусы |
|---|---|---|---|
| **А. `/kabinet/` + перенаправление с `/`** | Главная кабинета живёт по адресу `/kabinet/`. Гость на `/` видит лендинг. Вошедший пользователь, открыв `/`, сразу перенаправляется на `/kabinet/` (решает сервер по сессии). | Код меню не меняется: «Главная» по-прежнему ведёт на `/`. У главной кабинета свой адрес, её можно открыть закладкой. | Вошедший пользователь не видит лендинг; ссылку на него придётся дать в подвале («О платформе»). |
| Б. `/` для всех, содержимое по роли | Один адрес: гость видит лендинг, вошедший — главную кабинета. | Нет второго адреса. | Одна страница с двумя совсем разными задачами; на статическом хостинге невозможна; кэшировать лендинг сложнее. |
| В. Пункт «Главная» ведёт на `/kabinet/` | Меняется адрес пункта в `appNav.ts`. | Просто и прозрачно. | Правка меню — противоречит заморозке. |

**Решено: вариант А** (П-7).

Куда попадает пользователь после входа:

| Кто | Первый вход | Обычный вход | Вход по ссылке |
|---|---|---|---|
| Ученик | `/kabinet/` и поверх — знакомство (У-4) | `/kabinet/` | Возврат туда, откуда пришёл: ссылка-приглашение → класс принят → `/kabinet/`; страница платного задания → та же страница с плашкой кода (У-5); ссылка из письма на работу → экран работы (У-6) |
| Учитель | `/kabinet/`; при 0 классов на главной одна карточка «Начните с класса» | `/kabinet/` | Возврат на страницу, с которой пришёл (например, из письма — на результаты работы, Т-3) |

Почему учитель попадает на главную, а не в «Для учителя»: главная показывает то, что требует внимания сейчас (сдачи, сроки, сигналы), а «Для учителя» — рабочее место со списками. Правило «после входа — главная» одно для обеих ролей.

### 2.2. Карта экранов

| Код | Экран | Адрес | Кто видит | Сервер |
|---|---|---|---|---|
| О-1 | Вход | `/vhod/` | оба, одинаково | да |
| О-2 | Профиль | `/profil/` | оба, разный состав | да |
| О-3 | Главная кабинета | `/kabinet/` | оба, разный вид | да |
| О-4 | Мои задания | `/moi-zadaniya/` | оба, разный вид | да |
| О-5 | Статистика | `/statistika/` | оба, разный вид | нет / да |
| О-6 | Уведомления | `/uvedomleniya/` | оба, разные события | да |
| О-7 | Мои материалы | `/moi-materialy/` | оба, разный состав | нет / да |
| О-8 | Для учителя | `/dlya-uchitelya/` | оба, совсем разный вид | да |
| О-9 | Избранное | `/izbrannoe/` | оба | нет / да |
| О-10 | История решений | `/istoriya-resheniy/` | оба, разный вид | да |
| У-1 | Регистрация (и по приглашению) | `/registraciya/`, `/priglashenie/?kod=…` | ученик | да |
| У-2 | Согласие на обработку ПДн | `/registraciya/soglasie/` | ученик | да |
| У-3 | Подтверждение почты | `/registraciya/pochta/` | ученик | да |
| У-4 | Знакомство и перенос прогресса | поверх `/kabinet/` | ученик | нет / да |
| У-5 | Плашка кода доступа | на странице платного задания | ученик | да |
| У-6 | Экран работы | `/moi-zadaniya/rabota/?id=…` | ученик | да |
| У-7 | Итог работы и разбор | `/moi-zadaniya/rabota/?id=…&itog` | ученик | да |
| У-8 | Собрать самостоятельную работу | `/moi-zadaniya/novaya/` | ученик | да |
| У-9 | Экран зачёта | `/moi-zadaniya/zachet/?id=…` | ученик | да |
| У-10 | Итог зачёта | `/moi-zadaniya/zachet/?id=…&itog` | ученик | да |
| Т-1 | Класс | `/dlya-uchitelya/klass/?id=…` | учитель | да |
| Т-2 | Выдать работу | `/dlya-uchitelya/vydat-rabotu/` | учитель | да |
| Т-3 | Результаты работы или зачёта | `/dlya-uchitelya/rabota/?id=…` | учитель | да |
| Т-4 | Работа ученика | `/dlya-uchitelya/rabota/?id=…&uchenik=…` | учитель | да |
| Т-5 | Карточка ученика | `/dlya-uchitelya/uchenik/?id=…` | учитель | да |
| Т-6 | Банк вопросов зачётов | `/dlya-uchitelya/voprosy/` | учитель | да |
| Т-7 | Редактор вопроса | `/dlya-uchitelya/voprosy/vopros/?id=…` | учитель | да |
| Т-8 | Выдать зачёт | `/dlya-uchitelya/vydat-zachet/` | учитель | да |

Адреса новые, кроме существующих заглушек меню (`/moi-zadaniya/`, `/statistika/`, `/moi-materialy/`, `/dlya-uchitelya/`, `/uvedomleniya/`, `/izbrannoe/`, `/istoriya-resheniy/`). Идентификаторы — в параметрах адреса, как у страниц печати генератора. Страница «Настройки» (`/nastroyki/`) не проектируется.

### 2.3. Экраны, которые видят оба (в разном виде)

#### О-1. Вход — `/vhod/` · оба, одинаково · сервер: да

1. **Назначение.** Войти в аккаунт по почте и паролю.
2. **Элементы.** 1) Заголовок «Вход». 2) Поле «Почта» (`Input`, `FieldLabel`). 3) Поле «Пароль» с кнопкой «показать». 4) Кнопка «Войти». 5) Ссылка «Забыли пароль?». 6) Ссылка «Регистрация для учеников» (учителей не регистрируем). 7) Сообщение об ошибке под формой (`FieldMessage`).
3. **Навигация.** Откуда: «Войти» в шапке лендинга (`site.ts`, сейчас `#`), ссылка «Уже есть аккаунт?» с регистрации, любой адрес кабинета без входа (перенаправление сюда с запоминанием адреса). Куда: по таблице 2.1. «Забыли пароль?» ведёт на экран сброса из двух состояний: ввести почту → «Проверьте почту».
4. **CTA.** «Войти». Второстепенные: «Забыли пароль?», «Регистрация для учеников».
5. **Пустое состояние.** Форма пустая; после нажатия «Войти» с пустыми полями под ними подсказки «Введите почту», «Введите пароль».
6. **Загрузка.** Кнопка меняет подпись на «Входим…» и блокируется до ответа. Скелет не нужен.
7. **Ошибка.** Неверная пара — «Почта или пароль не подходят. Проверьте раскладку или восстановите пароль» (без уточнения, что именно не так — защита от подбора). Лимит попыток (`SECURITY.md` §7) — «Слишком много попыток. Попробуйте через N минут». Нет связи — «Нет связи с сервером. Проверьте интернет и нажмите «Войти» ещё раз». Почта не подтверждена — «Подтвердите почту: письмо ушло на …» и «Отправить ещё раз».
8. **Данные.** Хранится: аккаунт в Supabase Auth (почта, хеш пароля), `profiles.role`, `profiles.status`, `profiles.onboarded_at`. Вычисляется: куда вести после входа — по роли и по тому, пусто ли `onboarded_at`; N минут — из счётчика лимита.
9. **Телефон.** Одна колонка, поля во всю ширину. Оболочки кабинета и `BottomNavigation` нет. Поле почты — `inputmode="email"`, автозаполнение пароля включено.

#### О-2. Профиль — `/profil/` · оба, разный состав · сервер: да

Не «Настройки»: здесь только личные данные, согласия, доступы и выход.

1. **Назначение.** Посмотреть и поправить свои данные, управлять согласием на письма, выйти.
2. **Элементы ученика.** 1) Имя (можно изменить). 2) Почта (только чтение). 3) Школа и класс (можно изменить, поля обязательные). 4) Мой класс и учитель; если класса нет — поле «Код класса» и «Вступить». 5) Доступы: какое задание открыто и до какой даты, кнопка «Ввести код доступа». 6) Согласия: на обработку ПДн — дата и версия текста (только чтение); на письма — переключатель с датой последнего изменения. 7) «Выйти» и «Удалить аккаунт».
   **Элементы учителя.** 1) Имя. 2) Почта. 3) «Мой контакт для учеников» — что ученики видят в своей карточке учителя (Telegram, почта или телефон — только то, что учитель сам заполнил). 4) Согласия. 5) «Выйти».
3. **Навигация.** Откуда: имя и аватар в `Topbar` (сейчас это не ссылка — изменение компонента в 2.7), «Ввести код в профиле» с главной. Куда: «Выйти» → лендинг `/`; «Удалить аккаунт» → подтверждение в `Modal` → лендинг.
4. **CTA.** «Сохранить» — появляется активной только после изменения полей. Второстепенные: «Ввести код доступа», «Вступить», «Выйти», «Удалить аккаунт».
5. **Пустое состояние.** Нет класса — «Вы пока не в классе» и поле кода. Нет доступов — «Платных заданий пока не открыто» и ссылка «Как получить код».
6. **Загрузка.** Скелеты строк высотой `--h-control`.
7. **Ошибка.** Не сохранилось — `Toast` «Не сохранилось: нет связи. Изменения на экране, нажмите «Сохранить» ещё раз». Профиль не загрузился — `ErrorState` «Не удалось загрузить профиль» и «Попробовать ещё раз».
8. **Данные.** Хранится: `profiles.display_name`, почта в `auth.users`, `students.school` (новое поле), `students.grade`, `group_members` → `groups.name`, имя учителя из `profiles`, `teachers.contact` (новое поле), `entitlements` (задание, дата окончания), `profiles.consents` (вид, версия, дата, значение). Вычисляется: «осталось N дней доступа» = дата окончания − сегодня. «Удалить аккаунт» вызывает обезличивание `anonymize_profile()` (`DATABASE SCHEMA.md` §5): данные человека удаляются, обезличенные попытки остаются в статистике класса.
9. **Телефон.** Блоки один под другим. «Сохранить» закреплена над `BottomNavigation` только когда есть изменения.

#### О-3. Главная кабинета — `/kabinet/` · оба, разный вид · сервер: да

1. **Назначение.** Ученику — ответ на вопрос «что мне сделать сейчас». Учителю — «что требует моего внимания».
2. **Элементы ученика.** 1) Приветствие по имени (`t-caption`) и заголовок с главным действием. 2) Карточка «Что дальше» (`RecommendationCard`, у каждой рекомендации обязательна причина — правило компонента). Порядок: несданная работа с ближайшим сроком → начатая, но не сданная работа → доступная пересдача зачёта → «N задач ждут повтора» → слабое место. 3) «Ближайшие сроки» — до трёх `HomeworkCard` со сроком в бейдже и ссылка «Все работы». 4) «Продолжить» — последняя открытая подтема или незакрытый подход тренажёра. 5) `MetricRow` из трёх `Metric`: решено задач за 7 дней, точность за 7 дней, сдано работ из выданных. 6) «Как устроен кабинет» — пока нет ни одной решённой задачи. 7) Мой учитель и класс одной строкой или «Вступить в класс по коду».
   **Элементы учителя.** 1) Заголовок. 2) «Требует внимания» (`RecommendationCard`): N работ сдано и ждут просмотра, N учеников не сдали к сроку, «много ошибок в одном методе» в работе (порог — этап 3). 3) «Ближайшие сроки» выданных работ и зачётов: сколько сдали из скольких. 4) Классы: название, число учеников, «Открыть». 5) Быстрые действия «Выдать работу», «Выдать зачёт». 6) Черновики работ и вопросов, если есть. 7) «Начните с класса» — при 0 классов.
3. **Навигация.** Откуда: сразу после входа, пункт «Главная» (через перенаправление с `/`, вариант А в 2.1), логотип. Куда: ученик — У-6, У-9, О-4, О-5, тренажёр задания; учитель — Т-1, Т-2, Т-3, Т-8, О-4.
4. **CTA.** Ученик — кнопка первой рекомендации; подпись по её типу: «Продолжить работу», «Начать работу», «Пересдать зачёт», «Повторить ошибки», «Тренировать». Учитель — «Выдать работу», а при 0 классов — «Создать класс». Второстепенные: «Все работы», «Статистика».
5. **Пустое состояние.** Ученик без класса и без попыток — «Начните с задания» и «Открыть банк заданий», ниже строка «Если учитель дал код класса — введите его в профиле». Ученик без работ — в блоке сроков «Работ от учителя пока нет. Когда учитель выдаст работу, она появится здесь». Учитель без классов — «Классов пока нет» и «Создать класс».
6. **Загрузка.** Скелет у каждого блока свой: медленный блок не держит остальные.
7. **Ошибка.** Незагрузившийся блок показывает `ErrorState` в своих границах с «Попробовать ещё раз». Остальные блоки работают.
8. **Данные.** Ученик. «Что дальше» вычисляется: ближайшая несданная работа — из `assignments`, `deadlines` и `assignment_submissions` (новое: сдача работы учеником); начатая — `assignment_submissions.status = in_progress`; пересдача — последняя `theory_attempts` + пауза из конфига; «ждут повтора» — число типов задач, у которых последняя попытка неверная и после неё нет чистого решения (`attempts`); слабое место — тип с наименьшей точностью при ≥3 решённых задачах (правило из `TrainerStats`) по `statistics`. Метрики: решено за 7 дней — число `attempts` за 7 дней; точность — верные / все эти попытки; сдано работ — сдачи / выданные. «Продолжить» — последняя попытка или последняя открытая подтема (`profiles.last_topic`, новое). Учитель. «Ждут просмотра» — сдачи с пустым `reviewed_at` (новое поле); «не сдали к сроку» — получатели без сдачи на момент `due_at`; «сдали из скольких» и число учеников — считаются из `assignment_submissions` и `group_members`.
9. **Телефон.** Блоки в одну колонку по приоритету: «Что дальше» → сроки → «Продолжить» → метрики. Три `Metric` в ряд на 360 не помещаются — вариант `size="tight"` столбиком. В `BottomNavigation` активна «Главная». «Все работы» видна всегда; после правки экрана «Ещё» (П-8) в «Мои задания» можно попасть и оттуда.

#### О-4. Мои задания — `/moi-zadaniya/` · оба, разный вид · сервер: да

По решению о единой сущности «работа» вкладок две: «Работы» и «Зачёты». Домашние, тренировочные и самостоятельные — фильтр внутри «Работ».

1. **Назначение.** Ученику — все выданные мне работы и зачёты и мои самостоятельные работы с понятным сроком. Учителю — всё, что я выдал.
2. **Элементы ученика.** 1) «Ближайшие сроки» — до трёх; порядок как на старом сайте (`deadlines.js`): сегодня → скоро → позже → без срока → просрочено → сдано. 2) Вкладки `Tabs`: «Работы» · «Зачёты». 3) В «Работах» — фильтр лентой: Все · Домашние · Тренировочные · Самостоятельные; переключатель «Актуальные / Все». 4) Карточка работы `HomeworkCard`: название, вид, бейдж «Контроль», срок словами в бейдже, «выполнено N из M», после сдачи — результат. 5) Карточка зачёта по каждому открытому заданию: «доступен», «сдан», «не сдан», «пересдача с …», «Материал готовится» или «Зачёт выдаёт учитель»; попытка N из 4; кнопка «Проверить себя» (самостоятельный зачёт) — только если опубликованных вопросов по заданию больше 10 (размер самостоятельной попытки, конфиг). 6) «Собрать работу» (самостоятельная). 7) Счётчик «Актуальных: N».
   **Элементы учителя.** 1) Вкладки «Работы» · «Зачёты». 2) Фильтр по классу. 3) Карточка работы: название, вид, класс, срок, «сдали N из M», «ждут просмотра: K». 4) Карточка зачёта: задание, класс, срок, сдали и не сдали. 5) Черновики. 6) «Выдать работу» / «Выдать зачёт». 7) Переключатель «Показать закрытые».
3. **Навигация.** Откуда: «Мои задания» в сайдбаре (широкий экран), «Все работы» с главной, уведомление о новой работе. Куда: ученик — У-6, У-7, У-8, У-9, У-10; учитель — Т-2, Т-3, Т-8.
4. **CTA.** Ученик — кнопка ближайшей работы в блоке сроков («Начать», «Продолжить», «Посмотреть результат»). Второстепенные: «Собрать работу», фильтры. Учитель — «Выдать работу».
5. **Пустое состояние.** Ученик, «Работы»: «Работ пока нет. Когда учитель выдаст работу, она появится здесь. А пока можно собрать тренировочную работу самому» и «Собрать работу». Ученик, «Зачёты»: строка на каждое открытое задание — «Материал готовится», если опубликованных вопросов нет, и «Зачёт выдаёт учитель», если вопросы есть, но зачёт не выдан и для самостоятельной проверки вопросов мало (не больше 10). Учитель: «Вы ещё не выдали ни одной работы» и «Выдать работу».
6. **Загрузка.** Три скелета карточки `HomeworkCard`: заголовок, строка срока, полоса.
7. **Ошибка.** `ErrorState` «Список работ не загрузился. Проверьте интернет» и «Попробовать ещё раз».
8. **Данные.** Хранится: `assignments` (название, вид, правила, способ сдачи, показ результата, признак «самостоятельная»), `deadlines` (личный срок, иначе общий), `assignment_tasks` (M — число задач ученика), `attempts` по работе, `assignment_submissions` (статус, черновик, дата сдачи); зачёты — `theory_test_assignments` (срок, порог), `theory_attempts`, `theory_questions`. Вычисляется: N — закрытые задачи (при сдаче целиком — отвеченные в черновике); статус срока — из `due_at`, `grace_minutes` и сдачи по правилам `deadlines.js`; результат — верные / все задачи; «пересдача с …» — последняя попытка + пауза из конфига; «Материал готовится» — число опубликованных вопросов по заданию равно нулю. Учитель: «сдали N из M» — сдачи / получатели; «ждут просмотра» — сдачи с пустым `reviewed_at`.
9. **Телефон.** Вкладки во всю ширину, фильтр видов — горизонтальная лента `chip`, карточки в одну колонку. «Собрать работу» — под блоком сроков, не закреплена. На телефоне экран открывается с главной, из уведомлений и с экрана «Ещё» (П-8).

#### О-5. Статистика — `/statistika/` · оба, разный вид · сервер: нет / да

1. **Назначение.** Ученику — прогресс по заданиям и слабое место. Учителю — картина класса и кому нужна помощь.
2. **Элементы ученика** (вариант А меню: одна страница, три вкладки). 1) Вкладки «По заданиям» · «Попытки» · «Сроки». 2) «По заданиям»: строка на каждое открытое задание (название из `tasks.ts`) — решено, верно, точность, среднее время, «прочитано разделов N из M», зачёт; полоса `ProgressBar` точности показывается только при ≥1 решённой задаче, иначе подпись «ещё не решали». 3) «Слабое место» и кнопка «Тренировать слабое место». 4) «Попытки»: `BarChart` решённых задач по дням за 30 дней и число верных среди них. 5) Неотработанные ошибки по типам задач и «Повторить ошибки». 6) «Сроки»: сколько работ сдано вовремя, с опозданием, не сдано. 7) Результаты работ и зачётов таблицей: название, дата, верно из всего или «сдан / не сдан».
   **Элементы учителя.** 1) Выбор класса (`Select`). 2) Тепловая карта `Heatmap` «ученик × задание». 3) «Кому нужна помощь»: ученики с точностью по заданию ниже 60 % при не менее 10 решённых задачах этого задания (числа в конфиге); отдельной строкой — не заходившие 7 дней и больше (число в конфиге). 4) Таблица `Table` по работам: работа, срок, сдали, средний процент верных. 5) Зачёты: задание, сдали / не сдали / не начинали. 6) Частые ошибки класса по типам задач. 7) Переход к ученику (Т-5).
3. **Навигация.** Откуда: «Статистика» в шапке и в `BottomNavigation`, итог работы. Куда: ученик — «Тренировать слабое место» → тренажёр задания с выбранным навыком (ярлыки `trenazher/[mode]` уже есть), «Повторить ошибки» → тренажёр в режиме «Повтор ошибок», строка результата → У-7 или У-10; учитель — Т-5, Т-3.
4. **CTA.** Ученик — «Тренировать слабое место». Учитель — «Открыть ученика» в блоке «Кому нужна помощь»; второстепенное — «Назначить повтор ошибок».
5. **Пустое состояние.** Ученик без попыток — текст, который уже есть в `trainerStats.empty` («Здесь появится ваша статистика: сколько заданий решено, с какой точностью и какой тип стоит повторить.»), и «Открыть банк заданий». Учитель: класс без сданных работ — «Статистика появится после первой сданной работы» и «Выдать работу»; без классов — «Создать класс».
6. **Загрузка.** Скелет строк таблицы; у учителя — скелет сетки тепловой карты.
7. **Ошибка.** `ErrorState` «Статистика не загрузилась» и «Попробовать ещё раз».
8. **Данные.** Ученик. Хранится: `attempts` (каждая закрытая задача: тип, верно, время, контекст), `statistics` (агрегат ученик × прототип), `theory_reads` (новое: прочитанные разделы), `imported_progress` (новое: перенесённые из браузера счётчики, У-4), `theory_attempts`, `assignment_submissions`, `deadlines`. Вычисляется: решено — число закрытых задач задания плюс перенесённые счётчики (перенесённое помечено «с этого устройства до регистрации»); точность — верно / решено × 100 с округлением, как в `summarize`; среднее время — секунды / решено; M разделов — длина списка теории подтем задания из конфига; слабое место — по правилу «≥3 решённых»; график — попытки по дням; «вовремя / с опозданием» — сравнение `submitted_at` со сроком и `grace_minutes`. До запуска сервера вкладка «По заданиям» может работать на `localStorage` (ключи из 0.2) с подписью «Данные этого устройства». Учитель. Ступень ячейки тепловой карты — точность ученика в задании из `statistics`, пять ступеней по равным интервалам 20 %; ученик без попыток — пустая ячейка (сейчас у `Heatmap` такого состояния нет — 2.7). «Кому нужна помощь» — `statistics` (точность и число решённых по заданию) и `profiles.last_seen_at`; пороги 60 %, 10 задач и 7 дней — из конфига. Частые ошибки — неверные `attempts` по типу задач, считается число разных учеников. Самостоятельные работы ученика учитель видит в Т-5.
9. **Телефон.** Вкладки прокручиваются лентой. Тепловая карта шире 360 — в `.table-scroll` с закреплённым столбцом имён. У учителя на телефоне сначала «Кому нужна помощь» карточками, потом карта. В `BottomNavigation` активна «Статистика».

#### О-6. Уведомления — `/uvedomleniya/` · оба, разные события · сервер: да

1. **Назначение.** Список событий, которые меня касаются, с переходом к делу.
2. **Элементы.** 1) Список `Notification`: заголовок, «кто · когда», точка «не прочитано». 2) Группы «Новые» и «Ранее». 3) «Отметить все прочитанными». 4) Вся строка — ссылка на объект: работа, зачёт, ученик. 5) У ученика — строка «Письма: включены / выключены» со ссылкой в профиль. 6) «Показать ещё» для старых.
   События ученика: выдана работа, выдан зачёт, срок через 48 и 24 часа, комментарий учителя, результат открыт, пересдача доступна, доступ к заданию заканчивается и закончился. События учителя: работа сдана, не сдано к сроку (сводно), много ошибок в одном методе, зачёт сдан или не сдан, ученик вступил в класс. Правила и тексты — этап 3.
3. **Навигация.** Откуда: колокольчик в шапке (точка — только при непрочитанных), «Ещё» → «Уведомления» на телефоне. Куда: объект уведомления.
4. **CTA.** Строка уведомления целиком — переход к делу. Второстепенное: «Отметить все прочитанными».
5. **Пустое состояние.** «Уведомлений пока нет. Здесь появятся новые работы, сроки и результаты». Кнопка: ученику — «К моим заданиям», учителю — «Выдать работу».
6. **Загрузка.** Четыре скелета строки.
7. **Ошибка.** `ErrorState` «Уведомления не загрузились» и «Попробовать ещё раз». Точка на колокольчике при ошибке не показывается: нет данных — нет сигнала.
8. **Данные.** Хранится: `notifications` (новое: получатель, вид события, ссылка на объект, параметры текста, `created_at`, `read_at`). Вычисляется: непрочитанные — пустое `read_at`; по ним горит точка на колокольчике (заменяет вечную точку, П-5); «когда» — относительное время от `created_at`.
9. **Телефон.** Вход через «Ещё» и колокольчик. Строки во всю ширину; «Отметить все прочитанными» — ссылкой над списком.

#### О-7. Мои материалы — `/moi-materialy/` · оба, разный состав · сервер: нет (список из конфига) / да (листы работ)

1. **Назначение.** Все файлы для скачивания и печати в одном месте.
2. **Элементы ученика.** 1) Группы по открытым заданиям, названия из `tasks.ts`. 2) Карточки материалов в виде `tutor-card` из `TutorMenu`: значок, название, подпись, «Скачать» — только ученические файлы из конфига. 3) Приглушённые карточки без файла. 4) Интерактивная тетрадь там, где она открыта (`data/materials.ts`). 5) «Листы моих работ» — печать выданной мне работы с моими числами, без ответов.
   **Элементы учителя.** То же плюс 6) «Собрать вариант» — ссылки на вкладку «Генератор» открытых подтем. 7) «Листы выданных работ» — лист ученика и лист с ответами по каждой работе. PDF с ответами из CI в интерфейс не выкладываются (правило `lib/sheet/outputs.js`).
3. **Навигация.** Откуда: «Мои материалы» в шапке, «Ещё» → «Материалы». Куда: скачивание файла, вкладка «Генератор», страница печати.
4. **CTA.** Ученик — «Скачать» у карточки. Учитель — «Собрать вариант».
5. **Пустое состояние.** У задания нет материалов — группа не показывается. Нет ни одного — «Материалы появятся по мере открытия заданий». Листов работ нет — блок не показывается.
6. **Загрузка.** Список из конфига приходит без загрузки. Блок листов работ — скелеты карточек.
7. **Ошибка.** Файл не скачался — `Toast` «Файл не скачался. Попробуйте ещё раз». Блок листов не загрузился — `ErrorState` в его границах.
8. **Данные.** Список файлов — конфиг (`tutors.items` с полем `file` в `content/sections.ts`, `content/veroyatnost.ts`, `content/vychisleniya.ts`; `data/materials.ts`), в базе ничего не хранится. Листы работ — `assignments` и `assignment_tasks` → `generated_tasks`; лист собирает сервер по сохранённому seed, ученику — без ответов, учителю — с ответами.
9. **Телефон.** Группы свёрнуты в `Details`, раскрыта первая. Карточки в одну колонку.

#### О-8. Для учителя — `/dlya-uchitelya/` · оба, совсем разный вид · сервер: да

**Ученик — карточка своего учителя** (решение Натальи).

1. **Назначение.** Знать, кто мой учитель и как с ним связаться.
2. **Элементы.** 1) `Avatar` и имя учителя. 2) Название класса. 3) Контакт, который учитель сам разрешил показывать. 4) «Написать учителю» — внешняя ссылка в Telegram или на почту, как `submitTo` на старом сайте. 5) Последний комментарий учителя к моей работе со ссылкой на неё. 6) «Вступить в класс по коду». Если классов два — по карточке на каждого учителя.
3. **Навигация.** Откуда: «Для учителя» в шапке, «Ещё» → «Учитель». Куда: внешняя ссылка, работа с комментарием (У-7).
4. **CTA.** «Написать учителю». Второстепенное: «Вступить в класс по коду».
5. **Пустое состояние.** «Вы пока не в классе. Если учитель дал код класса — введите его здесь», поле и «Вступить».
6. **Загрузка.** Скелет карточки.
7. **Ошибка.** `ErrorState` и «Попробовать ещё раз». Неверный код — «Код не подошёл. Проверьте раскладку и регистр» (текст со старого сайта).
8. **Данные.** Хранится: `group_members` → `groups` → имя учителя в `profiles`, `teachers.contact`, `assignment_submissions.teacher_comment` (новое поле). Ничего не вычисляется.
9. **Телефон.** Карточка во всю ширину.

**Учитель — рабочее место.**

1. **Назначение.** Управлять классами, работами, зачётами и учениками.
2. **Элементы.** 1) Вкладки «Классы» · «Работы» · «Зачёты» · «Ученики». 2) «Классы»: карточки (название, учеников N, «Открыть») и «Создать класс». 3) «Работы» — тот же список, что учитель видит в «Мои задания» (одна страница с двумя входами — известный минус варианта А). 4) «Зачёты»: выданные зачёты и вход в банк вопросов (Т-6) со счётчиками «опубликовано N, черновиков K» по заданиям. 5) «Ученики»: все ученики всех классов — имя, класс, последний вход, точность, «Открыть». 6) «Выдать работу», «Выдать зачёт». 7) Поиск ученика по имени.
3. **Навигация.** Откуда: «Для учителя» в шапке, главная. Куда: Т-1, Т-2, Т-3, Т-5, Т-6, Т-8.
4. **CTA.** «Выдать работу»; при 0 классов — «Создать класс».
5. **Пустое состояние.** «Классов пока нет» и «Создать класс». «Учеников пока нет. Откройте класс и отправьте ссылку-приглашение». «Вопросов пока нет» и «Добавить вопрос».
6. **Загрузка.** Скелеты карточек и строк во вкладке.
7. **Ошибка.** `ErrorState` в пределах вкладки.
8. **Данные.** Хранится: `groups`, `group_members`, `profiles.last_seen_at`, `theory_questions` (статус). Вычисляется: число учеников, точность из `statistics`, счётчики опубликованных вопросов и черновиков.
9. **Телефон.** Вкладки лентой; таблица учеников превращается в карточки (имя, класс, последний вход). «Выдать работу» — под вкладками.

#### О-9. Избранное — `/izbrannoe/` · оба · сервер: нет / да · приоритет — позже (П-9), заглушка остаётся

1. **Назначение.** Быстро вернуться к отложенному: подтеме, разделу теории, типу задач.
2. **Элементы.** 1) Список отложенного, сгруппированный по заданиям. 2) Вид элемента: подтема, раздел теории, тип задач. 3) «Открыть». 4) «Убрать из избранного». 5) У типа задач — «Тренировать»: сохраняется тип, а не задача с числами, потому что числа у ученика всегда свежие. 6) У учителя у типа задач — «Выдать работу по этому типу».
3. **Навигация.** Откуда: «Избранное» в сайдбаре, значок-звезда на подтеме, разделе теории и в итоге тренажёра (новый элемент, 2.6). Куда: объект.
4. **CTA.** «Открыть».
5. **Пустое состояние.** «Здесь будут подтемы, разделы теории и типы задач, которые вы отметили звёздочкой» и «Открыть банк заданий».
6. **Загрузка.** Скелеты строк.
7. **Ошибка.** `ErrorState` и «Попробовать ещё раз».
8. **Данные.** Хранится: `favorites` (новое: пользователь, вид, идентификатор из конфига или прототипа, дата). До сервера возможен `localStorage` (например, `budetege:favorites:v1`) — только на этом устройстве.
9. **Телефон.** Вход через экран «Ещё» (П-8). Строки — карточки.

#### О-10. История решений — `/istoriya-resheniy/` · оба, разный вид · сервер: да · приоритет — позже (П-9), заглушка остаётся

1. **Назначение.** Ученику — хронология моих решений. Учителю — лента сдач учеников.
2. **Элементы ученика.** 1) Лента по дням. 2) Строка: время, задание и тип задачи, где решал (тренажёр, опорные задачи, работа, зачёт), итог (верно, неверно, с подсказкой, открыт разбор), время на задачу. 3) Фильтры: задание, где решал, только ошибки. 4) «Решить похожую» — тот же тип на новых числах. 5) «Показать ещё». Верный ответ в истории не показывается; разбор — только через экран работы по её правилам.
   **Элементы учителя.** 1) Лента сдач: ученик, работа или зачёт, когда, результат. 2) Фильтр по классу. 3) Переход к Т-4.
3. **Навигация.** Откуда: «История решений» в сайдбаре. Куда: У-7, тренажёр; у учителя — Т-4.
4. **CTA.** Ученик — «Решить похожую» у строки с ошибкой. Учитель — открыть сдачу.
5. **Пустое состояние.** «История появится после первой решённой задачи» и «Открыть банк заданий».
6. **Загрузка.** Скелет ленты.
7. **Ошибка.** `ErrorState` и «Попробовать ещё раз».
8. **Данные.** Хранится: `attempts` (время, прототип, работа, `is_correct`, `hint_used`, `solution_viewed`, `time_spent_ms`), `theory_attempts`, `assignment_submissions`. Вычисляется только группировка по дням. Без сервера экран невозможен: в `localStorage` лежат счётчики без дат каждой попытки.
9. **Телефон.** Вход через экран «Ещё» (П-8). Строки — карточки в две строки.

### 2.4. Экраны ученика

#### У-1. Регистрация — `/registraciya/`, `/priglashenie/?kod=…` · сервер: да

1. **Назначение.** Создать аккаунт ученика и, если есть код, сразу попасть в класс.
2. **Элементы.** 1) Заголовок; по приглашению — строка «Вас пригласил учитель {имя} в класс «{название}»». 2) Почта. 3) Пароль с правилом длины под полем. 4) Имя. 5) Школа и класс — обязательные; класс — `Select` со списком из конфига: 9, 10, 11, «Другое». 6) Код класса (при приглашении скрыт — он уже известен) и отметка «Мне исполнилось 14 лет» (обязательная). 7) «Зарегистрироваться», ссылки на политику конфиденциальности и оферту, «Уже есть аккаунт? Войти».
3. **Навигация.** Откуда: «Начать бесплатно» в шапке лендинга, ссылка-приглашение, О-1. Куда: У-2.
4. **CTA.** «Зарегистрироваться». Второстепенное: «Войти».
5. **Пустое состояние.** После отправки пустой формы — подсказки под обязательными полями. Приглашение недействительно — «Приглашение недействительно. Спросите у учителя новую ссылку или код», регистрация без класса остаётся доступной. Нет отметки «14 лет» — кнопка неактивна, под отметкой «Регистрация на платформе — с 14 лет».
6. **Загрузка.** По приглашению — скелет строки «Вас пригласил…», пока сервер проверяет код. При отправке — «Создаём аккаунт…».
7. **Ошибка.** Почта занята — «Эта почта уже зарегистрирована» и ссылки «Войти», «Восстановить пароль». Код класса не найден — «Код не подошёл. Проверьте раскладку и регистр», регистрация без класса возможна. Нет связи — общий текст и «Попробовать ещё раз».
8. **Данные.** Хранится: `auth.users` (почта, пароль), `profiles` (имя, роль `student`), `students.school`, `students.grade`, `students.age_confirmed_at` (новое: когда отмечено «14 лет»), `profiles.timezone` (новое: часовой пояс из браузера при регистрации, по умолчанию Москва), `group_members` (вступление по коду). Вычисляется: имя учителя и класс по коду (`groups.invite_code` → `groups.name` → учитель).
9. **Телефон.** Одна колонка, нативный `Select` класса, ссылки на документы открываются в новой вкладке. Оболочка публичных страниц, без `BottomNavigation`.

#### У-2. Согласие на обработку ПДн — `/registraciya/soglasie/` · сервер: да

1. **Назначение.** Получить отдельное согласие на обработку персональных данных и, по желанию, на письма.
2. **Элементы.** 1) Заголовок. 2) Блок под текст согласия с пометкой **«текст юриста»** — какие данные собираются и зачем. В документе текст не пишется. 3) Чекбокс согласия на обработку ПДн (обязательный) и ссылка на полный текст. 4) Чекбокс «Присылать напоминания о сроках на почту» (необязательный). 5) «Принимаю и продолжаю». 6) Строка «Согласие на письма можно отозвать в профиле».
3. **Навигация.** Откуда: У-1; при входе — если текст согласия обновился (новая версия). Куда: У-3.
4. **CTA.** «Принимаю и продолжаю» — неактивна без согласия на ПДн.
5. **Пустое состояние.** Не применимо.
6. **Загрузка.** Кнопка «Сохраняем…».
7. **Ошибка.** `FieldMessage` «Не сохранилось. Нажмите ещё раз». Без сохранённого согласия в кабинет не пускает.
8. **Данные.** Хранится: `profiles.consents` — вид (ПДн, письма), версия текста, дата, значение. Версии текстов — в конфиге документов.
9. **Телефон.** Текст и чекбоксы в колонку, кнопка закреплена внизу.

#### У-3. Подтверждение почты — `/registraciya/pochta/` · сервер: да

1. **Назначение.** Подсказать следующий шаг — открыть письмо.
2. **Элементы.** 1) «Проверьте почту». 2) Адрес, на который ушло письмо. 3) «Отправить письмо ещё раз» — не чаще раза в минуту, с обратным отсчётом на кнопке. 4) «Проверьте папку «Спам»». 5) «Указали не ту почту? Изменить». 6) После перехода по ссылке — `SuccessState` «Почта подтверждена» и «Войти в кабинет».
3. **Навигация.** Откуда: У-2, О-1 при неподтверждённой почте. Куда: `/kabinet/` и знакомство (У-4).
4. **CTA.** До подтверждения — «Отправить письмо ещё раз»; после — «Войти в кабинет».
5. **Пустое состояние.** Не применимо.
6. **Загрузка.** По ссылке из письма — «Проверяем ссылку…».
7. **Ошибка.** Ссылка устарела — «Ссылка устарела. Отправим новую» и кнопка.
8. **Данные.** Хранится: `auth.users.email_confirmed_at`, время последней отправки письма. Вычисляется: сколько секунд до повторной отправки.
9. **Телефон.** Одна колонка.

#### У-4. Знакомство и перенос прогресса — поверх `/kabinet/` при первом входе · сервер: нет (показ) / да (отметка и перенос)

1. **Назначение.** За минуту показать, где что лежит, и перенести накопленный в браузере прогресс, если он есть.
2. **Элементы.** 1) Три шага-карточки в `Modal`: «Задания», «Работы от учителя», «Ваш прогресс» — черновики текстов ниже. 2) Номер шага. 3) Класс и учитель, если ученик уже в классе. 4) Шаг переноса — только если в браузере есть накопленное: «На этом устройстве найдено: закрыто N задач в заданиях …» и «Перенести прогресс». 5) «Далее» / «Начать». 6) «Пропустить».
3. **Навигация.** Откуда: первый вход. Куда: главная.
4. **CTA.** «Начать» на последнем шаге. «Перенести прогресс» — отдельная кнопка на своём шаге, добровольная.
5. **Пустое состояние.** В браузере пусто — шага переноса нет совсем.
6. **Загрузка.** Перенос — «Переносим…», кнопка заблокирована.
7. **Ошибка.** «Не получилось перенести. Прогресс на этом устройстве не пропал — попробуйте ещё раз» и «Попробовать ещё раз». Неудачная попытка не считается тем «одним разом».
8. **Данные.** Вычисляется в браузере: N — сумма `kinds[*].done` по ключам из 0.2; список заданий — по имени ключа; `budetege:state` (демонстрационное число) не учитывается. Хранится после переноса: `imported_progress` (новое: ученик, ключ, счётчики, список ошибок, исходы, дата переноса), `theory_reads` (прочитанные разделы), `profiles.progress_imported_at` (новое: перенос выполнен — кнопка больше не появляется). Счётчики переносятся как счётчики, а не как попытки: дат и чисел задач в браузере нет, выдумывать их нельзя.
9. **Телефон.** `Modal` на весь экран, кнопки внизу.

**Тексты карточек знакомства — черновик, на утверждение Натальи.**

| Шаг | Заголовок | Текст | Кнопки |
|---|---|---|---|
| 1 | Задания | Здесь все задания профильного ЕГЭ по номерам. В открытом задании — теория, ключевые методы, опорные задачи и тренажёр. Числа в задачах у каждого свои, поэтому решать нужно самому. | «Далее» |
| 2 | Работы от учителя | Когда учитель выдаст работу или зачёт, они появятся в разделе «Мои задания» и на главной. Там же видно, до какого числа сдать. | «Далее» |
| 3 | Ваш прогресс | В «Статистике» видно, сколько задач решено и с какой точностью. Там же подсказка, что стоит потренировать. | «Начать» |
| Перенос (только если в браузере есть накопленное) | Ваши решения на этом устройстве | На этом устройстве уже есть ваши решения: {задания}. Перенести их в аккаунт, чтобы видеть прогресс на любом устройстве? | «Перенести прогресс» · «Не переносить» |

#### У-5. Плашка кода доступа — на странице платного задания · сервер: да

1. **Назначение.** Открыть платное задание по коду.
2. **Элементы.** 1) Плашка поверх закрытых вкладок платного задания; без кода открыта только вкладка «О задании» (П-10). 2) Заголовок «Задание открывается по коду доступа». 3) Поле кода (`Input`, без автоисправления). 4) «Открыть доступ». 5) «Где взять код»: у учителя или после оплаты. 6) «Написать учителю», если ученик в классе. 7) До появления аккаунтов (С-3) код вводится без входа: сервер выдаёт метку доступа в браузер (одна активация из 3). После С-3 у вошедшего ученика доступ записывается за аккаунтом; метка гостя переносится в аккаунт при регистрации и новой активации не тратит (П-16).
3. **Навигация.** Откуда: карточка платного задания в банке, сайдбар, прямая ссылка. Куда: после успеха плашка исчезает, открыта вкладка, куда шёл ученик.
4. **CTA.** «Открыть доступ».
5. **Пустое состояние.** Срок доступа истёк — та же плашка с заголовком «Срок доступа закончился» и датой окончания. За 14 дней и за 3 дня до окончания над вкладками задания — строка «Доступ действует до {дата}».
6. **Загрузка.** Кнопка «Проверяем код…».
7. **Ошибка.** «Код не подошёл» — один текст для неверного, исчерпанного и просроченного кода, чтобы не подсказывать перебор; под ним строка «Если код ваш и перестал работать — напишите учителю»: администратор может сбросить активации кода. Лимит попыток — «Слишком много попыток. Попробуйте через N минут». Нет связи — общий текст.
8. **Данные.** Хранится: `access_codes` (новое: хеш кода, задание, лимит активаций, число активаций, дата окончания — у каждого кода своя, по умолчанию 31.07.2027 из конфига), `code_activations` (метка устройства или аккаунт, дата, отозвана ли), `entitlements` (аккаунт, задание, до какой даты, код-источник — с С-3). Проверка и запись — **только на сервере**; в браузер хеш кода не уходит, в отличие от старого сайта. Вычисляется: открыто ли задание — есть ли действующая активация или запись в `entitlements`, не отключён ли код и не прошла ли дата.
9. **Телефон.** Плашка во всю ширину над лентой вкладок; поле с `autocapitalize="off"`.

#### У-6. Экран работы — `/moi-zadaniya/rabota/?id=…` · сервер: да

1. **Назначение.** Решить работу по её правилам и сдать.
2. **Элементы.** 1) Шапка: название, вид, бейдж «Контроль», срок в бейдже, таймер, если учитель задал время (`CountdownTimer`, новое). 2) Комментарий учителя ученикам. 3) Способ «по одной задаче» — экран как `TrainerScreen`: условие, чертёж, поле ответа, «Проверить», кружки задач. Способ «целиком» — все задачи списком (`WorkSheetScreen`, новое), ответы сохраняются черновиком по мере ввода и правятся до сдачи. 4) Счётчик «отвечено N из M». 5) Подсказки и разбор — только при обычных правилах и сдаче по одной задаче. 6) «Сдать работу» с подтверждением в `Modal`: «Не отвечено K задач. Сдать?». 7) «Лист для печати» — мои числа, без ответов.
3. **Навигация.** Откуда: О-4, главная, уведомление. Куда: после сдачи — У-7; «Назад» — О-4, черновик сохранён.
4. **CTA.** «Сдать работу». При сдаче по одной задаче у каждой задачи — «Проверить», а «Сдать работу» становится главной после последней задачи.
5. **Пустое состояние.** Работа отозвана или не найдена — «Эта работа больше недоступна» и «К моим заданиям». Работа с таймером ещё не начата — экран «Время пойдёт сразу после нажатия «Начать». Задач: M, время: T минут» и «Начать».
6. **Загрузка.** Скелет условия и поля; у чертежа — скелет его размера.
7. **Ошибка.** Ответ не отправился — введённое остаётся в поле, под ним «Не отправилось. Проверьте интернет» и «Отправить ещё раз». Ответы ученика (не верные ответы) до подтверждения сервером хранятся и в браузере. Сдача после срока принимается: работа помечается «сдано с опозданием», пока учитель не закрыл приём (настройка у работы, по умолчанию приём открыт). После закрытия приёма — «Приём работы закрыт. Напишите учителю» и «Написать учителю». Время вышло — работа сдаётся автоматически с тем, что введено.
8. **Данные.** Хранится: `assignments`, `assignment_tasks` → `generated_tasks` (условие и чертёж через `generated_tasks_public` — без seed, параметров и ответа), `attempts` (каждый проверенный ответ), `assignment_submissions` (начало, черновик ответов, статус, сдача), `deadlines`. Вычисляется: «отвечено N из M»; остаток времени = начало + лимит − текущее время сервера; статус срока. Верные ответы в браузер не приходят. На «Проверить» сервер отвечает «верно / неверно», в режиме «Контроль» — «ответ принят».
9. **Телефон.** При сдаче целиком задачи идут одна под другой, поле ответа под каждой, `inputmode="decimal"`. Таймер и «отвечено N из M» закреплены сверху, «Сдать работу» — снизу над `BottomNavigation`. Чертёж открывается на весь экран через `FigureZoom`.

#### У-7. Итог работы и разбор — `/moi-zadaniya/rabota/?id=…&itog` · сервер: да

1. **Назначение.** Понять результат и что делать с ошибками.
2. **Элементы.** 1) Итог в виде `TrainerResult`: верно из всего, процент, ошибок, решено с подсказкой, время, разбивка по типам. 2) Сдано вовремя или с опозданием. 3) Список задач: моё число, верно или неверно. 4) Разбор задачи по нажатию (`PrepSolution`), если правила показа разрешают; иначе плашка «Разбор откроется {дата срока}» или «Разбор откроет учитель». 5) Комментарий учителя. 6) «Повторить ошибки». 7) «К моим заданиям».
3. **Навигация.** Откуда: сдача в У-6, О-4, уведомление «результат открыт». Куда: тренажёр в режиме «Повтор ошибок» по типам задач этой работы, О-4.
4. **CTA.** «Повторить ошибки»; если ошибок нет — «К моим заданиям».
5. **Пустое состояние.** Результат скрыт правилом — «Работа сдана. Результат откроется {дата}» или «Работа сдана. Результат откроет учитель» и «К моим заданиям». Ошибок нет — `SuccessState` «Все задачи решены верно».
6. **Загрузка.** Скелет блока итога.
7. **Ошибка.** «Работа сдана, не загрузился только итог» и «Попробовать ещё раз».
8. **Данные.** Хранится: `attempts` по работе, `assignment_submissions` (сдача, `teacher_comment`, `result_released_at` — новое, для показа «когда откроет учитель»), разбор `answers.solution_md` — сервер отдаёт его только когда показ разрешён. Вычисляется: всё, что в итоге (верные, процент, время = сдача − начало, разбивка по типам). У самостоятельной работы результат открыт всегда сразу.
9. **Телефон.** Как `TrainerResult` сейчас — он уже свёрстан под телефон. Список задач — карточки, разбор раскрывается в `Details`.

#### У-8. Собрать самостоятельную работу — `/moi-zadaniya/novaya/` · сервер: да

1. **Назначение.** Ученик сам собирает тренировочную работу.
2. **Элементы** (шаги конфигуратора, `StepHead`). 1) Состав: «Задания на выбор» или «Формат ЕГЭ — из N открытых заданий». 2) Для «на выбор»: задания → навыки (`SkillCards`, как в тренажёре). 3) Количество задач (`Option`; значения — из `skillCounts`, как в тренажёре). 4) Время: «Без ограничения» или число минут. 5) Сводка «Выбранная работа», как в конфигураторе. 6) «Начать работу».
3. **Навигация.** Откуда: «Собрать работу» в О-4, главная. Куда: сразу У-6 — работа создаётся и начинается.
4. **CTA.** «Начать работу».
5. **Пустое состояние.** Если платное задание без кода — оно в списке неактивно с подписью «По коду доступа». Закрытые номера в «формате ЕГЭ» пропускаются, подпись «из N открытых заданий» говорит об этом прямо.
6. **Загрузка.** Навыки — из конфига, без загрузки. «Начать работу» → «Собираем работу…».
7. **Ошибка.** Сервер собрал меньше задач, чем выбрано (как `shortage` в `lib/trainerSession.ts`), — «Собрали K задач из N: задач выбранной сложности меньше» и «Начать с K» / «Изменить». Нет связи — общий текст.
8. **Данные.** Вычисляется: N открытых заданий — задания со статусом `active` в `tasks.ts`, входящие в состав первой части из нового конфига (сверяется с демоверсией ФИПИ), с учётом доступа ученика (`entitlements` для платных). Навыки и счётчики — конфиг и манифест генератора (`content/skills12.ts`, `lib/generator/manifest.ts` и аналоги разделов). Хранится при создании: `assignments` (вид «тренировочная», признак «самостоятельная», автор — ученик), `assignment_tasks`, `generated_tasks`, `answers`.
9. **Телефон.** Шаги в колонку, как `cfg` сейчас; сводка под шагами; «Начать работу» закреплена над `BottomNavigation`.

#### У-9. Экран зачёта — `/moi-zadaniya/zachet/?id=…` · сервер: да

В документе нет ни одного вопроса: только форматы и устройство экрана.

1. **Назначение.** Сдать теоретический зачёт по заданию.
2. **Элементы.** 1) Экран перед началом: задание (название из `tasks.ts`), число вопросов, порог «нужно K % верных», попытка N из 4 (1 + 3 пересдачи из конфига), срок, «Начать зачёт». 2) Вопрос в своём формате: выбор одного (`Radio`), выбор нескольких (`Checkbox`), ввод числа или короткого ответа (`Input`), соответствие (`QuestionMatch`, новое), порядок шагов (`QuestionOrder`, новое). Формулы набираются KaTeX. 3) Номера вопросов с отметкой «отвечен / нет». 4) Счётчик «отвечено N из M». 5) «Сдать зачёт» с подтверждением. 6) Ответы сохраняются черновиком до сдачи. 7) Самостоятельная попытка («Проверить себя»): случайная выборка вопросов, пометка «самостоятельный», пересдачи выданного зачёта не расходует, видна учителю в карточке ученика; кнопка есть, только если опубликованных вопросов больше 10.
3. **Навигация.** Откуда: вкладка «Зачёты» в О-4, главная, уведомление. Куда: У-10.
4. **CTA.** «Начать зачёт», затем «Сдать зачёт».
5. **Пустое состояние.** По заданию нет опубликованных вопросов — `EmptyState` «Материал готовится» и кнопка «К теории задания». Пересдача ещё недоступна — «Пересдача будет доступна {дата и время}» и «Повторить теорию». Попытки закончились — «Попытки закончились. Обратитесь к учителю» и «Написать учителю».
6. **Загрузка.** Скелет вопроса.
7. **Ошибка.** Сдача не прошла — «Не отправилось. Ваши ответы сохранены — нажмите «Сдать зачёт» ещё раз».
8. **Данные.** Хранится: `theory_test_assignments` (зачёт, кому, срок, порог), `theory_questions` и `theory_question_versions` (текст, формат, варианты — сервер отдаёт их без пометки верного), `theory_answer_keys` (верные ответы — закрытая таблица, как `answers`), `theory_attempts` (попытка, версии вопросов, порядок перемешивания, ответы, результат). Вычисляется: номер попытки, остаток пересдач, «доступна с» = последняя попытка + 24 часа (из конфига). При начале каждой попытки сервер выбирает вопросы (все опубликованные или случайная выборка заданного размера) и перемешивает порядок вопросов и вариантов. Самостоятельная попытка хранится в `theory_attempts` с признаком «самостоятельный» и в счёт пересдач не идёт; её размер — 10 вопросов (конфиг).
9. **Телефон.** Один вопрос на экран, номера вопросов — лентой сверху, «Сдать зачёт» закреплена внизу. «Соответствие» на телефоне — `Select` у каждого пункта вместо перетаскивания.

#### У-10. Итог зачёта — `/moi-zadaniya/zachet/?id=…&itog` · сервер: да

1. **Назначение.** Узнать «сдан / не сдан» и что делать дальше.
2. **Элементы.** 1) «Зачёт сдан» (`SuccessState`) или «Зачёт не сдан» (нейтральное состояние, без красного — правило `ProgressBar` про наказание цветом). 2) Процент верных и порог. 3) При «сдан» — пояснения ко всем вопросам; при «не сдан» — только методы с ошибками, без верных ответов (П-12). 4) Методы, в которых были ошибки, со ссылками на «Ключевые методы» и «Теорию» задания. 5) При «не сдан» — «Пересдача доступна {дата}» и «осталось пересдач: R». 6) «К моим заданиям».
3. **Навигация.** Откуда: У-9, О-4, О-5. Куда: вкладки методов и теории задания, О-4.
4. **CTA.** «Не сдан» — «Повторить теорию» (к методам с ошибками). «Сдан» — «К моим заданиям».
5. **Пустое состояние.** Не применимо.
6. **Загрузка.** Скелет блока итога.
7. **Ошибка.** «Результат не загрузился. Зачёт сдан, ответы сохранены» и «Попробовать ещё раз».
8. **Данные.** Хранится: `theory_attempts` (процент, сдан), порог из `theory_test_assignments`, метод вопроса — `theory_questions.method_id` → карточки методов из конфига (`content/metody.ts`, `content/veroyatnost-metody.ts`, `content/quadraticMetody.ts`). Вычисляется: сдан — процент ≥ порога; дата пересдачи; остаток пересдач.
9. **Телефон.** Одна колонка.

### 2.5. Экраны учителя

#### Т-1. Класс — `/dlya-uchitelya/klass/?id=…` · сервер: да

1. **Назначение.** Пригласить учеников и видеть состав класса.
2. **Элементы.** 1) Название класса (можно изменить) и учебный год. 2) Приглашение: ссылка, код, «Скопировать ссылку», «Показать код» (крупно, для доски), QR-код (`InviteCard`, новое). 3) «Сменить код» (старый перестаёт работать) и переключатель «Приглашение включено». 4) Ученики: имя, статус (активен / ждёт подтверждения почты), последний вход, точность, «Открыть». 5) Активные работы класса со счётчиком «сдали N из M». 6) «Выдать работу классу». 7) «Убрать ученика из класса» в строке и «Архивировать класс» — с подтверждением.
3. **Навигация.** Откуда: «Классы» в О-8, главная. Куда: Т-5, Т-2 (класс уже выбран), Т-3.
4. **CTA.** При 0 учеников — «Скопировать ссылку». Иначе — «Выдать работу классу».
5. **Пустое состояние.** «Учеников пока нет. Отправьте ссылку или назовите код на занятии».
6. **Загрузка.** Скелеты строк.
7. **Ошибка.** Смена кода не прошла — `Toast` «Код не сменился. Попробуйте ещё раз». Список не загрузился — `ErrorState`.
8. **Данные.** Хранится: `groups` (название, `invite_code`, `invite_enabled`, `archived_at`), `group_members` (`joined_at`, `left_at`), `auth.users.email_confirmed_at`, `profiles.last_seen_at`. Вычисляется: статус «ждёт подтверждения почты», точность из `statistics`, «сдали N из M» из `assignment_submissions`.
9. **Телефон.** Блок приглашения первым. QR и код — во весь экран по нажатию (`Modal`). Ученики — карточки.

#### Т-2. Выдать работу — `/dlya-uchitelya/vydat-rabotu/` · сервер: да

Один экран для обоих видов работы (решение о единой сущности).

1. **Назначение.** Собрать работу генератором и выдать классу или ученикам.
2. **Элементы** (шаги как в `GeneratorScreen`, `StepHead`). 1) Вид: домашняя или тренировочная; название — из `workKinds` или своё. 2) Состав: «задания на выбор» (задание → подтема → навыки `SkillCards`, количество, сложность) или «формат ЕГЭ — из N открытых заданий» (только у тренировочной). 3) Правила: обычные или «Контроль». Способ сдачи: по одной задаче или целиком; у тренировочной — всегда целиком, одна сдача. 4) Время: без ограничения (по умолчанию) или N минут. Показ результата и разбора: сразу (по умолчанию), после срока, когда откроет учитель. 5) Кому: класс (`Select`) и/или отдельные ученики; срок (дата и время); приём после срока — открыт (по умолчанию, работа помечается «сдано с опозданием») или закрыт; комментарий ученикам. 6) Предпросмотр варианта одного ученика с ответами (видит только учитель) и «Лист для печати». 7) «Выдать работу» и «Сохранить черновик».
3. **Навигация.** Откуда: главная, О-4, О-8, Т-1 (класс выбран), Т-5 (повтор ошибок — состав заполнен ошибками ученика), вкладка «Генератор» задания (кнопка «Выдать как работу», 2.7). Куда: Т-3 после выдачи.
4. **CTA.** «Выдать работу». Второстепенные: «Сохранить черновик», «Предпросмотр».
5. **Пустое состояние.** Нет классов — «Сначала создайте класс» и «Создать класс».
6. **Загрузка.** Навыки — из конфига, без загрузки. «Выдать работу» → «Выдаём работу: собираем задачи для N учеников…».
7. **Ошибка.** Для части учеников собрано меньше задач, чем выбрано, — «Для K учеников собрано меньше задач: для выбранной сложности их не хватает» и «Выдать как есть» / «Изменить». Срок в прошлом — `FieldMessage` «Срок уже прошёл». Нет связи — черновик сохранён, «Попробовать ещё раз».
8. **Данные.** Навыки, уровни и счётчики — конфиг и манифест генератора. N открытых заданий — как в У-8. Хранится при выдаче: `assignments` (вид, правила, способ сдачи, лимит времени, показ результата, комментарий, статус «черновик / выдана»), `deadlines`, для каждого ученика `assignment_tasks` + `generated_tasks` + `answers` (seed от работы и ученика — как в `PRODUCT STRATEGY.md` §14).
9. **Телефон.** Шаги в колонку, сводка внизу, «Выдать работу» закреплена над `BottomNavigation`. Предпросмотр — отдельным экраном.

#### Т-3. Результаты работы или зачёта — `/dlya-uchitelya/rabota/?id=…` · сервер: да

1. **Назначение.** Видеть, кто сдал и как, и отреагировать.
2. **Элементы.** 1) Шапка: название, вид, правила, класс, срок, «сдали N из M». 2) Таблица `Table`: ученик, статус (не начал / в процессе / сдана / сдана с опозданием / не сдана), верно из всего, время, ошибки по типам. По умолчанию сверху те, кому нужно внимание (больше ошибок, не сдали). 3) Сводка по типам задач: доля верных по классу и отметка «много ошибок в одном методе» (порог — этап 3). 4) «Открыть результат ученикам» — если показ «когда откроет учитель». 5) «Продлить срок» — всем или одному ученику (личный срок). 6) «Лист для печати»: ученика и с ответами. 7) «Закрыть приём работ» — после этого сдать нельзя, несданные остаются «не сдана».
   Для зачёта колонки другие: попытка, процент, сдан / не сдан, осталось пересдач.
3. **Навигация.** Откуда: О-4, О-8, главная, уведомление «сдано». Куда: Т-4.
4. **CTA.** Открыть работу ученика (строка таблицы). Если показ ручной и все сдали — «Открыть результат ученикам».
5. **Пустое состояние.** Никто не начал — таблица со статусами «не начал» (это тоже данные) и плашка «Пока никто не начал работу. Срок — {дата}».
6. **Загрузка.** Скелет таблицы.
7. **Ошибка.** `ErrorState` «Результаты не загрузились» и «Попробовать ещё раз».
8. **Данные.** Хранится: `assignments`, `deadlines`, `assignment_submissions`, `attempts` по работе; для зачёта — `theory_test_assignments`, `theory_attempts`. Вычисляется: статусы, верно из всего, время, доли по типам. Самостоятельные работы учеников в эту таблицу не попадают.
9. **Телефон.** Таблица превращается в карточки учеников: статус и «верно из всего». Сводка по типам — сверху, свёрнута в `Details`.

#### Т-4. Работа ученика — `/dlya-uchitelya/rabota/?id=…&uchenik=…` · сервер: да

1. **Назначение.** Разобрать работу одного ученика и оставить комментарий.
2. **Элементы.** 1) Ученик, статус, время начала и сдачи. 2) По каждой задаче: условие с его числами, его ответ, верный ответ (видит только учитель), верно или неверно, брал ли подсказку, открывал ли разбор. 3) Тип ошибки — если распознан по заранее описанным неверным ответам (`errors`); иначе не показывается. 4) Поле «Комментарий ученику» и «Отправить». 5) «Назначить повтор ошибок» → Т-2 с заполненным составом. 6) Переход к предыдущему и следующему ученику. 7) «Лист этого варианта» для печати.
3. **Навигация.** Откуда: Т-3, Т-5, История решений учителя. Куда: Т-2, соседний ученик.
4. **CTA.** «Отправить комментарий».
5. **Пустое состояние.** «Ученик ещё не начинал работу» и «Продлить срок ученику».
6. **Загрузка.** Скелет списка задач.
7. **Ошибка.** Комментарий не отправился — текст остаётся в поле, «Не отправилось. Нажмите ещё раз».
8. **Данные.** Хранится: `assignment_tasks` + `generated_tasks` ученика, верные ответы через `teacher_task_answers` (только учителю этой работы), `attempts` (ответ, `hint_used`, `solution_viewed`), `errors`, `assignment_submissions.teacher_comment`. При открытии экрана ставится `reviewed_at`.
9. **Телефон.** Задачи списком, комментарий внизу, «Отправить» закреплена.

#### Т-5. Карточка ученика — `/dlya-uchitelya/uchenik/?id=…` · сервер: да

1. **Назначение.** Полная картина по одному ученику.
2. **Элементы.** 1) Имя, класс, школа, последний вход, открыто ли платное задание (только факт). 2) Прогресс по заданиям — в том же виде, что у ученика в О-5. 3) Работы учителя: статус и результат. 4) Самостоятельные работы с пометкой «самостоятельная» (видны без уведомлений — решение Натальи). 5) Зачёты: статус и попытки; самостоятельные попытки — с пометкой «самостоятельный». 6) Ошибки по типам за период (`Select`: 7 дней, 30 дней, всё время) и «Назначить повтор ошибок». 7) «Убрать из класса».
3. **Навигация.** Откуда: Т-1, О-5, О-8 «Ученики», Т-3. Куда: Т-2, Т-4.
4. **CTA.** «Назначить повтор ошибок»; неактивна с подписью «У ученика нет неотработанных ошибок», если их нет.
5. **Пустое состояние.** «Ученик ещё ничего не решал».
6. **Загрузка.** Скелеты блоков.
7. **Ошибка.** `ErrorState` в границах блока.
8. **Данные.** Хранится: `profiles`, `students`, `group_members`, `entitlements` (только факт доступа), `statistics`, `attempts`, `assignment_submissions` (с признаком «самостоятельная»), `theory_attempts`, `imported_progress`. Вычисляется: все агрегаты. Учитель видит только учеников своих классов (правило `teaches_student` из `SECURITY.md`).
9. **Телефон.** Блоки в колонку; «Назначить повтор ошибок» закреплена над `BottomNavigation`.

#### Т-6. Банк вопросов зачётов — `/dlya-uchitelya/voprosy/` · сервер: да · только роль «учитель»

1. **Назначение.** Видеть, по каким заданиям и методам вопросы готовы, и управлять ими.
2. **Элементы.** 1) Фильтры: задание (13 из `tasks.ts`, открытые первыми) и метод. 2) По каждому заданию: «опубликовано N, черновиков K». 3) Список вопросов: метод, формат, статус (черновик / опубликован), версия, дата изменения, начало текста. 4) «Добавить вопрос». 5) «Редактировать», «Снять с публикации» (сданные попытки не меняются). 6) «Выдать зачёт по этому заданию» — активна при N ≥ 1. 7) Поиск по тексту вопроса.
3. **Навигация.** Откуда: «Зачёты» в О-8, главная (черновики). Куда: Т-7, Т-8.
4. **CTA.** «Добавить вопрос».
5. **Пустое состояние.** «Вопросов пока нет. Зачёт по заданию можно будет выдать, когда появится хотя бы один опубликованный вопрос» и «Добавить вопрос». По заданию без вопросов — строка «Материал готовится», как у ученика.
6. **Загрузка.** Скелеты строк.
7. **Ошибка.** `ErrorState` и «Попробовать ещё раз».
8. **Данные.** Хранится: `theory_questions` (задание, метод, формат, статус, текущая версия), `theory_question_versions` (текст, дата, номер версии). Вычисляется: счётчики опубликованных и черновиков. Методы — из конфига разделов (`content/metody.ts`, `content/veroyatnost-metody.ts`, `content/quadraticMetody.ts`); у задания без карточек методов поле метода необязательное.
9. **Телефон.** Фильтры — `Select`, список — карточки. Пользоваться можно, но редактор удобнее на широком экране.

#### Т-7. Редактор вопроса — `/dlya-uchitelya/voprosy/vopros/?id=…` · сервер: да · только роль «учитель»

Экран выполняет требования Натальи а–е (решение 13 в шапке документа).

1. **Назначение.** Написать или поправить вопрос зачёта и опубликовать его.
2. **Элементы.** 1) Привязка: задание (`Select`, названия из `tasks.ts`) → метод (`Select` из карточек методов задания или «без метода»). 2) Формат (`Radio`): выбор одного, выбор нескольких, ввод ответа (число или короткий текст), соответствие, порядок шагов. 3) Текст вопроса — `TexField` (новое): поле с формулами в `$…$` и живым предпросмотром KaTeX рядом — тот же набор формул `lib/tex.ts`, что увидит ученик (требование в). 4) Блок ответа по формату: варианты (каждый — `TexField`) с отметкой верного; верный ответ для ввода — число сравнивается через `parseAnswer`, поэтому «0,5», «0.5» и «1/2» — одна запись; пары соответствия; шаги в правильном порядке. 5) Пояснение к вопросу — ученик видит его после зачёта только при «сдан» (П-12). 6) Предпросмотр «как увидит ученик»: без отметки верного, варианты перемешаны. 7) «Сохранить черновик» и «Опубликовать»; строка версии: «Опубликована версия 2 · в сданных попытках — версии 1 и 2».
3. **Навигация.** Откуда: Т-6. Куда: Т-6 после сохранения.
4. **CTA.** «Опубликовать» для черновика, «Опубликовать изменения» для опубликованного вопроса. Второстепенное: «Сохранить черновик» (требование г).
5. **Пустое состояние.** Новый вопрос: поля пусты, в предпросмотре «Здесь появится вопрос так, как его увидит ученик».
6. **Загрузка.** Скелет полей при открытии. Предпросмотр формул — мгновенно в браузере: KaTeX уже есть в проекте, сервер не нужен.
7. **Ошибка.** Формула не собирается — предпросмотр показывает место ошибки (рендер без падения страницы), публикация заблокирована, пока формула не собирается. Не отмечен верный вариант — «Отметьте верный вариант». Сохранение не прошло — текст остаётся в поле и в черновике браузера, «Не сохранилось. Нажмите ещё раз».
8. **Данные.** Хранится: `theory_questions` (задание, метод, формат, статус «черновик / опубликован»), `theory_question_versions` (текст, варианты, пояснение, автор, дата, номер версии — после публикации не меняются), `theory_answer_keys` (верный ответ версии; читает только сервер при проверке и роль «учитель» в редакторе — требование б). Правка опубликованного создаёт новую версию; сданные попытки ссылаются на свою версию и не меняются (требование д). В зачёт попадают только опубликованные версии (требование г). Доступ к экрану и к таблице ответов проверяется по роли на сервере (требование а). Вычисляется: «в сданных попытках — версии …».
9. **Телефон.** Поле и предпросмотр рядом не помещаются — над полем переключатель `Tabs` «Текст / Предпросмотр». Кнопки закреплены над `BottomNavigation`.

#### Т-8. Выдать зачёт — `/dlya-uchitelya/vydat-zachet/` · сервер: да

1. **Назначение.** Назначить классу или ученикам зачёт по заданию.
2. **Элементы.** 1) Задание — только с опубликованными вопросами; остальные неактивны с подписью «Материал готовится». 2) Сколько вопросов в попытке — задаёт учитель; по умолчанию все опубликованные; если меньше — для каждой попытки случайная выборка из банка (П-13). 3) Порог — по умолчанию 80 % (конфиг), учитель может изменить. 4) Пересдачи и пауза — из конфига (3 и 24 часа), показываются справкой. 5) Кому: класс или ученики; срок. 6) «Выдать зачёт».
3. **Навигация.** Откуда: О-8, Т-6, главная. Куда: Т-3 в режиме зачёта.
4. **CTA.** «Выдать зачёт» — неактивна без опубликованных вопросов (требование е).
5. **Пустое состояние.** Ни по одному заданию нет вопросов — `EmptyState` «Материал готовится. Добавьте вопросы в банк» и «Добавить вопрос».
6. **Загрузка.** Скелет списка заданий.
7. **Ошибка.** Нет связи — «Зачёт не выдан. Попробуйте ещё раз». Срок в прошлом — `FieldMessage`.
8. **Данные.** Вычисляется: число опубликованных вопросов по заданию из `theory_questions`. Конфиг: порог по умолчанию, 3 пересдачи, 24 часа (новый конфиг зачётов). Хранится при выдаче: `theory_test_assignments` (задание, кому, срок, порог, число вопросов в попытке).
9. **Телефон.** Форма в колонку, «Выдать зачёт» закреплена над `BottomNavigation`.

### 2.6. Новые компоненты

| Компонент | Зачем | Почему не хватает существующих |
|---|---|---|
| `WorkSheetScreen` | Работа целиком: все задачи на одном экране, черновик ответов, одна сдача | `TrainerScreen` показывает задачи по одной и проверяет каждую сразу |
| `CountdownTimer` | Остаток времени работы по серверному времени, предупреждение за несколько минут до конца | Таймера в проекте нет |
| `QuestionMatch` | Формат вопроса «соответствие» | `Radio` и `Checkbox` покрывают только выбор |
| `QuestionOrder` | Формат вопроса «порядок шагов» | Такого элемента нет |
| `TexField` | Поле с формулами и живым предпросмотром, как увидит ученик | `Input` без предпросмотра; сам KaTeX и `lib/tex.ts` уже есть |
| `InviteCard` | Ссылка и код класса, копирование, код крупно для доски, QR | Нет. QR — библиотека `uqr` (unjs): лицензия MIT, без зависимостей, строит SVG прямо в браузере без внешних сервисов, подключается только на странице класса. Размер и лицензию проверить при установке |
| `AccessCodeGate` | Плашка кода доступа поверх вкладок платного задания | Нет; вкладки задания сейчас открыты всем |
| `OnboardingSteps` | Три шага знакомства и шаг переноса внутри `Modal` | `Modal` есть, пошагового содержимого нет |
| `FavoriteToggle` | Звезда «в избранное» на подтеме, разделе теории, в итоге тренажёра | Нет; нужна только если «Избранное» войдёт в MVP (П-9) |
| `lib/deadlines.ts` (не компонент) | Статусы и порядок сроков для `HomeworkCard` | Логика есть только в старом сайте (`assets/js/deadlines.js`), переносится как есть |

### 2.7. Изменения существующих компонентов (в будущих сессиях; меню не трогается)

| Компонент | Что меняется | Зачем |
|---|---|---|
| `Topbar` | Блок пользователя — ссылка на `/profil/`; `unread` по умолчанию `false` и берётся из данных | Вход в профиль; убрать вечную точку (П-5) |
| `AppShell` | Пользователь из аккаунта вместо `demoUser`; передаёт `unread` | Убрать DEMO (П-5) |
| `Heatmap` | Состояние «нет данных» у ячейки | Ученик без попыток не должен выглядеть как «0 %» |
| `Table` | Вид «карточки» ниже 768px | Таблицы результатов на 360px |
| `TaskCard` | Бейдж «По коду доступа» и замок | Видно платное задание ещё в банке |
| `GeneratorScreen` | Кнопка «Выдать как работу» для роли «учитель» | Путь учителя, этап 4: выдача из генератора |
| `TrainerScreen` | Проверка ответа через переданную функцию: в тренажёре — как сейчас, в работе — запрос к серверу | Ответы работ не должны попадать в браузер |
| `HomeworkCard` | Без изменений: срок передаётся в `priority` и `priorityTone` | Проп уже есть |

### Чеклист этапа 2

- [x] Не изменён ни один файл, кроме `docs/CABINET_UX_SPEC.md`
- [x] Меню не тронуто, «Настроек» нет (экран `/nastroyki/` не проектируется; расхождения с меню — в П-7, П-8)
- [x] Нет показателей без источника данных (у каждого экрана пункт 8)
- [x] Нет выдуманного содержания (ни одного вопроса зачёта; тексты согласия и знакомства — от Натальи; числа по умолчанию — из её решений или открытые вопросы)
- [x] Все термины объяснены
- [x] Для каждого экрана указано, нужен ли сервер (карта 2.2 и заголовок экрана)

---

## Этап 3. Автоматизации

Автоматизация — действие, которое система выполняет сама, без нажатия человека. Каждая описана тройкой: **trigger** (событие, которое запускает действие) → **condition** (условие, при котором действие выполняется) → **action** (что именно делает система).

### 3.0. Общие правила

**Письма.**
1. Письма-напоминания и сводки уходят **только тем, кто дал согласие на письма** (`profiles.consents`, вид «письма», значение «да») и подтвердил почту. Без согласия событие остаётся только в кабинете.
2. Служебные письма — подтверждение почты и сброс пароля — уходят без согласия на рассылку: без них аккаунт не работает. Автоматизациями этапа 3 они не считаются.
3. В каждом письме внизу строка «Вы получили это письмо, потому что разрешили напоминания на почту» и ссылка «Отключить письма» — отзыв согласия в один клик, без входа в кабинет.
4. Тон дружелюбный, без давления. В письмах нет слов «срочно», «последний шанс», «вы опять не…», нет восклицательных знаков и красных пометок. Письмо называет факт, срок и даёт одну ссылку.
5. Не больше одного письма-напоминания в день одному ученику. Если в один день набралось несколько событий, уходит одно письмо-сводка со списком.
6. Окно отправки — 8:00–21:00 по времени ученика. Часовой пояс определяется по браузеру при регистрации (`profiles.timezone`), по умолчанию — Москва. Письмо, попавшее вне окна, уходит в начале следующего окна. Числа — в конфиге.
7. Почтовый сервис — абстрактный: российский, данные хранятся в России (решение 17).

**Надёжность.**
1. Каждая автоматизация идемпотентна (повторный запуск не создаёт дубль): у каждого уведомления и письма есть ключ «вид события + объект + получатель», повтор с тем же ключом пропускается.
2. Все сроки хранятся в едином времени сервера; в интерфейсе и письмах показываются в часовом поясе пользователя.
3. Самостоятельные работы и самостоятельные попытки зачёта **никогда** не создают уведомлений учителю (решения 9 и 23).

**Что значит «технически нужно».**
- **База** — PostgreSQL в Supabase: таблицы, триггер базы (действие, которое база выполняет сама сразу после записи строки).
- **Запуск по расписанию (cron)** — `pg_cron` внутри базы (уже заложен в `DATABASE SCHEMA.md` §3.6).
- **Очередь** — отложенная обработка тяжёлых действий, чтобы экран не ждал: генерация задач на весь класс, отправка писем. На объёмах одного учителя достаточно таблицы заданий в той же базе, которую разбирает запуск по расписанию; внешняя очередь не нужна.
- **Отправка почты** — российский почтовый сервис.

### 3.1. Список автоматизаций

| № | Название | Trigger | Condition | Action | Канал | Кто получает | Что нужно технически | Приоритет |
|---|---|---|---|---|---|---|---|---|
| А-1 | Выдача работы | Учитель нажал «Выдать работу» (Т-2) | Выбраны получатели; срок в будущем; работа не самостоятельная | Для каждого ученика сгенерировать задачи на своём seed, сохранить ответы в закрытой таблице, поставить статус «выдана»; создать уведомление в кабинете; поставить письмо в очередь тем, у кого есть согласие | Кабинет + почта | Ученики-получатели | База, очередь (генерация на класс и письма), отправка почты | MVP |
| А-2 | Выдача зачёта | Учитель нажал «Выдать зачёт» (Т-8) | По заданию есть опубликованные вопросы; число вопросов в попытке не больше числа опубликованных | Записать назначение зачёта; уведомление в кабинете; письмо тем, у кого есть согласие | Кабинет + почта | Ученики-получатели | База, очередь, отправка почты | MVP |
| А-3 | Напоминание за 48 часов | Запуск по расписанию раз в час находит работы и зачёты со сроком через 48 часов | Ученик не сдал; приём открыт; работа выдана раньше чем за 48 часов до срока; такое напоминание ещё не отправлялось | Уведомление в кабинете; письмо тем, у кого есть согласие (или строка в дневной сводке) | Кабинет + почта | Ученик | База, cron, очередь, отправка почты | MVP |
| А-4 | Напоминание за 24 часа | Тот же запуск по расписанию: срок через 24 часа | Как у А-3; работа выдана раньше чем за 24 часа до срока | Уведомление в кабинете; письмо тем, у кого есть согласие | Кабинет + почта | Ученик | База, cron, очередь, отправка почты | MVP |
| А-5 | Срок наступил | Запуск по расписанию: прошли срок и `grace_minutes` | Работа или зачёт не закрыты вручную | Несдавшим поставить отметку «не сдана» (сдать ещё можно, пока приём открыт); учителю — одно сводное уведомление «Не сдали K из M» с именами; если показ результата «после срока» — запустить А-12 | Кабинет | Учитель (сводка); ученик видит статус на карточке | База, cron | MVP |
| А-6 | Автопроверка ответа (сдача по одной задаче) | Ученик нажал «Проверить» в работе | Задача выдана этому ученику; приём открыт; не превышен лимит частоты ответов (`SECURITY.md` §7) | Сервер нормализует ответ, сравнивает с закрытым ответом, пишет попытку; отвечает «верно / неверно», в режиме «Контроль» — «ответ принят»; при неверном ответе сверяет его с заранее описанными неверными ответами и записывает тип ошибки, если нашёл | Кабинет (на экране) | Ученик | База (проверка на сервере) | MVP |
| А-7 | Проверка работы при сдаче целиком и автосдача по времени | Ученик нажал «Сдать работу» или истекло время работы | Сдачи ещё не было (одна сдача) | Проверить все ответы черновика разом, записать попытки, посчитать результат; статус «сдана» или «сдано с опозданием»; открыть результат по настройке работы (у самостоятельной — сразу) | Кабинет | Ученик | База; для автосдачи — cron (сдать работы, у которых время вышло, а запроса не было) | MVP |
| А-8 | Учителю: «сдано» | Работа учителя сдана (А-7 или «Сдать работу» в режиме по одной задаче) | Работа не самостоятельная | Уведомление в кабинете «{Ученик} сдал работу «{название}»: {верно} из {всего}» с пометкой «с опозданием», если так | Кабинет | Учитель | База, триггер базы | MVP |
| А-9 | Учителю: «много ошибок в одном методе» | Сдача очередным учеником работы учителя; повторно — при наступлении срока | Не менее 40 % сдавших ошиблись хотя бы в одной задаче этого метода, при не менее 3 сдавших (числа в конфиге); для этой работы и этого метода сигнал ещё не отправлялся | Уведомление учителю с названием метода, числом ошибившихся и кнопкой «Назначить повтор ошибок» этим ученикам (открывает Т-2 с заполненным составом) | Кабинет | Учитель | База, триггер базы | MVP |
| А-10 | Проверка зачёта | Ученик нажал «Сдать зачёт» | Попытка ещё не сдана; ответы сверяются с версиями вопросов этой попытки | Сравнить ответы с закрытыми ответами, посчитать процент, «сдан» — если процент не ниже порога (по умолчанию 80 %); записать; ученику — итог по правилам П-12; учителю — уведомление «сдан / не сдан» (кроме самостоятельных попыток) | Кабинет | Ученик; учитель | База (проверка на сервере) | MVP |
| А-11 | Пересдача доступна | Запуск по расписанию раз в час: после несданной попытки прошло 24 часа | Зачёт выдан и не сдан; пересдачи остались (не более 3); срок зачёта не прошёл | Уведомление в кабинете «Пересдача доступна»; письмо тем, у кого есть согласие | Кабинет; почта — позже | Ученик | База, cron; для письма — очередь и отправка почты | MVP (кабинет), письмо — позже |
| А-12 | Открытие результатов | Наступил срок (А-5) при показе «после срока» или учитель нажал «Открыть результат ученикам» (Т-3) | Работа сдана учеником; результат ещё не открыт | Открыть итог и разбор (`result_released_at`); уведомление «Результат работы открыт» | Кабинет | Ученики, сдавшие работу | База, cron | MVP |
| А-13 | Открытие доступа после ввода кода | Ученик нажал «Открыть доступ» (У-5) | Код существует; активаций меньше лимита; срок действия кода не прошёл; не превышен лимит попыток ввода | Записать активацию и выдать подписанную метку доступа в браузер (до С-3), а у вошедшего ученика — доступ за аккаунтом; увеличить счётчик активаций (лимит 3, конфиг); повторный ввод того же кода на новом устройстве — новая активация; показать «Задание №{N} открыто до {дата}». При неудаче — один общий текст «Код не подошёл» и счётчик попыток | Кабинет | Ученик | База (проверка только на сервере), лимит попыток | MVP |
| А-14 | Окончание срока доступа | Запуск по расписанию раз в сутки | а) До окончания доступа осталось 14 дней или 3 дня, предупреждения с этим сроком ещё не было; б) срок доступа прошёл. Срок у каждого кода свой, по умолчанию — 31 июля 2027 года. Числа и дата — в конфиге | а) Уведомление и письмо тем, у кого есть согласие, со ссылкой «Как продлить доступ»; б) задание закрывается плашкой «Срок доступа закончился» — отдельной записи не нужно, доступ считается по дате; уведомление ученику. Прогресс и статистика сохраняются | Кабинет + почта | Ученик | База, cron, очередь, отправка почты | MVP |
| А-15 | Пересчёт прогресса | Записана попытка (любая: тренажёр, опорные задачи, работа, зачёт); плюс ночной запуск | — | Сразу: обновить агрегат ученика по прототипу (`statistics`: решено, верно, время). Ночью: сверить агрегаты с попытками, обновить витрины учителя (тепловая карта, «кому нужна помощь») | Нет (данные на экранах О-3, О-5, Т-5) | — | База: триггер базы и cron | MVP |
| А-16 | «Повтор ошибок» по неверно решённым задачам | Закрыта задача | Неверный ответ, подсказка или открытый разбор — тип задачи попадает в список ошибок ученика; решено начисто — уходит из него (правило из `lib/trainerProgress.ts`) | Обновить список ошибок; на главной ученика карточка «N задач ждут повтора» с кнопкой «Повторить ошибки» (тренажёр в режиме «Повтор ошибок», те же типы на новых числах). У учителя — кнопка «Назначить повтор ошибок» в Т-4, Т-5 и в сигнале А-9 | Кабинет | Ученик; учитель | База (сейчас — `localStorage`) | MVP |
| А-17 | Возврат неактивного ученика | Запуск по расписанию раз в сутки | Нет входа 10 дней; такого письма не было последние 14 дней; есть согласие на письма (числа в конфиге) | Письмо «Задания ждут вас» с одной ссылкой: на ближайшую несданную работу, а если её нет — на слабое место из статистики. Следующее такое письмо — не раньше чем через 14 дней. Учитель видит отметку «не заходил» в «Кому нужна помощь» раньше — после 7 дней без входа, без письма | Почта; учителю — кабинет | Ученик; учитель | База, cron, очередь, отправка почты | Позже |
| А-18 | Сдача после срока и закрытие приёма | а) Ученик сдал работу после срока и `grace_minutes`; б) учитель нажал «Закрыть приём работ» (Т-3) | а) Приём открыт (по умолчанию); б) — | а) Статус «сдано с опозданием», учителю — уведомление А-8 с этой пометкой; б) кнопка сдачи у учеников заменяется на «Приём работы закрыт. Напишите учителю», несданные остаются «не сдана», ученикам без сдачи — уведомление в кабинете | Кабинет | Учитель; ученики без сдачи | База | MVP |

Всего 18 автоматизаций: 15 по базовому лимиту и 3 из разрешённых дополнительных четырёх — на работы и зачёты (А-2, А-10, А-11).

### 3.2. Какие можно сделать уже сейчас без сервера, а какие — только после запуска Supabase

**Уже сейчас, на `localStorage`** (работают частично или полностью уже сегодня, данные видит только этот браузер):

| № | Что уже есть | Чего не хватает без сервера |
|---|---|---|
| А-6 | Проверка ответа в тренажёре и опорных задачах по отпечатку (`lib/secret.ts`) | Проверки ответов в работах — работ без сервера нет |
| А-15 | Счётчики пересчитываются сразу после каждой задачи (`recordAttempt`, `summarize`) | Учитель их не видит; другие устройства — тоже |
| А-16 | Список ошибок и режим «Повтор ошибок» в тренажёрах №3, №4, №5, №8, №12 | Назначения повтора учителем |

Без сервера можно ещё одно улучшение, если понадобится до запуска кабинетов: карточка «N задач ждут повтора» на странице задания по данным `localStorage`. Это не автоматизация с письмами, а подсказка на экране.

**Только после запуска Supabase:** А-1, А-2, А-3, А-4, А-5, А-7, А-8, А-9, А-10, А-11, А-12, А-13, А-14, А-17, А-18. Причины: им нужны аккаунты, общие для ученика и учителя данные, проверка ответа на сервере, запуск по расписанию или отправка почты. А-13 и А-14 при этом нужны раньше кабинетов: коды доступа по дорожной карте — с октября 2026 (пакет А2), и проверять их можно только на сервере.

### 3.3. Тексты писем — черновик, на утверждение Натальи

Черновики приняты к рассмотрению и утверждаются отдельно.

В каждом письме подпись «Будет на ЕГЭ» и строка про отписку из 3.0. В фигурных скобках — подстановки из данных.

| № | Тема письма | Текст | Кнопка |
|---|---|---|---|
| А-1 | Новая работа: «{название}» | Здравствуйте, {имя}. Учитель {имя учителя} выдал новую работу «{название}». Задач: {M}. Сдать до {дата, время}. | Открыть работу |
| А-2 | Новый зачёт по заданию №{номер} | Здравствуйте, {имя}. Учитель {имя учителя} выдал зачёт по заданию №{номер} «{название задания}». Сдать до {дата, время}. | Открыть зачёт |
| А-3 | Работа «{название}»: срок {дата} | Здравствуйте, {имя}. Напоминаем: работу «{название}» нужно сдать до {дата, время}. Решено {N} из {M}. | Продолжить работу |
| А-4 | Работа «{название}»: срок завтра | Здравствуйте, {имя}. Срок сдачи работы «{название}» — завтра, {время}. Решено {N} из {M}. | Продолжить работу |
| Сводка | Напоминания на сегодня | Здравствуйте, {имя}. Что ждёт вас в ближайшие дни: {список «название — срок»}. | Открыть мои задания |
| А-11 | Пересдача зачёта доступна | Здравствуйте, {имя}. Пересдача зачёта по заданию №{номер} уже доступна. Осталось попыток: {R}. Перед пересдачей можно повторить методы: {список методов}. | Открыть зачёт |
| А-14а | Доступ к заданию №{номер} до {дата} | Здравствуйте, {имя}. Доступ к заданию №{номер} «{название задания}» действует до {дата}. Если хотите заниматься дальше, продлить доступ можно по ссылке ниже. | Как продлить доступ |
| А-14б | Доступ к заданию №{номер} закончился | Здравствуйте, {имя}. Доступ к заданию №{номер} закончился {дата}. Ваш прогресс и статистика сохранены. Когда появится новый код, введите его на странице задания. | Как получить код |
| А-17 | Задания ждут вас | Здравствуйте, {имя}. Давно не виделись. {Если есть работа: «Работа «{название}» ждёт до {дата}.» Иначе: «Можно потренировать {тип задач} — там больше всего ошибок.»} | Открыть кабинет |

Учителю писем нет: все события и сводка — только в кабинете. Возможность включить письмо-сводку учителю — в разделе «Позже» (П-15).

### Чеклист этапа 3

- [x] Не изменён ни один файл, кроме `docs/CABINET_UX_SPEC.md`
- [x] Меню не тронуто, «Настроек» нет
- [x] Нет показателей без источника данных (пороги — из решений Натальи или открытые вопросы с хранением в конфиге)
- [x] Нет выдуманного содержания (тексты писем — черновик на утверждение; числа, которых Наталья не называла, не придуманы)
- [x] Все термины объяснены (trigger, condition, action, cron, очередь, триггер базы, идемпотентность)
- [x] Для каждой автоматизации указано, нужен ли сервер (3.2)
- [x] Письма — только тем, кто дал согласие; тон без давления

---

## Этап 4. Сводный план разработки

### 4.0. Находка перед планом: на статическом хостинге код доступа ничего не закрывает

Сейчас сайт — готовые HTML-файлы на Timeweb: сборка `output: 'export'`, выгрузка по FTP (`docs/DEPLOYMENT.md`). Любая страница задания №12 лежит на хостинге файлом и открывается по прямому адресу у любого посетителя. Плашка кода поверх такой страницы — только видимость, и правило «код проверяется исключительно на сервере» так выполнить нельзя.

Чтобы код действительно закрывал задание, страницы платного задания должен отдавать сервер — после проверки доступа. Для этого нужны собственный сервер (VDS на Selectel, пакет А1 дорожной карты) и перевод приложения из статической сборки в серверный режим Next.js. Поэтому «первая сессия — коды доступа» разбита на две: **С-1 «Сервер»** и **С-2 «Коды доступа»**. Обе идут раньше кабинетов (решение 40).

Честная оговорка. Даже с сервером код движка тренажёра и генератора №12 уходит в браузер: так устроен тренажёр, он собирает задачи на месте. Сервер закрывает страницы, но файлы скриптов (`/_next/static/…`) отдаются без проверки. Это тот же уровень защиты, что описан в `lib/secret.ts`: от случайного доступа, а не от человека с отладчиком. Для работ и зачётов задачи генерируются на сервере с сессии С-5. Для самого тренажёра перенос на сервер записан в «Позже».

`docs/TECH ARCHITECTURE.md` рекомендует хостинг Vercel и Supabase Cloud в Европе. Это уже заменено решением из `docs/PRODUCT STRATEGY.md` §4 и `LEGAL IP RU.md` §3: продакшен и база — в России, Supabase разворачивается на своём сервере. План ниже исходит из этого.

### 4.1. Сущности базы данных

Сверка с `docs/DATABASE SCHEMA.md`. Статус: **описано** — таблица есть в схеме и подходит как есть; **описано, новые поля** — таблица есть, нужны поля из этапов 1–3; **новое** — таблицы в схеме нет.

**Пользователи и классы**

| Сущность | Для чего | Статус | Что нового | Сессия |
|---|---|---|---|---|
| `profiles` | Аккаунт, роль, согласия, последний вход | описано, новые поля | `onboarded_at` (знакомство пройдено), `progress_imported_at` (перенос выполнен), `last_topic` («Продолжить» на главной), `timezone` (из браузера при регистрации, по умолчанию Москва). `consents` и `last_seen_at` уже есть | С-3 |
| `students` | Данные ученика | описано, новые поля | `school`, `age_confirmed_at` (отметка «14 лет»). `grade` есть. Поля `exam_date`, `target_score`, `guardian_email` пока не используются | С-3 |
| `teachers` | Данные учителя | описано, новые поля | `contact` — что ученики видят в карточке учителя | С-4 |
| `groups` | Класс или группа учителя с кодом приглашения | описано, новые поля | `academic_year`. `name`, `invite_code`, `invite_enabled`, `archived_at` есть | С-4 |
| `group_members` | Кто в каком классе | описано | — | С-4 |

**Доступ по коду**

| Сущность | Для чего | Статус | Что нового | Сессия |
|---|---|---|---|---|
| `access_codes` | Коды доступа к платному заданию | новое | Хеш кода (сам код не хранится), номер задания, лимит активаций (по умолчанию 3), число активаций, дата окончания (своя у каждого кода, по умолчанию 31.07.2027), кто создал, внутренняя пометка без персональных данных, `disabled_at` | С-2 |
| `code_activations` | Факт открытия задания кодом: на устройстве (решение 41) или за аккаунтом | новое | Код, хеш метки устройства или аккаунт, дата активации, `revoked_at` (отозвана сбросом). Считает лимит активаций; повторный ввод на новом устройстве — новая строка. При переносе в аккаунт строка связывается с аккаунтом, новой не создаётся | С-2 |
| `entitlements` | Действующий доступ к заданию: кому, к чему, до какой даты | есть в `PRODUCT STRATEGY.md` §15, **нет в схеме** — добавить | Аккаунт, задание, дата окончания, код-источник. Нужна с появлением аккаунтов (С-3) | С-2 / С-3 |
| `code_attempts` | Лимит попыток ввода кода против перебора | новое (в `SECURITY.md` §4 похожая `join_attempts` для кода класса) | Метка устройства или IP-хеш, время попытки | С-2 |

**Контент и задачи** — генераторы остаются в коде (движки `lib/graph`, `lib/zadanie3`, `lib/veroyatnost`, `lib/vychisleniya`); в базу пишутся только сгенерированные задачи.

| Сущность | Для чего | Статус | Что нового | Сессия |
|---|---|---|---|---|
| `exams`, `subjects`, `topics`, `prototypes`, `task_templates`, `task_parameters`, `error_types` | Каталог: задание → прототип → шаблон | описано | Нужно сопоставление идентификаторов наборов движков (например, `12.A`, `12Q.C`) с `prototypes.code` | С-5 |
| `generated_tasks` | Задача с числами ученика | описано | — | С-5 |
| `answers` | Верный ответ и разбор, закрытая таблица | описано | — | С-5 |
| `attempts` | Каждая закрытая задача ученика | описано, новые поля | `context` — где решал: тренажёр, опорные задачи, работа (сейчас в схеме есть только `assignment_id`) | С-5, С-7 |
| `errors` | Тип ошибки по заранее описанным неверным ответам | описано | — | С-5 |
| `statistics` | Агрегат «ученик × прототип» | описано | — | С-7 |

**Работы** (единая сущность, решение 8)

| Сущность | Для чего | Статус | Что нового | Сессия |
|---|---|---|---|---|
| `assignments` | Работа: домашняя или тренировочная | описано, новые поля | `kind` (домашняя / тренировочная); `mode` уже есть (`practice` / `control` = правила «обычные / Контроль»); `submission_mode` (по одной задаче / целиком); `time_limit_min` и `show_result` (сразу / после срока / вручную) — вынести из `config` в поля; `is_self` (самостоятельная); `message` (комментарий ученикам — предложено в `HTML REVIEW DZ3.md`); `accepting_closed_at` (учитель закрыл приём). **Изменить ограничение**: сейчас `teacher_id` обязателен, а у самостоятельной работы автор — ученик (`teacher_id` пусто, `student_id` = автор) | С-5, С-7 |
| `assignment_tasks` | Задачи работы для каждого ученика | описано | — | С-5 |
| `deadlines` | Общий или личный срок | описано | `late_policy` по умолчанию `accept_flagged` («сдано с опозданием», решение 30); `grace_minutes` есть | С-5 |
| `assignment_submissions` | Сдача работы учеником | новое | Ученик, работа, статус (не начал / в процессе / сдана / не сдана), `started_at`, `submitted_at`, черновик ответов ученика, `reviewed_at` (учитель посмотрел), `teacher_comment`, `result_released_at`. Результат не хранится — считается из попыток (принцип схемы). `attempt_comments` из `PRODUCT STRATEGY.md` §14 заменяется полем `teacher_comment` | С-5 |
| `worksheets`, `worksheet_tasks`, `generated_pdfs` | Варианты и PDF учителя | описано | Для листов выданных работ не обязательны: лист собирается по seed работы. Понадобятся для серверных PDF | позже |

**Теоретические зачёты** — всё новое

| Сущность | Для чего | Статус | Что нового | Сессия |
|---|---|---|---|---|
| `theory_questions` | Вопрос: задание, метод, формат, статус (черновик / опубликован), текущая версия | новое | — | С-6 |
| `theory_question_versions` | Неизменяемая версия вопроса: текст, варианты, пояснение, автор, дата, номер | новое | Правка опубликованного создаёт новую версию | С-6 |
| `theory_answer_keys` | Верный ответ версии — закрытая таблица, как `answers` | новое | Читают только сервер и роль «учитель» | С-6 |
| `theory_test_assignments` | Выданный зачёт: задание, кому, срок, порог, число вопросов в попытке; пересдачи и пауза — копия из конфига на момент выдачи | новое | — | С-6 |
| `theory_attempts` | Попытка: ученик, зачёт (пусто у самостоятельной), `is_self`, номер попытки, версии вопросов, порядок перемешивания, ответы, процент, сдан | новое | — | С-6 |

**Прогресс, уведомления, служебное**

| Сущность | Для чего | Статус | Что нового | Сессия |
|---|---|---|---|---|
| `theory_reads` | Прочитанные разделы теории (сейчас `lib/theoryRead.ts` в браузере) | новое | — | С-7 |
| `imported_progress` | Перенесённые из браузера счётчики, список ошибок, исходы | новое | Хранятся как счётчики, без выдуманных дат и попыток | С-3 |
| `notifications` | Уведомления в кабинете, точка на колокольчике | новое | Получатель, вид события, объект, параметры, `created_at`, `read_at`, ключ против дублей | С-5 |
| `email_outbox` | Очередь писем | новое | Получатель, шаблон, параметры, ключ против дублей, «не раньше» (окно 8:00–21:00), `sent_at`, ошибка | С-3 (служебные), С-9 |
| `jobs` | Очередь фоновых задач (генерация на класс, пересчёты) | новое | Разбирается запуском по расписанию `pg_cron` | С-5 |
| `favorites` | Избранное | новое | — | позже |
| `audit_log` | Журнал чувствительных действий | описано | Писать: создание и отключение кодов, «Сбросить активации» (кто, когда, какой код, сколько активаций было), смену кода класса, публикацию и снятие вопросов | С-2 и далее |
| `files` | Реестр файлов | описано | — | позже |
| `plans`, `subscriptions`, `payments`, `promo_codes` | Оплата | в `PRODUCT STRATEGY.md` §15, нет в схеме | — | позже (ЮKassa) |

**Конфиг, а не база** — значения, которые задаёт Наталья. Меняются правкой одного файла, без изменения кода экранов.

| Значение | Где используется | По умолчанию |
|---|---|---|
| Состав первой части ЕГЭ | «Формат ЕГЭ» в работах | сверяется с официальной демоверсией ФИПИ |
| «Кому нужна помощь» | О-5, Т-5 | точность ниже 60 % при не менее 10 решённых задачах задания |
| «Не заходил» | О-5 | 7 дней |
| Порог зачёта | Т-8 | 80 % |
| Пересдачи и пауза | У-9, А-11 | 3 пересдачи, 24 часа |
| Самостоятельная попытка зачёта | У-9 | 10 вопросов |
| «Много ошибок в одном методе» | А-9 | не менее 40 % сдавших, при не менее 3 сдавших |
| Срок действия кода | С-2, А-14 | 31.07.2027 (у каждого кода своя дата) |
| Лимит активаций кода | С-2, У-5 | 3 (у каждого кода можно задать свой) |
| Предупреждение об окончании доступа | А-14 | за 14 и за 3 дня |
| Окно писем и часовой пояс | 3.0 | 8:00–21:00 по времени ученика; Москва |
| Письмо неактивному (позже) | А-17 | после 10 дней, не чаще раза в 14 дней |
| Классы при регистрации | У-1 | 9, 10, 11, «Другое» |
| Версии текстов согласия и политики | У-2 | тексты — от юриста |

### 4.2. Сессии Claude Code

Порядок: сначала сервер и коды доступа (решение 40), затем аккаунты, затем кабинет учителя (Наталья — единственный учитель со своими учениками), потом кабинет ученика, статистика и письма. Сессия С-10 от сервера не зависит и может идти в любой момент.

Ветка у каждой сессии — `claude/<имя>-…` (окончание добавляется автоматически). Общие правила для всех сессий — из `PRODUCT BRIEF AND ROADMAP.md` §2: этапы с остановкой, никакого выдуманного содержания, цвета только из токенов, ответ никогда не в DOM, существующее не ломать, pull request не сливать самостоятельно, снимки 360 / 768 / 1440.

| № | Сессия | Ветка | Зависит от | Сервер |
|---|---|---|---|---|
| С-1 | Сервер | `claude/server-vds-…` | шаги Натальи в Selectel и Yandex Cloud (описаны в С-1), Д-1 | да |
| С-2 | Коды доступа | `claude/access-codes-…` | С-1, способ входа администратора (П-17), оферта | да |
| С-3 | Аккаунты и роли | `claude/accounts-…` | С-1, РКН, тексты юриста, почтовый сервис | да |
| С-4 | Кабинет учителя: классы и ученики | `claude/teacher-classes-…` | С-3 | да |
| С-5 | Работы | `claude/works-…` | С-4 | да |
| С-6 | Теоретические зачёты | `claude/theory-tests-…` | С-5 | да |
| С-7 | Кабинет ученика | `claude/student-cabinet-…` | С-5 | да |
| С-8 | Статистика учителя и сигналы | `claude/teacher-stats-…` | С-7 | да |
| С-9 | Письма и напоминания | `claude/emails-…` | С-5, С-6, утверждённые тексты писем | да |
| С-10 | Честный интерфейс | `claude/honest-ui-…` | — (правило для цифр лендинга — решение 45) | нет |

#### Д-1. Правка документов: убрать Cloudflare R2 (до С-1)

Решение 44: Cloudflare R2 — зарубежное хранилище, по 152-ФЗ для бэкапов не подходит; бэкапы — в Yandex Object Storage. Места, где R2 упоминается сейчас (поиск по репозиторию 24.09.2026):

| Файл | Строка | Что там сейчас | Что сделать |
|---|---|---|---|
| `docs/TECH ARCHITECTURE.md` | 448 | «Postgres внешняя копия: GitHub Action `pg_dump` → Cloudflare R2 (шифрование age)» | Заменить на Yandex Object Storage; шифрование оставить |
| `docs/TECH ARCHITECTURE.md` | 449 | «Storage: `rclone sync` Supabase → R2» | Заменить на Yandex Object Storage |
| `docs/TECH ARCHITECTURE.md` | 264 | «При росте объёма PDF — вынос в Cloudflare R2» | Заменить на российское хранилище (Yandex Object Storage) |
| `docs/TECH ARCHITECTURE.md` | 491 | Этап M6 «+ R2» | Убрать R2 |
| `docs/TECH ARCHITECTURE.md` | 504 | «Чертежи и PDF раздаются с Supabase CDN / R2» | Убрать R2 |
| `docs/TECH ARCHITECTURE.md` | 45, 65 | Вариант C в сравнении стеков «Neon + Auth.js + Drizzle + Cloudflare R2» | Это сравнение вариантов, а не план бэкапов; пометить, что R2 не подходит по 152-ФЗ |
| `TECH ARCHITECTURE.md` (копия в корне репозитория) | те же строки | то же | то же |
| `docs/REPOSITORY STRUCTURE.md` | 19 | `backup.yml # ежедневный pg_dump → R2` | Заменить на Yandex Object Storage |
| `docs/REPOSITORY STRUCTURE.md` | 109 | `StorageProvider: supabase.ts, (r2.ts позже)` | Убрать `r2.ts` |
| `docs/TECH ARCHITECTURE.pdf`, `docs/DATABASE SCHEMA.pdf`, `docs/SECURITY.pdf` и другие PDF-копии | — | Вероятно, тот же текст: проверить в этой сессии было нечем (нет программы для чтения PDF) | Пересобрать PDF после правки `.md` или пометить устаревшими |

В `docs/PRODUCT STRATEGY.md` R2 не упоминается: там уже Yandex Object Storage. В этой сессии правится только `docs/CABINET_UX_SPEC.md`, поэтому Д-1 записана как задача; когда её делать — открытый вопрос 26.

#### С-1. Сервер

- **Цель.** Сайт работает с собственного сервера в России, готового к базе и серверным проверкам.
- **Входит.** VDS на Selectel, арендованный Натальей на своё имя. Docker Compose (набор контейнеров, запускаемых одной командой): Next.js в серверном режиме вместо статической сборки, Supabase на своём сервере (PostgreSQL и авторизация — пока без пользователей), Caddy для HTTPS. Деплой по SSH из GitHub Actions вместо FTP (`DEPLOYMENT.md`, раздел про переезд). Ежедневный зашифрованный бэкап базы в Yandex Object Storage — у другого провайдера, чем сервер: если с сервером что-то случится, копии уцелеют. Хранение — 30 ежедневных и 12 месячных копий (как в `TECH ARCHITECTURE.md` §20). Старые страницы (корень репозитория, `/ege/3/`) продолжают открываться.
- **Не входит.** Коды, аккаунты, любые изменения интерфейса.
- **Зависимости.** Шаги Натальи ниже; Д-1 (желательно до С-1, чтобы документы не противоречили плану).
- **Готово, когда.** budetege.ru открывается с нового сервера. Снимки 360 / 768 / 1440 страниц из `scripts/preview-shots.mjs` совпадают с прежними. `pnpm lint`, `typecheck`, `build` проходят. Бэкап появляется в Yandex Object Storage каждый день и восстанавливается на пустой базе. Старый FTP-пароль отозван.

**Что делает Наталья — пошагово.** Названия кнопок в кабинетах провайдеров со временем меняются; в начале С-1 шаги сверяются с их текущей справкой. Пароли и секретные ключи **в чат не присылаются** — только в GitHub → Settings → Secrets and variables → Actions, как сейчас пароли FTP (`DEPLOYMENT.md`).

*Selectel — сервер.*
1. Зарегистрироваться на selectel.ru как физическое лицо, на своё имя. Подтвердить почту и телефон.
2. Включить двухфакторный вход в аккаунт — код из приложения на телефоне.
3. Пополнить баланс: Selectel работает по предоплате.
4. Создать облачный сервер в российском регионе. Система — Ubuntu LTS. Мощность — 4 ядра и 8 ГБ памяти (оценка из `PRODUCT STRATEGY.md` §4). Размер диска и точную версию системы разработчик подскажет в начале С-1.
5. При создании сервера вставить публичную часть SSH-ключа, которую даст разработчик в начале С-1. SSH-ключ — пара «замок и ключ» для входа на сервер без пароля; публичная часть — это «замок», она не секретна. Вход по паролю не включать.
6. Передать разработчику IP-адрес сервера — это не секрет.

*Yandex Cloud — хранилище бэкапов.*
1. Войти в Yandex Cloud (console.yandex.cloud) со своей учётной записью Яндекса и создать платёжный аккаунт как физическое лицо.
2. Создать бакет (bucket — отдельное хранилище файлов), например `budetege-backup`. Доступ — только закрытый: публичное чтение и публичный список файлов выключены.
3. Создать сервисный аккаунт — служебную учётную запись для программы, а не для человека. Дать ему права только на этот бакет: загружать и читать файлы. Точное название роли сверим в начале С-1.
4. Создать для сервисного аккаунта статический ключ доступа. Это два значения — идентификатор и секрет; секрет показывается один раз.
5. Вписать оба значения в GitHub → Settings → Secrets (имена секретов даст разработчик). В чат не присылать.
6. Настроить автоматическое удаление старых копий (правило жизненного цикла бакета) — по подсказке в С-1, чтобы хранились 30 ежедневных и 12 месячных копий.

*Домен и старый хостинг.*
1. В конце С-1, когда новый сервер проверен, у регистратора домена budetege.ru сменить адрес сервера (A-запись) на IP нового сервера. Где зарегистрирован домен — открытый вопрос 25.
2. После переезда сменить пароль FTP на Timeweb (старый считается скомпрометированным, `DEPLOYMENT.md`). Тариф статического хостинга оставить, пока новый сервер не проработает без сбоев, затем отключить.

#### С-2. Коды доступа (А-13, А-14)

- **Цель.** Задание №12 открывается только по коду, код проверяется только на сервере.
- **Входит.**
  - Таблицы `access_codes`, `code_activations`, `code_attempts`, запись в `audit_log`.
  - Плашка кода У-5 на №12; без кода открыта только «О задании» (решение 22).
  - Метка доступа (решение 41): после верного кода сервер выдаёт в браузер cookie — небольшую подписанную запись, которую нельзя подделать. Это одна активация из 3 (конфиг). Повторный ввод того же кода после очистки браузера или на новом устройстве — новая активация в пределах лимита. При каждом запросе сервер сверяет метку с базой: активация не отозвана, код не отключён, дата не прошла.
  - Проверка доступа на сервере до отдачи страницы (middleware — код, который сервер выполняет перед каждой страницей).
  - Страница выдачи кодов `/admin/kody/` для аккаунта администратора (решение 42): создать код (задание, дата окончания — по умолчанию 31.07.2027, лимит — по умолчанию 3), список кодов с числом активаций, «Отключить код», **«Сбросить активации»** у конкретного кода — с записью в `audit_log`: кто, когда, какой код, сколько активаций было (решение 43; что происходит со старыми метками — вопрос 24). Вход — по варианту, который Наталья выберет в П-17.
  - Лимит попыток ввода кода. Строка «Доступ действует до …» за 14 и 3 дня и закрытие после даты (А-14 — только на экране, без писем). Бейдж «По коду доступа» в банке (`TaskCard`).
- **Не входит.** Регистрация учеников, письма, ЮKassa, кабинеты.
- **Зависимости.** С-1; способ входа администратора (П-17, вопрос 22); поведение сброса (вопрос 24); оферта и политика опубликованы до продажи первых кодов (пакет А4, делает Наталья с юристом).
- **Готово, когда.** Без кода запрос любой вкладки №12, кроме «О задании», не отдаёт содержимого (проверка прямым запросом без метки доступа). С верным кодом задание открыто. Неверный, исчерпанный и просроченный код дают один и тот же текст. Четвёртое устройство с тем же кодом получает «Код не подошёл». «Сбросить активации» обнуляет счётчик и оставляет запись в журнале. После серии неудачных попыток — пауза. В браузер не уходит ни хеш кода, ни список кодов. Страница `/admin/kody/` недоступна без входа администратора. №3, №4, №5 работают как раньше.

#### С-3. Аккаунты и роли

- **Цель.** Вход, регистрация ученика с согласием, аккаунт учителя, профиль.
- **Входит.** О-1, У-1 (без вступления в класс — оно в С-4), У-2, У-3, У-4 с переносом прогресса, О-2. Перенаправление вошедших с `/` на `/kabinet/`; главная пока с честными пустыми состояниями. Роли `student` / `teacher` / `admin`, RLS (правила базы, кто какие строки видит). Аккаунт Натальи создаётся вручную. Отметка «14 лет», классы 9 / 10 / 11 / «Другое», `profiles.timezone`. Перенос метки доступа из браузера в аккаунт при регистрации (решение 41): активация связывается с аккаунтом, новой не тратит. Аккаунт администратора из С-2 входит в общую систему ролей с ролью `admin` — как именно, зависит от способа входа, выбранного в П-17. Российский почтовый сервис — только для служебных писем (подтверждение почты, сброс пароля). Блок пользователя в шапке — из аккаунта вместо `demoUser` (часть П-5).
- **Не входит.** Классы, работы, зачёты, письма-напоминания.
- **Зависимости.** С-1; уведомление в Роскомнадзор подано до первой регистрации; тексты политики и согласия от юриста; выбран почтовый сервис (российский, данные в России).
- **Готово, когда.** Без согласия на ПДн регистрация невозможна. Согласие хранится с версией и датой. Неподтверждённая почта не пускает в кабинет. Ученик не открывает страницы учителя (тест на чужой доступ — IDOR). Кнопка переноса появляется только при непустом браузере и один раз. Тест базы: ученик не читает чужой профиль.

#### С-4. Кабинет учителя: классы и ученики

- **Цель.** Наталья создаёт классы, приглашает учеников и видит их список.
- **Входит.** О-8 для учителя (вкладки «Классы» и «Ученики»; «Работы» и «Зачёты» — пустые состояния до С-5 и С-6). Т-1 с `InviteCard` и QR на `uqr`. Регистрация по ссылке-приглашению и вступление по коду класса (У-1, О-2, О-8 ученика). Карточка учителя для ученика (`teachers.contact`). Главная учителя (О-3): классы и «Начните с класса». Т-5 в базовом виде: имя, класс, последний вход.
- **Не входит.** Работы, зачёты, статистика.
- **Зависимости.** С-3.
- **Готово, когда.** Ученик по ссылке попадает в класс. Смена кода отключает старую ссылку. Учитель видит только учеников своих классов (тест RLS). QR строится без внешних запросов (проверка сетевых запросов страницы).

#### С-5. Работы

- **Цель.** Связка «учитель выдал → ученик сдал → учитель увидел».
- **Входит.** Таблицы работ из 4.1, `generated_tasks`, `answers`, `attempts`, `errors`, `notifications`, `jobs`. Генерация задач на сервере для каждого ученика — вызов существующих движков на сервере (они уже работают на сборке), без изменения их логики. Т-2, Т-3, Т-4, О-4 (вкладка «Работы», обе роли), У-6, У-7, О-6 (уведомления в кабинете, точка на колокольчике по данным). Компоненты `WorkSheetScreen`, `CountdownTimer`, `lib/deadlines.ts`; проверка через сервер в `TrainerScreen`; кнопка «Выдать как работу» в `GeneratorScreen`. «Формат ЕГЭ» из конфига. Автоматизации А-1, А-5, А-6, А-7, А-8, А-12, А-16 (для работ), А-18 — без писем.
- **Не входит.** Письма, зачёты, самостоятельные работы ученика (С-7), статистика и А-9 (С-8).
- **Зависимости.** С-4.
- **Готово, когда.** У двух учеников одной работы разные числа. В ответах сервера и в HTML ученика нет верных ответов (вкладка «Сеть» браузера и `pnpm test:secrets`). «Контроль» не показывает подсказки и разбор до сдачи. Сдача после срока — «сдано с опозданием», после закрытия приёма — невозможна. Автосдача по времени работает. Учитель видит таблицу и ответы ученика, назначает повтор ошибок из Т-4.

#### С-6. Теоретические зачёты

- **Цель.** Наталья пишет вопросы в редакторе, выдаёт зачёты, ученики сдают.
- **Входит.** Таблицы зачётов из 4.1. Т-6, Т-7 (`TexField`), Т-8, У-9, У-10, вкладка «Зачёты» в О-4 для обеих ролей. Самостоятельный зачёт (при опубликованных вопросах больше 10). `QuestionMatch`, `QuestionOrder`. А-2, А-10, А-11 — в кабинете.
- **Не входит.** Сами вопросы — их пишет Наталья. Письма.
- **Зависимости.** С-5 (общий список «Мои задания», уведомления).
- **Готово, когда.** Без опубликованных вопросов — «Материал готовится», выдать нельзя. Правка опубликованного вопроса создаёт версию, сданные попытки не меняются (тест). Верные ответы не приходят в браузер ученика ни в каком виде (вкладка «Сеть»). Вопросы и варианты перемешиваются, выборка случайная. Пересдача — не раньше 24 часов и не больше 3. Самостоятельная попытка не расходует пересдачи и не уведомляет учителя. Ученик получает отказ при открытии редактора.

#### С-7. Кабинет ученика

- **Цель.** Ученик видит, что делать, свой прогресс и собирает работы сам.
- **Входит.** О-3 для ученика («Что дальше», сроки, метрики), О-5 для ученика (три вкладки), У-8 (самостоятельные работы, «формат ЕГЭ — из N открытых заданий»). Запись попыток тренажёра и опорных задач на сервер для вошедших — через существующую прослойку `lib/storage.ts` и `lib/progressStore.ts`. `statistics`, `theory_reads`, А-15 и А-16 на сервере, перенесённые счётчики в статистике с пометкой.
- **Не входит.** Статистика учителя.
- **Зависимости.** С-5; перенос из С-3.
- **Готово, когда.** Числа на главной и в статистике совпадают с пересчётом из попыток (тест). Гость без аккаунта работает как сейчас, на `localStorage`. Самостоятельная работа не создаёт уведомлений учителю, но видна в Т-5.

#### С-8. Статистика учителя и сигналы

- **Цель.** Учитель за минуту видит, кому нужна помощь.
- **Входит.** О-5 для учителя: тепловая карта, «кому нужна помощь» (60 % и 10 задач), «не заходил» (7 дней). Т-5 полностью. А-9 (40 % и 3 сдавших). Ночной пересчёт и витрины. `Heatmap` с пустой ячейкой, `Table` с видом карточек на телефоне.
- **Не входит.** Письма учителю (позже).
- **Зависимости.** С-7.
- **Готово, когда.** Ученик без попыток — пустая ячейка, а не 0 %. Изменение порога в конфиге меняет список без правки кода. Сигнал А-9 приходит один раз на работу и метод.

#### С-9. Письма и напоминания

- **Цель.** Ученики получают дружелюбные напоминания — только с согласия.
- **Входит.** `email_outbox`, письма через почтовый сервис. Согласие на письма в профиле и отписка в один клик без входа. Окно 8:00–21:00 по поясу ученика, не больше одного напоминания в день (сводка). Письма А-1, А-2, А-3, А-4, А-11, А-14 (за 14 и 3 дня и после окончания) — по утверждённым текстам 3.3.
- **Не входит.** А-17 и письма учителю — позже.
- **Зависимости.** С-5, С-6; Наталья утвердила тексты писем.
- **Готово, когда.** Без согласия письмо не уходит (тест). Письмо, наступившее ночью, ждёт окна. Повторный запуск не отправляет дубль. Отписка работает без входа.

#### С-10. Честный интерфейс (П-5, П-8)

- **Цель.** На сайте не остаётся показателей без источника; экран «Ещё» открывает личные разделы.
- **Входит.** Пункты П-5, которым не нужны данные с сервера: честное кольцо разделов у линейной подтемы, точка колокольчика по умолчанию выключена, поиск скрыт до появления поиска, блок «Пройти диагностику», ссылки на `#`, цифры лендинга по решению 45 (счётные — считаются при сборке из конфигов и манифестов; зависящие от учеников и использования — убираются; выдуманных и «примерных» не остаётся), отправка через Formspree в старой домашней работе. Правка экрана «Ещё» по П-8: «Мои задания», «Избранное», «История решений», без «Настроек» (разрешение Натальи). П-14 — только если Наталья к тому времени решит.
- **Не входит.** Блок пользователя из аккаунта (С-3).
- **Зависимости.** Нет. Может идти до С-1 или параллельно.
- **Готово, когда.** Снимки 360 / 768 / 1440 затронутых страниц приложены к pull request. На сайте нет чисел без источника. «Ещё» содержит три новых пункта и не содержит «Настроек».

### 4.3. Где собраны решения и вопросы

- Всё спорное — в разделе «Предложения, требующие решения Натальи» ниже. Новые на этапе 4: **П-16** (код в октябре — к аккаунту или к браузеру) и **П-17** (кто выдаёт коды).
- Отложенное — в разделе «Позже».
- Вопросы к разработке и числа, которых ещё нет, — в разделе «Открытые вопросы» (новые — 18–21).

### Чеклист этапа 4

- [x] Не изменён ни один файл, кроме `docs/CABINET_UX_SPEC.md`
- [x] Меню не тронуто, «Настроек» нет (правка экрана «Ещё» — только в плане сессии С-10, по разрешению Натальи)
- [x] Нет показателей без источника данных (все сущности и числа конфига перечислены)
- [x] Нет выдуманного содержания
- [x] Все термины объяснены (Docker Compose, middleware, RLS, IDOR, `pg_cron`, метка доступа)
- [x] Для каждой сессии указано, нужен ли сервер

---

## Предложения, требующие решения Натальи

Нумерация сквозная. У каждого предложения — варианты ответа; рекомендация отмечена. Решённые предложения остаются в списке с пометкой «Решено».

### П-1. Целевое меню кабинета — **Решено: вариант А** (24.09.2026). Вариант Б — план на время, когда появятся другие учителя

С учётом П-2 в «Мои задания» не три вкладки, а две: «Работы» (фильтр: домашние, тренировочные, самостоятельные) и «Зачёты».

**Вариант А (действующий).**

| Роль | Главная | Задания | Статистика | Мои материалы | Для учителя | Колокольчик | Мои задания | Избранное | История решений |
|---|---|---|---|---|---|---|---|---|---|
| Ученик | Главная кабинета: «Что дальше», ближайшие сроки, слабое место | Банк 13 заданий (как сейчас) | Три вкладки: По заданиям · Попытки · Сроки | Материалы для скачивания и листы моих работ | Карточка своего учителя | Уведомления | Работы · Зачёты; сверху — «Ближайшие сроки» | Отложенные подтемы, разделы, типы задач | Все попытки по датам |
| Учитель | Главная учителя: требует внимания, сроки, классы | Банк заданий с «Выдать как работу» в генераторе | Статистика классов | Генератор и листы выданных работ | Классы · Работы · Зачёты · Ученики | Уведомления о сдачах | Работы и зачёты, выданные мной | Отложенные типы задач | Лента сдач учеников |

Плюсы: меню не меняется совсем; у ученика все задания от учителя в одном месте; «Для учителя» имеет смысл и для ученика. Минусы: «Мои задания» несут и работы, и зачёты, и сроки; у учителя работы видны из двух мест.

**Вариант Б (план на будущее).** Названия пунктов те же, но подписи личных пунктов зависят от роли: у учителя «Мои задания» → «Работы и зачёты», «Избранное» → «Классы», «История решений» → «Ученики», пункт «Для учителя» скрыт. Плюсы: полноценное рабочее место учителя без второго входа. Минусы: подписи пунктов меняются по роли — правка `appNav.ts`.

### П-2. Домашняя и тренировочная работа — **Решено: объединить**

Одна сущность «работа»: вид (домашняя / тренировочная), правила (обычные / «Контроль»), способ сдачи (по одной задаче / целиком).

### П-3. Тренировочные работы — **Решено**

а) Создают учитель и ученик; работа ученика — «самостоятельная», без уведомлений учителю, видна учителю в статистике ученика. б) Оба состава; «формат ЕГЭ» с подписью «из N открытых заданий», N из конфига, состав первой части — в конфиге со сверкой по демоверсии ФИПИ. в) Время на выбор учителя, по умолчанию без ограничения. г) Одна сдача, правка ответов до сдачи. д) Показ результата — настройка работы, по умолчанию сразу; у самостоятельных — всегда сразу.

### П-4. Теоретические зачёты — **Решено**

а) Зачёт независим от тренажёра. б) Порог задаёт учитель, умолчание — в конфиге. в) Не более 3 пересдач, пауза 24 часа, вопросы и варианты перемешиваются; числа — в конфиге.

### П-5. Убрать фейковые показатели — **Решено: в план, в этой сессии не исправлять**

| Что | Файл | Что сделать |
|---|---|---|
| Пользователь «Наталья Витальевна · Преподаватель» в шапке и сайдбаре | `app/src/data/demo.ts` (`demoUser`), `app/src/components/layout/AppShell.tsx` | Показывать имя из аккаунта; до появления аккаунтов — не показывать блок пользователя |
| Кольцо «Вы изучили 6 из M разделов» у линейной подтемы | `app/src/data/demo.ts` (`demoStudied`), `app/src/state/AppState.tsx` (`INITIAL.studied`), `app/src/components/tasks/TopicProgress.tsx` (ветка без `trackKey`) | Включить честный подсчёт дочитанных разделов и для линейной подтемы, как у квадратичной (`theoryProgress`) |
| Точка «есть новые» на колокольчике горит всегда | `app/src/components/ui/Topbar.tsx` (`unread = true` по умолчанию), `AppShell.tsx` не передаёт значение | По умолчанию `false`; включать только по данным уведомлений |
| Поле поиска, которое не ищет | `app/src/components/ui/Topbar.tsx`, `AppShell.tsx` (`search`) | Скрыть до появления поиска или сделать поиск по 13 заданиям |
| «Пройти диагностику» ведёт на заглушку | `app/src/content/tasks.ts` (`tasksPage.start`), `app/src/app/diagnostika/page.tsx` | Убрать блок или переписать в «С чего начать» со ссылкой на открытое задание |
| «Войти», «Начать бесплатно», «Тарифы», «Оферта», «Обработка данных», «Контакты» → `#` | `app/src/content/site.ts` | Пока страниц нет — не показывать пункты или вести на реальные страницы, когда появятся |
| Цифры и таблицы лендинга: «9 980+ задач», результат диагностики «76», таблица «11 «А»», «Что делать дальше» | `app/src/content/landing.ts` (`metrics`, `diagnostics.card`, `path.card`, `teachers.card`) | **Решено (45):** числа, которые можно посчитать по содержимому сайта (прототипы, задания, темы), считаются автоматически при сборке; всё, что требует данных об учениках или использовании (результат диагностики, таблица класса, «Что делать дальше»), убирается до появления реальных данных; выдуманных и «примерных» чисел не остаётся |
| Отчёт старой домашней работы через Formspree | `dz_stereometria_3.html` (`SEND_ENDPOINT`) | Убрать отправку за рубеж (152-ФЗ), оставить копирование отчёта — как в `HTML REVIEW DZ3.md` |

### П-6. Что видит ученик в пункте «Для учителя» — **Решено: карточку своего учителя**

### П-7. Адрес главной кабинета — **Решено: вариант А**

Варианты: А — главная по адресу `/kabinet/`, вошедший пользователь с `/` перенаправляется туда, меню не меняется; Б — `/` показывает лендинг гостю и главную вошедшему; В — пункт «Главная» ведёт на `/kabinet/` (правка меню). **Рекомендация: А.** После входа и ученик, и учитель попадают на `/kabinet/`, при входе по ссылке — туда, откуда пришли (таблица в 2.1).

### П-8. На телефоне «Мои задания», «Избранное» и «История решений» недоступны из навигации — **Решено: добавить на экран «Ещё»**

Экран «Ещё» строится из пунктов шапки и уведомлений (`appNavMore` в `appNav.ts`); личных разделов из сайдбара в нём нет, а сайдбар на телефоне скрыт. Для ученика это значит, что работы учителя на телефоне открываются только с главной и из уведомлений.

| Вариант | Плюсы | Минусы |
|---|---|---|
| 1. Добавить личные разделы (кроме «Настроек») на экран «Ещё» | Шапка, сайдбар и нижняя панель не меняются; все разделы доступны с телефона | Меняется состав экрана «Ещё» — это правка меню |
| 2. Меню не менять; «Мои задания» — через «Все работы» на главной и уведомления; «Избранное» и «История» на телефоне недоступны | Меню не трогается | Главный раздел ученика прячется за главной; два пункта недоступны |
| 3. Заменить «Статистику» в нижней панели на «Мои задания» | Самый короткий путь к работам | Правка нижней панели; статистика уходит в «Ещё» |

**Решено: вариант 1.** На экран «Ещё» добавляются «Мои задания», «Избранное», «История решений»; «Настройки» не добавляются. Разрешение Натальи — только на эту правку экрана «Ещё»; в этой сессии код не меняется.

### П-9. «Избранное» и «История решений» — **Решено: позже, заглушки остаются**

Варианты: 1) оба позже, до этого остаются честные заглушки «Раздел готовится» — **рекомендация**: MVP держится на работах и зачётах; 2) «История решений» в MVP (данные всё равно копятся в `attempts`), «Избранное» позже; 3) оба в MVP.

### П-10. Что открыто в платном задании без кода — **Решено: только «О задании»**

Доступ продаётся к заданию №12 целиком. Варианты: 1) закрыто всё, кроме вкладки «О задании» — ученик видит, что внутри, — **рекомендация**; 2) закрыто всё, в банке только карточка с замком; 3) открыты «О задании» и «Теория», закрыты опорные задачи, тренажёр и генератор.

### П-11. Может ли ученик сдать зачёт без выдачи учителем — **Решено: да, с условием**

Самостоятельный зачёт доступен, только если опубликованных вопросов в банке задания больше, чем вопросов в одной попытке; каждая попытка — случайная выборка. Попытки помечаются «самостоятельный», не расходуют пересдачи выданного зачёта, видны учителю в карточке ученика. Если вопросов не больше, чем в попытке, кнопки нет.

### П-12. Что видит ученик после зачёта — **Решено: вариант 1**

| Вариант | Плюсы | Минусы |
|---|---|---|
| 1. «Сдан» — пояснения ко всем вопросам; «не сдан» — только методы с ошибками, без верных ответов | Пересдача не превращается в перебор ответов | Ученик при «не сдан» не видит, где именно ошибся |
| 2. Верные ответы и пояснения после любой сдачи | Максимум обратной связи | Пересдачу можно пройти по памяти, не повторяя теорию |
| 3. Только «сдан / не сдан» и процент | Просто | Мало пользы для подготовки |

**Рекомендация: 1.**

### П-13. Сколько вопросов в попытке зачёта — **Решено**

Число задаёт учитель при выдаче. По умолчанию — все опубликованные. Если учитель задал меньше — случайная выборка из банка для каждой попытки.

### П-14. Переименовать «Мои задания» — **Решено: позже** (меню в этой сессии не меняется)

Название раздела с работами и зачётами легко спутать с пунктом «Задания» (13 номеров ЕГЭ). Варианты на будущее:

| Вариант | Плюсы | Минусы |
|---|---|---|
| 1. «Работы и зачёты» (короткая подпись для нижней панели и «Ещё» — «Работы») | Точно называет содержимое, не пересекается со словом «Задания» | Длинное в полной форме |
| 2. «Мои работы» | Коротко, привычно для школы | Зачёт — не работа; название неполное |
| 3. «От учителя» | Сразу ясно, откуда содержимое | Самостоятельные работы и зачёты ученика сюда не подходят по смыслу |

Рекомендация на будущее: **вариант 1**. Правка — одна строка в `appNav.ts` (`label` и `short` пункта), когда Наталья решит.

### П-15. Ежедневная сводка учителю письмом — **Решено: писем учителю пока нет**

Сводка — только в кабинете. Возможность включить письмо-сводку (одно письмо в день, только при согласии учителя) записана в «Позже».

### П-16. Код доступа в октябре: к аккаунту или к браузеру — **Решено: без аккаунта (вариант 2)**

Повторный ввод того же кода после очистки браузера или на новом устройстве — новая активация в пределах лимита 3 (решения 41, 43).

Этапы 1–2 спроектированы так, что код привязывается к аккаунту. Но аккаунты требуют регистрации, согласия на ПДн, уведомления в Роскомнадзор и почтового сервиса — это сессия С-3 и юридическая работа, которые могут не успеть к октябрю.

| Вариант | Плюсы | Минусы |
|---|---|---|
| 1. Код только за аккаунтом: сначала С-3, потом С-2 | Сразу окончательная схема; доступ на всех устройствах ученика | Первые продажи ждут регистрации, согласий, Роскомнадзора и почты |
| 2. **В октябре код без аккаунта**: сервер проверяет код и выдаёт метку доступа — cookie (небольшая подписанная запись в браузере, которую нельзя подделать); число устройств на код ограничено лимитом активаций. Когда появятся аккаунты (С-3), при регистрации код переносится в аккаунт | Коды работают с октября; персональные данные не собираются, поэтому регистрация и Роскомнадзор не блокируют продажи; проверка всё равно только на сервере | После очистки браузера код вводится заново и расходует активацию; до С-3 доступ живёт на устройстве, а не на человеке |

План сессий написан под вариант 2: С-2 идёт раньше С-3, экран У-5 до С-3 не просит входа.

### П-17. Кто и где выдаёт коды доступа — **Решено: отдельный аккаунт администратора; способ входа в С-2 выбирает Наталья**

Задача: в С-2 Наталья должна входить на страницу выдачи кодов (`/admin/kody/`), а общая система аккаунтов появится только в С-3. Условия: вход только для одного администратора, открытой регистрации нет, чужие персональные данные не собираются, а в С-3 вход должен перейти в систему ролей без переделки. Рекомендации нет — выбор за Натальей.

**Вариант 1. Настоящий аккаунт с первого дня, регистрация закрыта.**

Как устроено в С-2. Авторизация Supabase уже стоит на сервере после С-1. Открытая регистрация в её настройках выключена. Разработчик один раз создаёт аккаунт Натальи: её почта и пароль, который Наталья задаёт сама; плюс второй фактор — код из приложения на телефоне. В таблице `profiles` у аккаунта роль `admin`. Страница `/admin/kody/` на сервере пускает, только если человек вошёл и у него роль `admin`.

Как переходит в С-3. Ничего не переделывается. С-3 включает регистрацию учеников и добавляет роли `student` и `teacher` в ту же таблицу, аккаунт администратора остаётся как был.

| Плюсы | Минусы |
|---|---|
| Сразу окончательная схема, в С-3 ничего не меняется | С-2 немного больше: страница входа, второй фактор |
| В журнале `audit_log` видно, кто именно выдал или сбросил код | Чтобы Наталья сама задала пароль по ссылке из письма, почтовый сервис нужен уже в С-2; без него пароль задаётся вместе с разработчиком при настройке |
| Пароль Наталья может сменить сама, есть второй фактор | |
| Чужих персональных данных нет — только почта Натальи | |

**Вариант 2. Временный вход по одному паролю из настроек сервера.**

Как устроено в С-2. Страница `/admin/kody/` закрыта одним паролем. Сервер хранит не сам пароль, а его хеш — в секретных настройках сервера (как сейчас пароли FTP хранятся в секретах GitHub). Вход — форма с одним полем, с лимитом попыток; после входа сервер выдаёт метку сессии на ограниченное время. Аккаунтов и таблицы пользователей нет.

Как переходит в С-3. Пароль заменяется аккаунтом администратора. Адрес и сама страница не меняются; меняется одна проверка «можно ли сюда»: была «верный пароль», станет «вошёл и роль `admin`». Пароль удаляется из настроек.

| Плюсы | Минусы |
|---|---|
| Самый быстрый и простой путь к кодам в октябре | В журнале видно только «администратор», без имени (пока администратор один — это не мешает) |
| Нет аккаунтов, писем и второго фактора | Сменить пароль может только разработчик, в настройках сервера |
| Чужих персональных данных нет | Нет второго фактора: защита держится на длинном пароле и лимите попыток |
| | В С-3 одну проверку всё равно нужно заменить — маленькая правка, но правка |

Нужен ответ: вариант 1 или 2.

---

## Позже

То, что решено не проектировать сейчас.

1. **Регистрация младше 14 лет через родителя**: почта родителя, согласие законного представителя (`LEGAL IP RU.md` §4). Сейчас регистрация — с 14 лет.
2. **Вариант Б меню** (П-1) — когда появятся другие учителя.
3. **Роль «родитель»** (`parent` в `TECH ARCHITECTURE.md` §8) — только чтение статистики ребёнка.
4. **«Избранное» и «История решений»** — решено позже (П-9), заглушки остаются.
5. **Письмо-сводка учителю** (П-15) — одно письмо в день, только при согласии учителя.
6. **Возврат неактивного ученика письмом** (А-17): 10 дней без входа, не чаще раза в 14 дней.
7. **Генерация задач тренажёра на сервере** — сейчас тренажёр собирает задачи в браузере (см. 4.0); для работ и зачётов задачи генерируются на сервере с С-5. Решено: позже (решение 46).
8. **Оплата через ЮKassa** и автоматическая выдача кода после оплаты (пакет А3 дорожной карты).
9. **Переименование «Мои задания»** (П-14) — рекомендация на будущее «Работы и зачёты».

---

## Открытые вопросы

Закрытые вопросы остаются с ответом, чтобы решения не потерялись.

1. ~~Как вводятся вопросы зачётов~~ — **закрыт**: редактор в кабинете учителя, сразу в MVP (Т-7).
2. ~~Перенос прогресса из браузера~~ — **закрыт**: один раз при первом входе, по кнопке, только реальные счётчики (У-4).
3. ~~Согласие младше 14 лет~~ — **закрыт**: регистрация с 14 лет, путь через родителя — «Позже».
4. ~~Обязательны ли школа и класс~~ — **закрыт**: обязательны.
5. ~~Почтовый сервис~~ — **закрыт**: абстрактный «почтовый сервис», российский, данные в России.
6. ~~Состав первой части ЕГЭ~~ — **закрыт**: конфиг и сверка с официальной демоверсией ФИПИ.
7. ~~Пороги «кому нужна помощь»~~ — **закрыт**: точность по заданию ниже 60 % при не менее 10 решённых задачах, в конфиге.
8. ~~Порог зачёта по умолчанию~~ — **закрыт**: 80 %, в конфиге.
9. ~~Библиотека для QR~~ — **закрыт**: `uqr`, обоснование в 2.6.
10. ~~Тексты знакомства и согласия~~ — **закрыт**: черновики знакомства в У-4 (на утверждение Натальи), согласие — «текст юриста».
11. ~~Правило опоздания~~ — **закрыт**: принимать с пометкой «сдано с опозданием», учитель закрывает приём вручную, настройка у работы.
12. ~~Список классов~~ — **закрыт**: 9, 10, 11, «Другое», в конфиге.
13. ~~«Не заходил N дней»~~ — **закрыт**: отметка учителю после 7 дней, письмо ученику после 10 дней и не чаще раза в 14 дней.
14. ~~Размер самостоятельной попытки зачёта~~ — **закрыт**: 10 вопросов.
15. ~~Порог «много ошибок в одном методе»~~ — **закрыт**: не менее 40 % сдавших ошиблись хотя бы в одной задаче метода, при не менее 3 сдавших.
16. ~~Предупреждение об окончании доступа~~ — **закрыт**: доступ до 31.07.2027 (у каждого кода своя дата), предупреждения за 14 и за 3 дня.
17. ~~Окно отправки писем~~ — **закрыт**: 8:00–21:00 по времени ученика, пояс из браузера при регистрации, по умолчанию Москва.
18. ~~Лимит активаций~~ — **закрыт**: 3 на код, в конфиге; кнопка «Сбросить активации» с записью в `audit_log`.
19. ~~VDS и бэкапы~~ — **закрыт**: VDS на Selectel арендует Наталья на своё имя; бэкапы — в Yandex Object Storage; Cloudflare R2 убирается из документов (Д-1).
20. ~~Цифры лендинга~~ — **закрыт**: решение 45.
21. ~~Генерация тренажёра на сервере~~ — **закрыт**: позже.
22. Способ входа администратора в С-2 — вариант 1 или 2 в П-17. Нужен до С-2.
23. «1 → ∞ уникальных вариантов» на лендинге — не счётное число и не данные об учениках, а утверждение о генераторе. По решению 45 его нельзя посчитать при сборке: убрать или заменить фразой без числа — решает Наталья до С-10.
24. «Сбросить активации»: предлагается, что сброс отзывает все прежние метки этого кода (они перестают открывать задание) и обнуляет счётчик — иначе сбросами можно открыть код на любом числе устройств. Подтвердить до С-2.
25. У какого регистратора зарегистрирован домен budetege.ru — там в конце С-1 меняется адрес сервера (A-запись).
26. Правка других документов по Д-1 (убрать Cloudflare R2) затрагивает файлы кроме `docs/CABINET_UX_SPEC.md` — нужно разрешение сделать её в этой ветке или отдельной сессией.
