<?php
/*
 * Образец настроек обработчика заявок. НЕ в веб-корне и НЕ в сборке.
 *
 * На хостинге файл кладётся руками один раз:
 *   ~/zakaz-private/config.php   (папка рядом с public_html, не внутри)
 * Репозиторий публичный, поэтому настоящий файл здесь не хранится.
 * docs/SERVICE TEACHERS LANDING.md, §9.2–9.3.
 */

return [
    /* Ящик заявок: сюда уходят письма. Российский сервис (решение 24.09). */
    'komu' => 'zakaz@budetege.ru',
    /* Отправитель — адрес на том же домене, иначе письма уйдут в спам. */
    'ot' => 'zakaz@budetege.ru',
    'ot_imya' => 'Материалы под ключ',
    /* Соль для хеша адреса отправителя в лимите частоты: любые 32+ случайных знака. */
    'sol' => 'ЗАМЕНИТЬ-на-длинную-случайную-строку',
    /* Вложения одного письма, байт. 25 МБ файлов в письме весят около 34 МБ,
       а многие ящики принимают 25–30 МБ. Больше — несколько писем. */
    'limit_pisma_bayt' => 18 * 1024 * 1024,
    /* 'mail' — отправка хостингом; 'fayl' — письма пишутся в test-pisma/ (для проверки). */
    'otpravka' => 'mail',
];
