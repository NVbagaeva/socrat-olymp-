/**
 * Значки раздела №4, которых нет в общих наборах.
 *
 * Рисуются теми же средствами, что и остальные значки проекта:
 * инлайновый SVG, поле 24×24, обводка currentColor, заливки нет.
 * Цвет и размер задаёт место вставки. Библиотеки иконок в проекте
 * нет и не заводится — см. ui/NavIcons.tsx.
 *
 * Здесь только то, что нужно вкладке «О задании» и чего в общих
 * наборах не нашлось: шапочка выпускника и росчерк под рукописной
 * подписью. Остальные значки вкладки взяты готовыми — столбики
 * статистики из ui/NavIcons, восклицательный знак из ui/StateIcons,
 * лампочка из tasks/prep/PrepIcons.
 */

/** Шапочка выпускника: заголовок «Какие задачи встречаются?». */
export function ShapochkaIcon() {
  return (
    <svg
      className="z4-ico"
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth="1.6"
      strokeLinecap="round"
      strokeLinejoin="round"
      aria-hidden="true"
      focusable="false"
    >
      {/* Доска шапочки: ромб, вид сбоку-сверху. */}
      <path d="M12 3.4 21.8 8 12 12.6 2.2 8z" />
      {/* Тулья: борта уходят вниз и подбираются к центру. */}
      <path d="M6.6 9.9v4.5c0 .6.34 1.14.92 1.45 1.32.71 2.85 1.06 4.48 1.06s3.16-.35 4.48-1.06c.58-.31.92-.85.92-1.45V9.9" />
      {/* Кисточка: от правого угла доски вниз, узелок ниже тульи. */}
      <path d="M19.6 9v8.4" />
      <path d="M19.6 17.4a1.6 1.6 0 1 0 0 3.2 1.6 1.6 0 0 0 0-3.2z" />
    </svg>
  );
}

/**
 * Росчерк под рукописной подписью — декор, как на макете.
 *
 * Не картинка, а SVG на токенах: цвет наследуется от подписи.
 * Поле вытянутое, поэтому своё, а не общее 24×24, и линия тянется
 * по ширине места вставки.
 */
export function Roscherk() {
  return (
    <svg
      className="z4-about__roscherk"
      viewBox="0 0 320 34"
      preserveAspectRatio="none"
      fill="none"
      stroke="currentColor"
      strokeWidth="2"
      strokeLinecap="round"
      aria-hidden="true"
      focusable="false"
    >
      <path d="M4 8c46 17 110 23 196 18 42-2 76-8 104-17-22 15-58 23-108 25" />
    </svg>
  );
}
