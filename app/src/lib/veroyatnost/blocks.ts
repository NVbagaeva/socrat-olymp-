/**
 * Блоки задачника: по ним фильтрует тренажёр.
 *
 * Названия блоков задания №4 взяты из самого задачника
 * Е. А. Ширяевой: «Блок 1. ФИПИ» делится там на два подраздела,
 * и чипы тренажёра повторяют эти два названия слово в слово.
 *
 * Блок — это ещё и порядок: прототипы в тренажёре и в списке идут
 * в том же порядке, что и здесь.
 */

import { METODY_5 } from './metody5';

export interface Blok {
  /** Идентификатор: он же значение фильтра. */
  id: string;
  /** Название на кнопке фильтра. */
  nazvanie: string;
  /** Короткая форма для узкой ленты чипов. */
  korotko: string;
}

export const BLOKI_4: readonly Blok[] = [
  {
    id: 'klassicheskoe',
    nazvanie: 'Классическое определение вероятности',
    korotko: 'Классическое',
  },
  {
    id: 'statisticheskoe',
    nazvanie: 'Статистическое определение вероятности',
    korotko: 'Статистическое',
  },
  /* Блока в задачнике нет: задачи составлены по схеме автора,
     карточка рисует к ним координатную прямую. */
  {
    id: 'pryamaya',
    nazvanie: 'Координатная прямая',
    korotko: 'Координатная прямая',
  },
];

/**
 * Блоки задания №5 — двенадцать методов, утверждённых автором
 * (metody5.ts): блок банка и метод здесь одно и то же. Порядок —
 * порядок методов, задачи 1–58 задачника Е. А. Ширяевой разложены по
 * ним диапазонами номеров.
 *
 * В самом задачнике подзаголовков нет: задачи идут подряд. Деление —
 * автора, по способу решения.
 */
export const BLOKI_5: readonly Blok[] = METODY_5.map((m) => ({
  id: m.id,
  nazvanie: m.nazvanie,
  korotko: m.nazvanie,
}));
