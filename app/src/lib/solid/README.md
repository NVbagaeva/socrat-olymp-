# Движок стереометрических чертежей, задание №3

Живёт в `app/src/lib/solid/` рядом с `graph/` и так же не знает о React:
модель → строка SVG. Компонент `components/solid/Solid.tsx` вставляет
эту строку на страницу.

## Слои

| Файл | Что делает |
|---|---|
| `vec.ts` | векторы в пространстве и на плоскости |
| `project.ts` | два вида (`ortho`, `cabinet`) за общим интерфейсом `View` |
| `model.ts` | типы модели: тела, пометки, описание; помощники для граней |
| `visibility.ts` | видимость: линия делится на видимые и скрытые куски по модели |
| `render.ts` | `renderSolid(model) -> SVG`; оформление в `THEME`, цвета только токенами |
| `figures.ts` | сборщики тел: призмы, пирамиды, параллелепипеды, усечённые тела |
| `catalog.ts` | 45 базовых фигур старого каталога, для витрины |

## Виды

Видов проекции два, и это следствие геометрии, а не удобства.

| Вид | Кто рисуется | Что даёт |
|---|---|---|
| `ortho` | пирамиды, тела вращения, комбинации | шар — круг, горизонтальная окружность — симметричный эллипс с горизонтальной большой осью |
| `cabinet` | призмы, параллелепипеды, куб | переднее ребро AB строго горизонтально, глубина уходит вправо-вверх под 60° с сокращением 0,4 |

Одним видом обойтись нельзя. Горизонтальное AB при диагональном AD
требует, чтобы луч зрения не был перпендикулярен плоскости чертежа
(косоугольная проекция), а при таком луче шар проецируется в эллипс,
и горизонтальная окружность кренится: у цилиндра один край верхнего
основания оказывается выше другого. Ортогональная проекция держит круги
ровными, но в ней горизонтально может лечь только одно направление
основания, и перпендикулярное ему встаёт строго вертикально, сливаясь
с боковыми рёбрами.

Внутри одного чертежа вид всегда один: комбинации тел (шар в кубе,
призма в цилиндре) рисуются целиком в `ortho`, иначе вписанное тело
перестало бы касаться описанного. Поэтому куб на отдельном чертеже
и куб в комбинации показаны в разных ракурсах — так же, как в учебниках
и в старых чертежах.

Вид указывается полем `view` модели (по умолчанию `ortho`), а
ориентация правильного основания — `polygonStart(n, view)`: в `cabinet`
это 270° − 180°/n (сторона AB спереди и горизонтальна), в `ortho` углы
подобраны так, чтобы ни одно ребро не легло вдоль луча зрения и ни одна
вершина не встала на ось чертежа.

## Видимость

Линия опрашивается в десятках точек; точка скрыта, если между ней
и зрителем лежит грань непрозрачного тела (проверка попадания
в проекцию грани и сравнение глубин) или поверхность шара. Границы
кусков уточняются делением пополам. Своё ребро своя грань не прячет.

Тела вращения для заслонения разбиты на 48 граней, а их собственные
контуры считаются точно и через сам вид: контурная образующая цилиндра
стоит на `view.silhouette`, у конуса она отстоит от неё на
`view.coneTangent`, ближняя половина основания — `view.nearSide`,
контур шара — `view.spherePoint`. Ни одна из этих формул не знает,
какой вид активен.

В комбинациях внешнее тело помечается `glass: true`: сквозь него видно
всё, что внутри, но свои невидимые рёбра оно по-прежнему прячет само.
Внутреннее тело непрозрачно и прячет чужие линии, включая
вспомогательные внутри себя.

## Модель

```ts
const model: Model = {
  alt: 'Куб ABCDA₁B₁C₁D₁ с диагональю AC₁',
  bodies: [box(4, 4, 4)],
  lines: [{ a: A, b: C1 }],
  angles: [{ at: C, u: dir(C, A), v: dir(C, C1) }],
};
```

Тела: `polyhedron` (вершины, грани, имена), `cylinder`, `cone`
(в том числе усечённый и перевёрнутый сосуд), `sphere` (экватор,
сечения). Пометки: `marks` (точки с подписью), `lines`
(вспомогательные отрезки с подписью), `angles` (знак прямого угла),
`sections` (сечение с заливкой), `measures` (число у ребра).

## Оформление

Цвета в `THEME` только через переменные: `--solid-edge`, `--solid-face`,
`--solid-liquid` (объявлены в `styles/tokens.css`), `--graph-accent`
для вспомогательных построений, `--color-text` для подписей,
`--color-surface` для обводок. Невидимые рёбра — тот же цвет
с прозрачностью и штрихом. Шрифт подписей — `--font-sans` (Inter),
индексы — `tspan` меньшего кегля.

Подписи расставляются автоматически: у каждой перебираются
16 направлений на двух расстояниях, штрафуются пересечения с линиями,
точками и другими подписями, поощряется положение снаружи фигуры.

## Витрина

`src/app/styleguide/solid-3/page.tsx` — все 45 фигур каталога рядом
со старыми чертежами из `content-source` (читаются на сборке, только
для сравнения). В меню страницы нет.
