/* paginate.js — классический скрипт без экспортов: сборка листа в CI
   кладёт его в документ как есть, и export там был бы ошибкой. Для
   бандла он импортируется ради побочного эффекта — window.sheetPaginate. */
export {};
