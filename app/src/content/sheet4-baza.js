/* content/sheet4-baza.js — слова печатной базы «Задание 4»: все
   прототипы тренажёра с вариантами 1–10.

   Здесь и только здесь лежат тексты листа базы: подзаголовок, состав
   разделов, двойники конспекта, имена файлов. Название задания и
   строка компактной шапки — из LIST_4 в content/veroyatnost.ts, как
   на сайте; шапка и подвал общие с сборником №12 — берутся оттуда.
   Условия, решения и ответы приходят из банка lib/veroyatnost/ и
   здесь не лежат.

   Порядок — по разделам конспекта (три блока подготовки №4, их
   заголовки — из lib/veroyatnost/podgotovka4.ts), внутри раздела —
   по номеру прототипа: сначала прототипы конспекта k4-…, затем
   прототипы задачника p4-…, внутри прототипа варианты 1–10.
*/

import { head, foot } from './sheet12.js';

export { head, foot };

/** Подзаголовок под названием. Даты на листе нет намеренно. */
export const subtitle = 'Печатная база тренажёра: все прототипы, варианты 1–10';

/** Сколько вариантов каждого прототипа идёт в базу: с первого по этот. */
export const variants = 10;

/**
 * Двойники: задачи конспекта, которые есть в банке прототипов
 * задачника тем же сюжетом и тем же методом. Такая задача печатается
 * внутри своего прототипа задачника (у большинства она и лежит там
 * одним из вариантов), а прототип конспекта в базу отдельно не идёт —
 * иначе один сюжет шёл бы дважды. Ключ — прототип конспекта, значение —
 * прототип задачника, который его заменяет; сборка проверяет, что он
 * есть в банке.
 */
export const dvoyniki = {
  'k4-07': 'p4-14',
  'k4-08': 'p4-01',
  'k4-09': 'p4-07',
  'k4-12': 'p4-15',
  'k4-13': 'p4-15',
  'k4-16': 'p4-05',
};

/**
 * Разделы базы — блоки конспекта по порядку; id — блок PODGOTOVKA_4,
 * заголовок и приписка берутся оттуда. Прототипы конспекта попадают
 * в свой блок сами; прототипы задачника расставлены здесь по смыслу
 * сюжета: ids — их список. Прототип банка, не названный ни в одном
 * разделе, — ошибка сборки, а не молчаливая потеря.
 */
export const sections = [
  {
    id: 'opredelenie',
    ids: [
      'p4-01', 'p4-03', 'p4-06', 'p4-07', 'p4-08', 'p4-09', 'p4-10', 'p4-11', 'p4-12',
      'p4-13', 'p4-14', 'p4-16', 'p4-17', 'p4-18', 'p4-19', 'p4-20', 'p4-21',
    ],
  },
  { id: 'zhrebiy', ids: ['p4-02', 'p4-04', 'p4-15'] },
  { id: 'geometricheskaya', ids: ['p4-05', 'p4-22', 'p4-23'] },
];

/** Подпись метода в плашке прототипа: «Метод 1 · Прямой пересчёт исходов». */
export const metodLabel = (nomer, nazvanie) => 'Метод ' + nomer + ' · ' + nazvanie;

/** Раздел «Ответы» файла для учителя. */
export const otvety = { title: 'Ответы', note: 'по разделам, сквозная нумерация' };

/** Названия файлов базы. */
export const files = {
  uchenik: 'Zadanie_4_baza_uchenik',
  uchitel: 'Zadanie_4_baza_uchitel',
};

const content = { head, foot, subtitle, variants, dvoyniki, sections, metodLabel, otvety, files };

export default content;
