/**
 * Вкладка «Опорные задачи» — подпись и адрес, одни на все задания.
 *
 * Вкладка есть у заданий №3, №4, №5, №8 и №12, и называлась она везде
 * «Подготовительные задачи». Имя и хвост адреса живут здесь, чтобы
 * у одной вкладки не завелось пяти разных подписей.
 *
 * `staryyTail` — прежний адрес. По нему стоят страницы-редиректы
 * (components/layout/Redirect.tsx): сохранённые ссылки и закладки
 * учеников не должны ломаться от переименования.
 */
export const OPORNYE = {
  title: 'Опорные задачи',
  /** Часть адреса после адреса раздела. */
  tail: 'opornye-zadachi/',
  /** Прежняя часть адреса: с неё ведёт редирект. */
  staryyTail: 'podgotovka/',
} as const;
