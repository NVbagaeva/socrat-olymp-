/**
 * Загрузка услуги «Материалы под ключ»: правится руками, раз в неделю
 * или когда меняется очередь. §4.3 документа услуги.
 *
 * Как править с телефона: GitHub → этот файл → карандаш → поменять
 * даты → Commit → pull request → слить. Сайт обновится за 2–4 минуты.
 *
 * Забытый файл не врёт: прошедшая дата старта не показывается
 * («Принимаю заказы»), а отметка «занято» за прошлую неделю
 * считается снятой. Логика — lib/usluga/zagruzka.ts.
 */

import type { ZagruzkaFayl } from '@/lib/usluga/zagruzka';

export const zagruzka: ZagruzkaFayl = {
  /** Ближайший старт обычного заказа. */
  nextStart: '2026-09-29',
  /** Срочное место на неделе weekOf: 'free' — свободно, 'taken' — занято. */
  urgentSlot: 'free',
  /** Понедельник недели, к которой относится urgentSlot. */
  weekOf: '2026-09-28',
};
